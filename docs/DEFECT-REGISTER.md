# Defect Register & Binding Decisions

**Owner:** Dev 1 · **Created:** 2026-07-29 · **Last updated:** 2026-08-21 · **Status:** ACTIVE

> **AMENDED 2026-07-30.** The line below previously read *"none is waiting on Dev 1"*. A
> route-by-route frontend wiring audit run **after** that claim was made found **three Dev 1
> defects** — D31, D32, D33 — one of which breaks the documented frontend setup path. The
> claim was wrong when written. Full audit:
> **`docs/reports/frontend-wiring-audit-2026-07-30.md`**.
>
> See also `docs/handoffs/DEV1-FINAL-HANDOVER.md` (amended). All open entries carry a named
> owner and a trigger.
>
> ⚠️ **ID COLLISION — this actually happened, twice, on 2026-07-30.**
>
> 1. The wiring-audit tooling emitted labels `D-1…D-30` that collide numerically with register
>    ids meaning something entirely different (audit `D-28` = a `package_builder` subscript bug;
>    register **D28** = the chapter/subsection hierarchy inversion). The report therefore
>    relabels everything **`W-*`**. **`W-…` ids are NOT register ids.**
> 2. **Two devs independently claimed D29 and D30 the same day.** The Sprint 2 completion audit
>    landed D29 (DPDP `user_consents`) / D30 (3 failing tutor tests) on `main` while Dev 1's
>    wiring audit was being written against the same free ids. Dev 1's entries were renumbered
>    **D31–D37** on discovery; nothing was overwritten.
>
> **Rule going forward: re-read the highest allocated id from `main` immediately before writing
> a new entry, and never from a branch.** This register has no id allocator, so concurrent
> branches will keep colliding until it does.
>
> ⚠️ **ID COLLISION, third occurrence, 2026-08-06.** `main` allocated D57 (withdrawn)/D58
> (eval harness)/D59 (unbounded queries) the same day `sprint3-master` independently allocated
> its own D57 (later withdrawn as a duplicate of D31)/D58 (PdfBookmark crash)/D59 (rate-limit
> ES256 regression) — the exact rule stated above, violated a third time from a long-lived
> branch that hadn't re-synced with `main` before allocating. On the 2026-08-06 merge of `main`
> into `sprint3-master`: `main`'s D57/D58/D59 kept their numbers (already the trunk's canonical
> meaning); `sprint3-master`'s D58/D59 were renumbered **D60**/**D61**. No entry was overwritten.
>
> ⚠️ **ID COLLISION, fourth occurrence, 2026-08-12/13.** The "Sprint 3 CES v2 audit" (below,
> §D61–D65) allocated ids **D61, D62, D63, D64** for five CES-subsystem defects on 2026-08-12,
> without re-reading the highest id already in use on `main` first — the exact rule this banner
> states above, violated a fourth time. All four collide with pre-existing, unrelated entries:
> D61 with the closed beta-access-allowlist test regression, D62 with the closed
> `LANGFUSE_HOST` fix, D63 with **both** the closed `package_builder` fix and Dev 4's
> INTERVENING-trap fix (already a two-way collision before this), and D64 with Dev 4's open
> confusion-type-interventions-uncapped defect. Local reconciliation notes are added at each
> collision site below rather than renumbering the audit's own section, matching this file's
> established convention (see the D63 note). Found while reviewing `docs/ces-lifecycle-learner-
> scenarios.md` / `docs/ces-decisions-developer-guide.md` against the register during the
> 2026-08-13 `throwaway/lesson-planner-batch-fix` merge.

This file exists because `docs/deferred-work.md` did not. That file was created to hold
deferred findings; it contains **zero items** across a period in which the story files
accumulated 131 `[Review][Defer]` markers. A register nobody writes to is worse than no
register, because it lets people believe the deferral was recorded.

## How this file is binding

Every entry names an **enforcement**: a test, a CI gate, or the word **DISCIPLINE**.

`DISCIPLINE` means *nothing stops us breaking this*. That label is not an admission of
laziness — it is the most important column in the table. On 2026-07-29 we established
empirically that prose guidance does not hold: Dev 1 wrote `DEV1-FIX-PLAN.md`, then
deviated from it four times in one day (the "Honest framing" instruction, sub-tasks 5.1,
5.5 and 6.3). The plan was correct each time. It was simply not enforced.

**So the rule is: prefer a failing test to a paragraph.** Any decision that *can* be
machine-checked *must* be, and the count of `DISCIPLINE` rows is the honest measure of how
fragile this register is.

## Closure rule

An entry may move to `CLOSED` only when its **Enforcement** column names something that
runs in CI and would fail if the defect returned. "Fixed" is not closure. A fix without an
enforcement is `FIXED-UNGUARDED`, which is a distinct and worse state than it sounds —
`{**state, ...}` was "fixed" once and reached 18 sites.

---

## Part 1 — Root causes

Ranked by number of defects each explains. Derived from a 10-agent analysis on 2026-07-29;
every claim below was re-verified by execution before being written here.

| # | Root cause | Explains | Evidence |
|---|---|---|---|
| **RC-1** | **Mocks are written by the consumer and never reconciled with the producer.** `CLAUDE.md`'s anti-deadlock rule — *"each dev mocks the other's interface"* — was correct for Week 1 and has **no expiry clause**. It is now the primary bug-concealment mechanism. | 12 of 17 | Bug D9 replanted on a different table today: **776 passed, 0 failed**. 567 of 2,328 assertions (24%) describe a conversation with a mock rather than an outcome. |
| **RC-2** | **Nothing in the process reads both ends of a seam.** `graph.py` is ~4,600 lines; seams are relationships between functions ~2,000 lines apart, and a story diff shows one end. | 9 of 17 | D11 is the pure case: a 6,000-char prompt window and a 90%-coverage guard, each defensible, jointly satisfiable only when `len(raw_text) ≤ 6,667`. |
| **RC-3** | **CI verifies roughly half of what the ACs claim.** | why they survived weeks | `pytest tests/unit tests/integration` skips the root `tests/` dir (22 real failures) and uses `-x`. The web job has **no `pnpm test`** at all. |
| **RC-4** | **Detection works; disposition does not.** Findings are recorded and never closed. "Matches existing accepted pattern" is a ratchet that turns instance #1 of a defect class into standing justification for #2–18. | the *feeling* of recurrence | 131 defer markers → **0 registered items**. That ratchet is literally how `{**state, ...}` reached 18 sites. |
| **RC-5** | **The mandated 5-agent review gate is a 3-agent gate in the skill.** | D12–D17 | `CLAUDE.md` requires Story Quality + Process Integrity; `bmad-code-review/SKILL.md` ships only Blind Hunter, Edge Case Hunter, Acceptance Auditor. Story Quality is chartered for exactly the question that would have killed D12–D17: *"does this AC measure the behaviour, or a proxy for it?"* |

### The finding that reframes everything

**9 of 11 pre-existing defects never worked for a single minute.** Only 1 of 17 is a true
regression. This is not an unstable codebase — it is a codebase whose verification never
confirmed anything worked, so we meet the same never-tested assumption in a new subsystem
and experience it as recurrence.

Median time-to-discovery was **13 days**, which measures *when a human read the code*, not
when anything detected anything.

---

## Part 2 — Defect register

Class key: **BW** born-wrong (never worked) · **REG** regression · **LAT** latent, unlatched
by a later fix · **SELF** self-inflicted while fixing · **EXT** wrong model of an external
system.

### Closed — fixed AND guarded

| ID | Defect | Class | Decision | Enforcement |
|----|--------|-------|----------|-------------|
| D1 | `return {**state, ...}` at 18 sites re-appended six `operator.add` channels → 16× in one clean run; ~4× real TTS spend | REG | Return only the node's own reduced keys. Never spread state into a reducer channel. | `test_node_return_shape.py` — AST scan with taint-tracking; e2e duplication assertions |
| D2 | `_FAN_OUT_STATE_KEYS` omitted `"tier"`; a `Send()` payload *replaces* state, so all six Phase-1 nodes read the T2 default. Every T1/T3 lesson shipped T2 content while marked complete. | BW | Fan-out payload must carry every key any Phase-1 node reads. | `test_fan_out_state_keys.py`; `test_tier_differentiation_and_cost.py` (kills the mutation that drops `"tier"` at source) |
| D3 | **Zero** OpenAI exceptions derive from `httpx.HTTPError`, so `with_retry` never classified any of them. A 429 was fatal. | EXT | Classify SDK exceptions by `status_code`, network class by type. Guard the import. | `test_retry.py::test_openai_*`; `test_openai_exceptions_are_not_httpx_derived` pins the premise |
| D4 | SDK default `max_retries=2` under `with_retry(3)` = 9 HTTP requests/call, two backoff schedules, 600s read timeout | EXT | `max_retries=0`; `core/retry.py` owns retry exclusively. Timeout must be an explicit `httpx.Timeout(..., connect=5.0)` — a bare float overwrites `connect`. | `test_openai_clients_disable_sdk_retries_and_set_explicit_timeouts` |
| D5 | `record_failure` inside the retried function counted attempts, not logical calls → breaker tripped ~3× too fast once D3 was fixed | LAT | Breaker accounting lives in `guard_breaker`, outside the retry decorator. Never in a provider module. | `test_breaker_accounting.py`; `test_tts_providers_no_longer_record_breaker_outcomes_themselves` |
| D6 | `raise ... from None` does **not** clear `__context__`; the httpx exception it holds embeds the key-bearing URL | EXT | Build the sanitized error inside `except`, raise it **after** the block exits. Assigning `__context__ = None` does not work. | `test_sanitized_error_does_not_retain_the_original_via_context` |
| D7 | `_fallback_narration` discarded narration text that was present in state | BW | Recover by `segment_id`; lift `["script"]` only — `Narration` is `extra="forbid"` | `test_package_builder_node.py` AC-1 suite |
| D8 | Imagen's key-redaction converted retryable errors into un-retryable `RuntimeError` | BW | `SanitizedHTTPError` carries `status_code` + `network_error` | `test_imagen_network_error_is_retried` |
| ~~D19~~ | **CLOSED 2026-07-29.** `redis.exceptions` defines its OWN `TimeoutError`/`ConnectionError` shadowing the builtins by NAME without inheriting from them, so `except (..., TimeoutError)` never matched one. `is_circuit_open()` is the first statement of every `@with_retry` body and talks to Redis, so a blip killed the node before the provider was contacted — permanently. | EXT | Three layers: `is_circuit_open` fails **open** (inner-body split so the guard covers the HALF_OPEN promotion *write*, not just reads); redis errors classified transient; `guard_breaker` no longer counts a redis error as a **provider** failure (5 blips/120s would open the breaker for a provider never contacted). | `test_redis_exceptions_are_not_the_builtins` pins the premise; `test_redis_errors_are_retried_then_succeed`; `test_is_circuit_open_fails_open_on_a_write_failure_too`; `test_redis_failure_does_not_open_the_breaker` — **mutation-verified, 9/9 caught** |
| ~~D20~~ | **CLOSED 2026-07-29.** `httpx.RemoteProtocolError` is neither `NetworkError` nor `TimeoutException` (sibling under `TransportError`), so a server closing the connection mid-response was never retried. | EXT | Named **explicitly**, never via its parent: `httpx.ProtocolError` also covers `LocalProtocolError` (*we* built a bad request — cannot succeed on attempt two), and `TransportError` also covers `UnsupportedProtocol`. Widening is the tempting wrong fix. | `test_remote_protocol_error_is_retried_then_succeeds` + `test_client_side_protocol_errors_are_not_retried` — the latter is the only test that fails if someone widens to `ProtocolError`/`TransportError` |
| ~~D23~~ | **CLOSED 2026-07-29 (PR #113).** `lesson_ready` reached no client: the worker published to `lesson_ready:{session_id}` from `lesson_row.get("session_id") or lesson_id`, but `lessons` has **no `session_id` column**, so the fallback always fired — while `core/websocket.py` registered connections under the client-supplied `crypto.randomUUID()`. The keys could never match. The right channel was produced **by accident**, one migration away from silently breaking. | BW | **Dev 4 chose option A** (handoff §2): key by `lesson_id` — generation completion is lesson-scoped, not viewer-scoped. Dev 4 fans out from a `lesson_waiters:{lesson_id}` set. Dev 1 deleted the accidental read and the stale payload comment. No schema change, no §16 gate. | `test_lesson_ready_routing_key.py` — **mutation-verified 4/4**. The load-bearing test feeds a row that DOES carry a `session_id` and asserts the key ignores it; plus a premise test that `lessons` must not gain the column silently. **Note: `test_schema_column_guard.py` does NOT cover this shape** — it walks `.select(...)`, this was a `dict.get()` on a fetched row |
| ~~D27~~ | **CLOSED 2026-07-29 by Dev 2 (commit 158e51f).** `next build` failed on a missing Suspense boundary around `useSearchParams()` at `/signin`, so `apps/web` had **never produced a production build** and was never deployable. Invisible to every workflow anyone used — `next dev` does not prerender and `vitest` does not either — and the one job that would have caught it (D25) had been dead since it was written. | BW | Dev 2 wrapped `<SignInForm />` in `<Suspense fallback={<SignInFormSkeleton />}>`. Dev 1 raised it and deliberately left the fallback choice to Dev 2, since it is visible UX on their page. | The `Build` step in the web job — **verified green in CI run 30470590163**, the first successful web job in the repo's history |
| D25 | **The entire `web` CI job has never executed on any commit.** `cache-dependency-path: apps/web/pnpm-lock.yaml` — but this is a pnpm *workspace*, so the single lockfile is at the repo root. `actions/setup-node` failed with "Some specified paths were not resolved", killing the job before install. Lint, type-check and build have therefore never run. Compounding it, `pnpm type-check` did not exist as a script in `apps/web/package.json` at all, so the step would have failed even once setup-node was fixed. | SELF (infra) | Point at the root lockfile; add `"type-check": "tsc --noEmit"`. Verified 2026-07-29: lint 0 errors, `tsc --noEmit` clean, **506 tests pass**. All three web steps now gate. | The job itself — it now runs, and a green run is the proof it never had |
| D9 | `completed_at` named in a narrowed select — a `lesson_jobs` column, not `lessons`. Would 42703 `GET /lessons` for every user. | SELF | Column lists must be validated against the migrations. | `test_list_columns_names_no_column_absent_from_the_lessons_table` — **but see D22: this guards one table only** |
| ~~D18~~ | **CLOSED 2026-08-04 (PR #119, merge commit `962f763`).** `sessions` had zero writers anywhere in `apps/api` — all 7 references were `.select(...)`. The frontend invented `crypto.randomUUID()`, which the ownership check in `grade_quiz` correctly rejected — quiz and teach-back could not complete for any student. | BW | New `POST /api/assessment/sessions` (Story 2-35) is the sole writer of `sessions`. `user_id` comes from the verified JWT only, never the request body; `session_id`/`started_at` are DB-generated, never client-supplied. Missing-or-unowned-lesson both return the same 404 (matches `content/router.py`/`media/router.py`'s existence-oracle-avoidance pattern). 5-agent review passed, mutation-tested — full writeup in `docs/reports/d18-session-lifecycle-implementation-report.md`. | The session-creation test suite (ownership 404, JWT-only `user_id`, DB-generated id/timestamp) + the implementation report's mutation-testing results — verified directly against the router/schemas/service code, not taken on faith |
| ~~D30~~ | **CLOSED 2026-08-03 (commit d0669aa).** 3 tests in `test_tutor_service.py` failing: `_setup()` and `_intervention_redis()` mock Redis never returned `"TEACHING"` for `tutor_state:sess-1`, so `service.py:328`'s correct TEACHING-state CES guard silently blocked all three trigger assertions. | SELF | Updated both fixtures to return `"TEACHING"` for the session key — guard is correct (CLAUDE.md §10), tests were wrong. | `test_two_below_threshold_no_cooldown_dispatches`, `test_intervention_delivers_tutor_intervene_message`, `test_intervention_no_delivery_on_cache_miss` — now pass; will fail if fixture is stripped |
| ~~D63~~ | **CLOSED 2026-08-11 (Story 4-24, branch `sprint4/s4-6-intervention-recovery`, PR #129 — closed AFTER a six-layer adversarial review found and fixed a bug in the fix itself; see below).** `INTERVENING` was a one-way trap — its only exit event, `intervention_complete`, was dispatched by nothing. `route_from_intervening` (`graph.py:337-339`) left INTERVENING only on `intervention_complete`, absent from `_CLIENT_DRIVABLE_EVENTS`, `_TUTOR_CLIENT_EVENTS`, `wireTypes.ts`, and every server path, with no timeout. CES monitoring is TEACHING-gated (CLAUDE.md §10), so the first intervention would have put a session in INTERVENING permanently, silently. Surfaced in Dev 3's 2026-08-05 `docs/handoffs/lesson-delivery-dev4.md`; found by review, not by an incident — no real attention signal had ever driven a real intervention yet. | High (latent; total and silent once triggered) | **First-round fix (two independent exits):** (a) `intervention_complete` added to `_CLIENT_DRIVABLE_EVENTS`/`_TUTOR_CLIENT_EVENTS`/`wireTypes.ts`'s `FlowEvent`; (b) `intervening_node` writes `intervention_deadline_at` (new `settings.intervention_timeout_seconds`, default 45s, bounds `ge=1,le=3600` added in round 2), checked by `_intervention_deadline_expired` in both `process_attention_signal` and `advance_tutor_state`. **Round-2 fixes, same session, found by the six-layer review of PR #129 before merge (not after):** the first round's `advance_tutor_state` guard only handled the *expired* case — any other client event arriving while INTERVENING and NOT yet expired fell through to `dispatch_event`, which `route_from_intervening` routed straight back into `intervening_node`, which unconditionally rewrote `intervention_deadline_at` — **perpetually re-arming the timeout and reopening the exact trap D63 exists to close.** Fixed: that fall-through is now a no-op unless the event is `intervention_complete`. Also fixed in round 2: the delete-before-dispatch guard was a plain GET-then-DELETE with no value check — vulnerable to a cross-generation race between two connections on one `session_id`; replaced with `_delete_intervention_deadline_if_expired`, an atomic Lua compare-and-delete. And: the expired-but-wrong-event case (client sends e.g. `segment_complete` exactly as the deadline expires) previously dropped that event's side effects (the `segment_index` increment); now replayed through `dispatch_event` after the self-heal. Also fixed while in the file (round 1): all 7 FSM nodes stopped returning `{**state, ...}` (CLAUDE.md-banned pattern); `test_node_return_shape.py`'s AST scan widened to cover `tutor/state_machine`. | Round 1: `test_intervening_complete_returns_to_teaching`, `test_intervening_node_writes_intervention_deadline`, `test_intervening_node_returns_only_owned_keys`, `test_intervention_deadline_expired_*` (5 cases), `test_no_pipeline_node_returns_spread_of_state` (widened) + `test_guard_scans_the_tutor_state_machine_directory_for_real`. Round 2 (post-review): `test_advance_tutor_state_intervening_other_event_not_expired_is_noop` (the re-arming regression test — proves the CRITICAL finding is closed), `test_delete_intervention_deadline_if_expired_*` (atomic helper unit tests), `test_advance_tutor_state_intervening_expired_replays_original_event`, plus corrected assertions on the pre-existing not-expired test (`redis.delete.assert_not_called()`, previously missing — see the six-layer review's Test Coverage finding). **176/176 green in the 4 directly-affected test files, independently re-verified after round 2's changes.** Full-suite `pytest tests -q` run: 174 failed/1592 passed/113 skipped/45 errors — from 4 known, pre-existing, unrelated causes (missing `python-multipart`; missing `fpdf`; `test_dna_growth.py`'s cross-test pollution, already **D40**; a live-network-dependent LLM smoke test) — corrected here after the six-layer review's Acceptance Auditor layer caught this task's own earlier "only two gaps" claim as an understatement of what was actually verified (binding rule 1: verification scope = CI scope). |

### Fixed, in open PR

| ID | Defect | Class | Decision | Enforcement |
|----|--------|-------|----------|-------------|
| D10 | Unpriced model skipped `accumulate_cost` entirely → $3.00 ceiling could never fire | BW | Fail **closed**: charge the most expensive *known* rate, derived from the table; log ERROR; never abort. | `test_cost_ceiling_failopen.py` (PR #105) |
| D11 | `structure_node`'s LLM validation: 6,000-char prompt window vs ≥90%-coverage guard → always discarded above ~6,667 chars | BW | Delete it. Detection is font+regex only, and that is a **limitation, not a feature**. | `test_structure_no_llm.py` (PR #107) — **incomplete, see D12** |

### Review round 2 (2026-07-29) — ALL RESOLVED

Five fixed, one **rejected as a wrong finding**. Kept as its own section because the
shared shape — a guard that watches a *proxy* rather than the property it claims to
prove — is the failure most likely to recur.

These are mine, from today, and they are listed at the same weight as everything else on
purpose.

| ID | Defect | Decision | Enforcement |
|----|--------|----------|-------------|
| ~~D12~~ | **CLOSED 2026-07-29 (PR #107).** Six "no LLM call" assertions spied on `complete_structured` alone; a regression via `provider.complete()` passed all of them. | SELF | Assert on the **factory** — if `get_llm_provider` is never called, no provider method can run. | `factory.assert_not_called()` — **mutation-verified**: "provider obtained but never used" passed the old suite entirely, fails 3 tests now |
| ~~D13~~ | **CLOSED 2026-07-29 (PR #107).** The `chapter` branch was unreachable with the fixture (median 14.0 -> threshold 17.5 -> chapter band starts at 20.125; largest block 18.0pt), so two of three level branches were unpinned dead code. | SELF | Derived font set (24.0/17.0/15.2 against median 12.0) landing one heading in each band. Asserted on `detect_headings` directly, since `coalesce_sections` would make a failure ambiguous. | `test_all_three_heading_levels_are_reachable_from_font_metadata` — catches "chapter branch deleted" and "bands swapped" |
| ~~D14~~ | **CLOSED 2026-07-29 (PR #107).** Golden test was environment-dependent: `STRUCTURE_MAX_SECTIONS=3` turned it red with "detection itself moved" — a misdiagnosis; nothing moved, the cap merged sections. | SELF | Pin `structure_max_sections` and `structure_min_section_chars`, as every neighbouring structure test already did. | The pinned settings in `test_sections_are_identical_to_the_pre_deletion_baseline` |
| ~~D15~~ | **REJECTED 2026-07-29 — the review claim was wrong.** It called `test_fallback_is_at_least_every_known_model_rate` a mathematical tautology ("max over a set is >= every element"). True of the *arithmetic*, but it ignores that the test discriminates *implementations*. **Mutation-tested rather than taken at face value: it catches max->min, output-uses-input-max, AND a hardcoded literal.** Redundant with its neighbour, not tautological. | — | **Keep it.** Deleting a working test on a false premise is worse than keeping a redundant one. Recorded so the claim is not re-raised. | n/a — this entry exists to prevent a wrong "fix" |
| ~~D16~~ | **CLOSED 2026-07-29 (PR #105).** Two defects in one. (a) The story claimed "AC-1 alone would not have caught the original" — **false**: AC-1 asserts `assert_awaited_once()` and the original bug was an early `return` *before* `accumulate_cost`. (b) AC-5's test stubbed `check_ceiling -> True` unconditionally, so it tested the `if` statement, blind to the arithmetic — it could not tell a charge that trips the ceiling from one that does not. | SELF | Correct the claim; run the REAL `accumulate_cost`/`check_ceiling` against fakeredis seeded just under $3.00, with nothing telling `check_ceiling` what to return. | `test_unpriced_charge_really_trips_the_ceiling_via_real_arithmetic` + `test_a_charge_below_the_ceiling_does_not_trip_it` — the two scenarios the stubbed version could not distinguish |
| ~~D17~~ | **CLOSED 2026-07-29 (PR #105).** `None` token counts raised `TypeError: unsupported operand type(s) for /` — an *unknown* exception, so `with_retry` would not retry and the node died. Verified reachable by execution. The call had already succeeded and been billed, so throwing made ARQ re-run and re-pay. Negatives were worse: `accumulate_cost` raises `ValueError` on a negative cost, equally unclassifiable. | SELF | `max(0, x or 0)` at the source, with ERROR/WARNING logs stating the total is now an under-estimate. | `test_missing_token_counts_do_not_crash_the_node` (3 cases) + `test_negative_token_counts_are_clamped_not_propagated` |

### OPEN — live in production

| ID | Defect | Sev | Decision | Enforcement |
|----|--------|-----|----------|-------------|
| ~~D22~~ | **CLOSED 2026-07-29.** D9's class was live at 43 sites; replanting it on `sessions` left the suite green. | High | Guard generalised to every table + column, resolving module-level constants. | `test_schema_column_guard.py` — **mutation-verified**: catches the replant both via a literal AND via `_LIST_COLUMNS` |
| ~~D29~~ | **CLOSED 2026-08-05 (Story 3-32, branch `sprint3/s3-32-dpdp-consent-endpoint`).** `POST /api/assessment/consent` now writes `user_consents` rows. INSERT-first idempotency (TOCTOU-safe via UNIQUE constraint in migration `20260805000000_user_consents_unique_constraint.sql`). 24 unit tests; all ACs including body-injection guard and idempotent to_thread coverage. Enforcement: `apps/api/tests/test_consent_endpoint.py`. | High (compliance) | Fixed + guarded. | `apps/api/tests/test_consent_endpoint.py` — 24 tests covering all ACs (AC 8: `test_record_consent_service_never_updates_users_table`; AC 15: `test_record_consent_idempotent_path_both_db_calls_wrapped_in_to_thread`). |
| ~~D30~~ | **CLOSED 2026-08-03 (commit d0669aa).** See closed section above. | — | — | — |

### OPEN — found by the 2026-07-30 frontend wiring audit

Full report and the ~40 lower-severity items: **`docs/reports/frontend-wiring-audit-2026-07-30.md`**.
D31-D33 are **Dev 1's own**, found *after* Dev 1 declared its work finalized.

| ID | Defect | Sev | Decision | Enforcement |
|----|--------|-----|----------|-------------|
| ~~D31~~ | **CLOSED 2026-08-11 (Story 3-35, branch `sprint3/s3-35-env-config-fixes`).** `NEXT_PUBLIC_API_URL` omits the `/api` segment in every Dev-1-owned source, so following the setup documentation 404s every API call. `apps/web/src/lib/api.ts:4` falls back to `http://localhost:8000/api` (correct), but `.env.example:10`, `.github/workflows/ci.yml:126` and `docs/handoffs/dev2-handoff-2026-07-29.md:154` all say `http://localhost:8000`. Verified empirically: axios joins that with `content/lessons` to give `/content/lessons`, and `main.py:166-172` mounts every router under `/api` with **no unprefixed alias**. A dev who configures nothing works; a dev who reads the docs, or runs a CI build, is dead on arrival. Two other docs have it right — the repo contradicts itself in six places. | High | **Fixed.** `.env.example` and `ci.yml` both now carry `/api`. Repo-wide grep confirmed only these two were live instruction sites — the rest were dated incident/audit records left untouched (annotating, not rewriting, history). `docs/stories/W0-contract-harness.md`'s stale trap note annotated as resolved. | `apps/web/src/__tests__/lib/api.test.ts` — resolves the documented `NEXT_PUBLIC_API_URL` value for a known route and asserts it ends in `/api/content/lessons`. RED-confirmed against pre-fix code, GREEN after. |
| ~~D32~~ | **CLOSED 2026-08-11 (Story 3-36, branch `sprint3/s3-36-package-builder-defensive-fixes`).** `_group_by_segment_id` did a raw `item["data"]` subscript (moved to `graph.py:4067` by the time of the fix — line drift confirmed, same class the register already warns about for D31) while its docstring claimed *"Same defensive-skip philosophy as `_index_by_segment_id`"*. It guarded `segment_id` but subscripted `"data"` blind and never checked `isinstance(item, dict)` either — a non-dict entry, an entry missing `"data"`, or a non-dict `"data"` value each crashed `package_builder_node` with a different real exception (`AttributeError`/`KeyError`/`TypeError`, all three reproduced before the fix) — **the last node, after 100% of the lesson's LLM/TTS/image spend**. | Med | **Fixed — matches `_index_by_segment_id`'s pattern exactly, closing site 2 of the defect closed at site 1** (Story 2-31, binding rule 6). All three of its callers (`slides_by_segment`, `quiz_by_segment`, `jargon_by_segment`) inherit the fix with no changes to their own call sites. | 3 new tests in `apps/api/tests/unit/test_package_builder_node.py` reproduce all three failure modes (non-dict item, missing `data` key, non-dict `data` value) — RED-confirmed against pre-fix code with the exact exception types named above, GREEN after. Full 42-test file re-run, zero regressions. |
| ~~D33~~ | **CLOSED 2026-08-11 (register correction, Story 3-36) — fix predates this entry's closure by a week.** `book_id`/`chapter_id` defaulted to `""` against `UUID` fields (`graph.py:711,3742` at the time this was registered); `schemas/lesson.py:212-213` declare `UUID`. An empty string can never satisfy that, so a missing `chunk` output surfaced as a bare Pydantic `ValidationError` at the final node — after full spend — instead of a diagnostic naming the actual cause. | Low | **Already fixed — verified via `git blame`, not assumed.** Commit `1c4360b1` (2026-08-04, Story 1-13 "chapter-scoped generation — Phase 5 Implemented") replaced the `""` defaults with explicit `RuntimeError`s naming exactly what's missing (`graph.py:3930-3953` as of Story 3-36). This register entry was simply never updated to close it — the inverse of the usual failure shape here (code fixed, register stale, rather than register-claims-fixed/code-not-fixed). Found and corrected during Story 3-36's prep, before writing any code assuming this was still open. | `test_missing_chapter_id_raises_diagnostic_not_model_validate_failure`, `test_missing_book_id_raises_diagnostic_not_model_validate_failure`, `test_chunk_present_but_missing_chapter_id_key_behaves_like_chunk_absent` — all three already existed, all three name D33 directly, all three green. |
| ~~D34~~ | **CLOSED 2026-08-04 (Story 1-15, book-scale Phase 6.5).** Fixed at the writer: `pubsub.py` keeps the id's name (`lesson_id`), resolves waiting sessions from `sessions.lesson_id` (NOT NULL, indexed, already correct — `20260611000000_initial_schema.sql:177`, `:300`), delivers to each, and writes the package cache under **every** resolved session so `_seed_learner_tier` and `_segment_intervention_messages` hit. Neither consumer was changed: their key shape was right, the writer was wrong. The `lesson_waiters:{lesson_id}` set those comments describe still does not exist and was deliberately not built — `sessions.lesson_id` is the same fact, already indexed. Residual timing gap registered separately as **D55**. Original text: **AMENDED AND WIDENED 2026-08-04 (Story 1-14 AC22) — the "dead code" verdict was true of the push only, and the other half is on the Sprint 3 critical path.** `core/pubsub.py:67` strips the `lesson_ready:` prefix and binds the resulting **lesson_id** to a variable named `session_id`. That value is used **twice**, not once: it is passed to `manager.send()` at `:80` (the push — genuinely dead today), **and it is the cache key at `:96-98`**, `f"lesson_package:{session_id}"`. Two real consumers read that key by the **session** id the client connected under — `_seed_learner_tier` (`core/websocket.py:279`) and `_segment_intervention_messages` (`modules/tutor/service.py:253`, the < 50 ms intervention hot path that must never touch the DB). Written under a lesson id and read under a session id, the key can never match, so **every intervention lookup is a permanent cache miss** and `_seed_learner_tier` always returns early. This is not visible today only because Sprint 3 has not turned interventions on. Also: `tests/test_lesson_ready_integration.py:194-211` sets `session_id = "test-lesson-abc"` and uses that **one string for both `session_id` and `lesson_id`** in the payload, then asserts `manager.send.assert_awaited_once_with(session_id, message)` against a `MagicMock` — it encodes the defect as its premise and **cannot fail** whichever id the code picks (binding rule 2). **Original text (2026-07-30):** *"`lesson_ready` is routed with the wrong key type. `core/pubsub.py:67,80` strips the `lesson_ready:` prefix and passes the resulting **lesson_id** into `manager.send()`, which keys connections by the path-param **session_id** (`websocket.py:72,110`). The `lesson_waiters:{lesson_id}` set Dev 4 described does not exist in `apps/api`. Med. Dev 4 owns the SADD/SMEMBERS fan-out. **Not currently load-bearing**: `useLessonSocket.ts:50-55` deliberately no-ops `lesson_ready` and readiness comes from REST polling — so this is dead code, not an outage."* | **High** (was Med) | Dev 4 owns the SADD/SMEMBERS fan-out for the push. The **cache** half needs an owner separately: the writer must key `lesson_package:` by the id the readers use, or the readers must change — the two ends have never been read together (RC-2). **Fix is Phase 6.5, explicitly out of scope for Story 1-14.** **Trigger: Sprint 3 enabling interventions**, or any earlier work that depends on `lesson_package:` resolving. | *(to add — the existing test cannot fail; it must first be given two DISTINCT ids, then assert `manager.send` receives an id a client actually connected under **and** that a `lesson_package:` write is read back by a session-keyed reader)* |
| ~~D35~~ | **CLOSED 2026-08-04 (commit `9475331`, Story 2-39).** `setSessionId` had no caller — `player.machine.ts:146` minted `sessionId: crypto.randomUUID()` and nothing ever replaced it, so the id sent to every session-scoped assessment route was never persisted. The frontend half of D18. | BW | `Player.tsx`'s existing `loadedLessonIdRef`-guarded mount effect now also calls `createSession({lesson_id})` once per lesson and `setSessionId(session_id)` on success — `loadLesson()` no longer invents an id. Verified field-for-field against the real `SessionCreated` schema (`session_id`, `lesson_id`, `started_at`), not guessed. | `apps/web/src/__tests__/components/player/Player.test.tsx`'s session-creation describe block + `apps/web/src/__tests__/stores/player.machine.test.ts` |
| D36 | **Stack drift, unowned: `apps/web` is Next 16.2.9 / React 19.2.4 while `CLAUDE.md` locks "Next.js 14 + TypeScript + Tailwind".** Two major versions. Nobody was tasked with reconciling it, and it may silently invalidate other Next-14-shaped assumptions in the frontend and in this document. | Med | Either amend the locked stack in `CLAUDE.md` (a stack change needs the §16-style four-dev conversation) or pin back. **Do not leave the two disagreeing** — `CLAUDE.md` is declared the source of truth, so a divergence makes it untrustworthy generally. | DISCIPLINE — needs an owner first |
| ~~D37~~ | **CLOSED 2026-08-04.** **`_LIST_COLUMNS`' PostgREST JSON-path selectors have never been executed against real Postgres.** `subject:content->metadata->>subject` and the `estimated_duration_mins` sibling (`content/router.py:112-116`) are exercised only against Supabase mocks. **The `completed_at` reference in this exact select list already caused one outage-class `42703`** (D9), and per binding rule 4 a mock has no Postgres catalog and cannot raise it. | Med | One integration test against real PostgREST covering this select. Until then `GET /lessons` is unverified against the database it queries. | `tests/integration/test_book_select_lists_against_postgrest.py` — runs every select list the app sends against a real PostgREST, including `_LIST_COLUMNS`. Carries a live-trap premise test asserting a bogus column really does raise 42703, so it cannot pass vacuously. **9 passed 2026-08-04.** |
| ~~D38~~ | **CLOSED 2026-08-03.** ~~The book-scale Phase 2 migration has never touched the real Supabase project.~~ Applied to the live project and verified there: both columns present, `chapters.lesson_id` now nullable per the live OpenAPI schema, 23/23 backfilled to `fallback`, row counts unchanged, 0 duplicates. RLS confirmed with real minted JWTs — owner sees 9 chapters / 1,507 chunks, stranger 0, anon 0, matching the ownership graph exactly. Original text: `20260803000000_chapters_book_scoped.sql` is verified only against a PostgreSQL 16 container plus a hand-written Supabase shim (`apps/api/tests/integration/supabase_shim.sql`), which reproduces the *contract* of `auth.users` / `auth.uid()` / `storage.buckets` / the `anon`,`authenticated`,`service_role` roles — not Supabase's implementation. Every RLS verdict in that suite is conditional on the shim being faithful, and no test in this repo can disconfirm it (binding rule 2). | Med | Dev 1 applies the migration to the real project and re-runs the RLS assertions there. **Trigger: before Phase 3 writes any chapter row to a real database.** Until then Phase 2 stays `🧪 Implemented`, never `✅ Verified`. | `# MOCK-CONTRACT:` marker on `test_shim_auth_uid_reads_jwt_claims` naming this ID |
| ~~D39~~ | **CLOSED 2026-08-03.** ~~The duplicate `(book_id, chapter_index)` pre-flight was run against an empty container, not production.~~ Live project queried read-only: **23 chapters, 23 distinct books, every `chapter_index=1`, 0 duplicates**. The migration was then rehearsed over a production-shape copy and applied cleanly with all row counts intact. Original text: Story 1-9 AC10 requires the migration to fail loudly rather than discard data if duplicates already exist; that behaviour is now proven (`test_migration_aborts_loudly_on_preexisting_duplicates`), but whether the real database *contains* duplicates is unknown. The tracker expects 23 chapter rows, all written by the hardcoded `chapter_index: 1` path (`graph.py:624`) — so any book with two ingestions would collide. | Med | Query the real `chapters` for duplicate pairs before applying. **Trigger: the apply itself** — the migration aborts atomically (`BEGIN/COMMIT`), so a collision is loud and recoverable, but it will block the deploy. | Migration is atomic + `test_aborted_migration_destroys_no_data_and_leaves_no_partial_schema` |
| D40 | **`tests/integration` leaks state into `tests/test_dna_growth.py`.** Running the integration suite first fails 18 of the 21 dna_growth tests; they pass in isolation. Reproduced identically on `main` via a git worktree, so it predates the book-scale work. This is the bulk of the 19 failures in CI's advisory `pytest tests -q` step (D24). | Med | Dev 3 owns `learner_dna`. The leak is in module-scope state, not in the migration work. **Trigger: the D24 ratchet** — these 19 must reach zero before `continue-on-error` can be dropped. | *(to add — isolate the fixture that leaks)* |
| D41 | **`POST /api/content/lessons` changes shape and `apps/web` cannot generate a lesson until Phase 6.** Upload returns `{book_id}` instead of `{lesson_id}` and 422s on `tier`. `UploadFlow.tsx:74-79,155-157` sends `tier` on **100 %** of uploads (mode selection is the only path to `processing`), so it fails at the first click; with no tier it polls `content/lessons/undefined` forever. The replacement write endpoint is Phase 6, three phases later. | Med | **The change never reaches `main` until Phase 7.** All of Phases 3–6.5 and Track W land on `book-scale/integration`; `main` keeps its working `POST /lessons` meanwhile. Dev 2 is told before merge, not after. **Trigger: merge of `book-scale/integration` to `main`.** | Track W's W0 contract CI job fails when the committed `docs/contracts/book-api.v1.json` and the live FastAPI schema diverge |
| D42 | **Book ingest is dominated by the Storage download, not by detection.** Measured on the Phase 3 gate against the live project: ingest of a 44.7 MB / 1,151-page book took **28.4 s**, of which **18.9 s (67 %) was downloading the source at 2.4 MB/s** and 10.1 s was processing. AC15's 15 s budget came from Phase 1, which measured `get_toc()` + text sweep on a **local** file and never included the fetch. | Low | AC15 split into processing (met, 10.1 s) and end-to-end (recorded, not capped) rather than moving the number until it passed. Transfer cost is environment-dependent — this was a dev machine to a remote Supabase. **Trigger: the India-region migration** (CLAUDE.md already requires it before Sprint 3), or the first user complaint about upload latency. | Numbers recorded in the tracker's Phase 3 Observed result; re-measure from a co-located worker after the region migration |
| D43 | **Phase 5 (chapter-scoped generation) is `Implemented`, not `Verified` — no chapter has been generated through the eleven nodes for real.** AC10 requires one real end-to-end generation, which spends real money on the project's OpenAI key. Everything else in Story 1-13 is proven: `chapter_id` reaches the subprocess as `page_start`/`page_end`, no `chapters` write survives anywhere under `pipeline/`, and none of the eleven generation nodes was modified. What is unproven is that the eleven nodes actually produce a schema-valid package from ~40 pages instead of ~1,151 — the entire premise of the effort. | Med | **DECISION 2026-08-04: pay once, not twice.** Phase 7's acceptance run has to generate two chapters at two tiers anyway, so AC10 is folded into it rather than run separately here. Phase 6 proceeds on Phase 5's implementation, which it needs regardless of AC10's verdict. **Trigger: the Phase 7 acceptance run** — if it fails, Phase 5 returns to `In Progress` and Phase 6 is un-Verified with it. This is a deliberate, recorded exception to the tracker's gate rule, not an oversight. | The tracker's Phase 5 `Observed result` states the gap in the same words; Phase 7's exit criterion names AC10 explicitly, so the acceptance run cannot pass without discharging it |
| ~~D115~~ | **CLOSED 2026-08-15 — rule 6 (`MIN_TITLE_HIT_RATE=0.80`) silently rejected a 100%-correct 27-entry PDF outline on a real user upload ("Evading EDR", Matt Hand, No Starch Press, 315 pages).** Root cause: the book's chapter-opener typography emits the "N / TITLE" block AFTER the body paragraph, so it lands past the first 400 characters of the page's text — and `extract_text_only()` had already hard-truncated every page past `front_pages=40` to `head_chars=400` (discarding it entirely) before `title_present()`'s own head-only 400-char window ran. Measured: 6/13 real chapters (46%) passed the old check under the real production extraction contract; the `toc` rung was rejected and the ladder fell through to R4's untuned font-signal detector, which returned 6 garbled, mid-sentence-titled pseudo-chapters instead of the book's real 13. | Med | **FIXED.** `title_present()` gained an opt-in `tail_window` kwarg (default 0, byte-identical for every existing caller); `extract_text_only()` gained an opt-in `tail_chars` kwarg (default 0) that appends a bounded tail slice only when it doesn't overlap the head slice; `gate.py` rule 6 is the only call site that opts in, via the new `TITLE_TAIL_WINDOW=400` constant (`text.py`) — an unbenchmarked, reasoned-margin value (one measured data point: a title in the last 17 of 746 chars), not a Phase-1-measured constant like `TITLE_WINDOW`. `contents_like_pages()`'s `CONTENTS_HDR_RE` was also widened to match "Brief Contents"/"Contents in Detail" headers (strict regex superset — both original alternatives unchanged). All 5 pre-existing pinned fixtures (`d2l`, `openstax-bio2e`, 3×NCERT) verified byte-identical before/after (rung, raw_count, kept count, `contents_like_pages()` output) via direct comparison against the pre-fix code, not assumed. D2L (1,151p) re-timed with `tail_chars=400` wired in: payload grew from 656 KB to 1.16 MB JSON, wall time unchanged within noise (parsing dominates, not payload size) — no threat to the 15 s ingest budget. **Residual, accepted gap:** a title genuinely in the true MIDDLE of an oversized page (past both the head-400 and tail-400 windows) is still silently unrecoverable — no real book has been observed to need this yet; revisit if one is. | `tests/unit/test_chapter_detection.py`'s `test_evading_edr_*` (new, against a real captured fixture, `tests/fixtures/chapter_detection/evading-edr.json.gz`) + `title_present`'s new `tail_window` unit coverage + the extraction-side `tail_chars` overlap-safety case in `test_extract_page_bounds.py`/`test_extract_text_only_signature_matches_the_contract` |
| ~~D117~~ | **CLOSED 2026-08-17 — closes D116's residual gap.** Evading EDR detected **14** chapters, not 13: "Introduction" (front matter, before the numbered chapter sequence) survived because `filter.py`'s exact-title blocklist deliberately excluded it (D116) to avoid deleting Dive into Deep Learning's real, already-pinned Chapter 1, which is also bare-titled "Introduction." | Low | **FIXED** — new `drop_leading_unnumbered_front_matter()` (`filter.py`), called after `drop_non_content` and before the gate. Looks at title SHAPE across the whole kept list, not any one title's text: if ≥70% of a rung's kept titles carry an explicit leading chapter number (`^\d{1,3}[.:)]\s+\S`, e.g. "1. EDR-Chitecture"), the book is numbered-chapter style, and the LEADING run of unnumbered entries (before the first numbered one) is dropped as front matter — mid-sequence unnumbered entries are left alone. Threshold (0.70) sits with a clear margin above the one measured real case (13/14 = 92.9%) and a clear margin below "some but not most." **Verified directly against all 6 fixtures, not assumed:** every one of the 5 Phase-1 pinned books is 0/N numbered by this exact regex (OpenStax Biology's "Chapter 1 The Study of Life" starts with the word "Chapter," not a digit, so it doesn't match either) — the new filter never even reaches its threshold check for any of them, and Evading EDR now resolves to exactly 13 chapters, matching the book's own numbering. | `TestDropLeadingUnnumberedFrontMatter` (5 synthetic, fixture-free cases: fires above threshold, inert below threshold, leading-only scope, empty input, all-unnumbered input) + `test_evading_edr_leading_introduction_is_dropped_as_front_matter` (real fixture, asserts exactly the 13 numbered chapters, in order) + `test_d2l_leading_introduction_is_not_touched_by_the_numbered_title_heuristic` (real fixture, proves D2L's real "Introduction" chapter 1 survives byte-identical) |
| ~~D116~~ | **CLOSED 2026-08-15 — same Evading EDR upload: `filter.py`'s non-content blocklist, derived only from Phase 1's original 5-book sample, missed this 6th real book's front/back-matter labels ("Cover", "Back Cover", "Title Page", "Brief Contents", "Contents in Detail", "Praise for Evading EDR"), so once D115's fix let the `toc` rung through, these would have leaked into the teachable chapter list as bogus extra "chapters."** | Med | **FIXED** — added `cover`, `back cover`, `title page`, `brief contents`, `contents in detail` to `_BLOCKED_EXACT`, and a spaceless `praisefor` to `_BLOCKED_PREFIX` (a literal `"praise for"` would silently never match — `is_content()` compares against `norm()`, which strips all whitespace before comparison; caught before shipping by checking `norm()`'s actual behavior, not assumed). **NOT fixed, deliberately: "Introduction" stays off the blocklist.** It is this book's 8th front-matter junk entry (confirmed directly against the real extracted TOC: `kept` count was 21 = 7 named junk + 13 real chapters + "Introduction", reconciling an arithmetic gap an earlier draft of this fix left open), but D2L's real, already-pinned Chapter 1 (`tests/fixtures/chapter_detection/d2l.json.gz`, `toc` `page_index=40`) is *also* titled bare "Introduction" — confirmed by direct check against the fixture before rejecting this entry. Exact-title blocking cannot distinguish "front matter preceding a numbered chapter sequence" from "a real, unnumbered chapter 1" — so Evading EDR now detects **14** chapters (13 real + "Introduction"), not 13. **Trigger for a real fix:** a structural signal — e.g. drop a leading unnumbered entry only when it precedes a run of otherwise-numbered chapter titles — was considered but is a materially bigger, unreviewed change; owner is whoever picks this up next, or the first time a user reports the extra "Introduction" lesson as wrong rather than acceptable. | `test_evading_edr_junk_front_and_back_matter_is_dropped` (the 7 blocklisted items) + `test_evading_edr_introduction_is_a_documented_residual_gap_not_a_silent_regression` (asserts the gap stays visible — 14 kept, not silently 13) |
| D131 | **OPEN, NOT FIXED, NOT SCOPED — real students may have no upper bound on "chapter" size when a real book's structure is undetectable.** Surfaced as a side effect of diagnosing D130, not from a failing test — a genuinely distinct, more serious risk than D130 itself. Chapter detection's R5 rung (`app/modules/content/chapter_detection/ladder.py`) always accepts and always returns at least one chapter — by design, so ingestion never hard-fails on a real book — but when a real upload has no detectable TOC, headings, or font hierarchy (a bad scan, an unusual layout, anything the fixtures in D130 also lack), the single "chapter" R5 returns spans the ENTIRE book with no size ceiling. Unlike the eval harness (which picks the largest chapter automatically), a real student presumably picks which chapter to generate from a list the frontend shows — if the only option is the whole book, generating it would hit the same consequence D130 measured: a lesson far larger than the ~40-page/~5-15-min/~$1-3 assumptions the whole system (timing, UX expectations, quality) is built around. The $3.00 cost ceiling would still hold (downshift-not-abort, already enforced) so this is NOT an overspend risk — it is a time/UX/quality-degradation risk with no known real-world trigger observed yet, only a proven mechanism. | Med-High (mechanism confirmed via D130's investigation; real-world likelihood unmeasured — none of Phase 1's 8-book real corpus needed R5, but that sample is small and pre-dates book-scale's wider intake) | **Deliberately not investigated or fixed here** — this is a product/UX decision (should the frontend warn or block generation from an oversized undetected "chapter"? cap it? offer a manual chapter split?), not a code fix to make unilaterally alongside a test-fixture change. **Owner: TBD. Trigger: before Sprint 4 (real-student load testing, D129) or the first real book observed to hit the R5 fallback in production, whichever comes first — recommend deciding this alongside D129 since both concern real-student-scale behavior nothing has exercised yet.** | None — no code changed; this entry exists so the decision isn't lost, per binding rule 5 (a documented limitation with no register ID is a defect wearing a decision's clothes) |
| D130 | **OPEN, NOT FIXED — the S3-1 eval harness's 20 synthetic fixtures ALL fall through chapter detection to the R5 whole-document fallback, so the 4 "long" fixtures generate single chapters of 100/150/250/400 pages instead of a realistic ~40-page chapter.** Confirmed directly, zero cost: ran `detect_chapters()` (pure function, no PDF/DB/network) against all 20 local fixtures — every one resolves to exactly 1 chapter spanning the full document (`rung=fallback` or a rung that still returns one whole-span chapter), because none of `generate_eval_pdfs.py`'s fixtures carry a real PDF outline/table of contents or a font-hierarchy signal the ladder's rungs 1-4 can detect (same root cause D124/D125's fixtures already lack any TOC per their own closure notes — this is that same gap's cost/time consequence, not a new one). Discovered live: a full `--run-live-eval` attempt on 2026-08-21 ran for **6+ hours without completing a single one of the 20 PDFs' worth of results** (no output is written until all 20 finish) and was stopped intentionally rather than left running indefinitely — CPU/memory were confirmed still actively climbing throughout (not hung/deadlocked), just doing roughly 2.5x-10x the work per "long" fixture that the harness's own ~5-15 min/lesson time budget assumes. | High (this is the harness meant for "cheap, frequent" regression checks per its own design goal — a single run that cannot complete in a working session defeats that purpose entirely, independent of the money spent) | **Deliberately not fixed here** — the real fix (give the 4 "long" fixtures a detectable structure — e.g. real PDF bookmarks/outline entries, or heading-styled section breaks the font-signal rung can find — so they resolve to several realistic ~40-page chapters instead of one whole-document one) changes `generate_eval_pdfs.py`'s fixture design, a decision for whoever owns the harness next, not an unplanned side effect of investigating why a run was slow. **Owner: TBD. Trigger: before the next `--run-live-eval` attempt**, so it doesn't repeat the same multi-hour, no-visibility run. A second, smaller, independent gap the same investigation surfaced: this test writes zero progress output until all 20 PDFs finish, so a multi-hour run is completely opaque from the outside — no way to tell "PDF 3 of 20" from "PDF 18 of 20" without instrumenting it externally. | `detect_chapters()` output for all 20 fixtures captured and verified 2026-08-21 (`n_chapters=1` for every one, `biggest_span` == the fixture's own page count for all 4 "long" fixtures) — ad hoc verification, not yet a committed regression test |
| D129 | **OPEN, NOT TESTED — no multiple-concurrent-real-user load has ever been run against this pipeline.** Every live confirmation this register has (D127's 2026-08-21 real-world run included) is ONE lesson generating at a time. What happens when several real students trigger generation simultaneously is genuinely unknown, not merely under-measured — surfaced explicitly discussing this gap rather than found by a failing test. Three concrete, already-known reasons this is a real risk, not a hypothetical one: (1) the circuit breaker (`core/circuit_breaker.py`) trips PER PROVIDER, globally, not per-user — a transient failure from one user's request can trip the breaker and block every other concurrent user's requests to that provider too, and this has never been observed happening; (2) **D45** (this register) — the `(chapter_id, tier)` idempotency pre-check has no `UNIQUE` constraint, so two concurrent identical requests can both bill; (3) **D49** — `RATE_LIMIT_STORAGE_URL` defaults to `memory://`, so the real per-deployment rate ceiling silently multiplies by replica count under real multi-instance concurrency, not the single number anyone would assume from reading the config. | High (this is the exact shape of the project's stated goal — Week 10, first paying student, PRD roadmap explicitly plans multiple real students — and it has zero verification) | **Deliberately not tested here** — this was surfaced as a documentation/planning gap during Sprint 3 real-world eval work, not something to test as an unplanned side effect of that work. **Explicitly scoped to Sprint 4** ("Load test + calibration," `CLAUDE.md`'s Build Roadmap, Weeks 8-9) — this entry exists so Sprint 4 planning starts from a named, evidenced list of concurrency risks (the breaker, D45, D49) rather than a generic "do some load testing" task. **Trigger: the start of Sprint 4**, or before real students are onboarded, whichever comes first. Recommended first step when picked up: a SMALL controlled test (2-3 concurrent real lesson generations, not a large burst) to observe the shared-circuit-breaker risk specifically, before any larger-scale load test. | None yet — this is a planning entry, not a code change; `test_breaker_accounting.py` and friends cover the breaker's logic in isolation but nothing exercises it under real concurrent multi-user load |
| ~~D128~~ | **CLOSED 2026-08-21 — `_ocr_page_text`/`extract_pdf` (`extract_subprocess.py`) accepted ANY non-empty Tesseract output with no confidence gate, so a low-quality real-world scan could produce readable-looking-but-wrong text that sailed through undetected.** Measured directly: a real page of `d2l.pdf` rendered to a 300 DPI image and OCR'd upright recovers the real text at 96% mean per-word confidence; the SAME image rotated 90 degrees (Tesseract here has no orientation correction) OCRs to complete gibberish at 38% mean confidence — non-empty, so it passed `extract_pdf`'s only check and `structure_node`'s only guard. **Confirmed live 2026-08-21 before the fix:** D127's live tier ran this exact fixture through the REAL, fully-billed pipeline — `real_scan_like_rotated` (`lesson_id=91b6f717-b02d-425f-92f7-8425f0129992`) produced `package_valid=true`, `slide_quality=1.0`, `quiz_relevance=1.0`, real cost `$0.35`. A real, real-money-billed lesson built from OCR gibberish scored a PERFECT 1.0/1.0 and would have shipped to a student with zero visible signal. | High (was Med, raised after live confirmation — the harness's own scoring metrics did not catch it either, no safety net at any layer between OCR and the shipped lesson) | **FIXED — flag, not reject, matching this codebase's existing "downshift not abort" degradation philosophy.** `_ocr_page_text` now also calls `pytesseract.image_to_data()` (the same call used to measure the bug) and returns `(text, mean_confidence)` instead of a bare `str`. New `_OCR_LOW_CONFIDENCE_THRESHOLD = 60` (`extract_subprocess.py`) sits with a clear margin below the measured "readable" data point (96%) and above the measured "garbage" one (38%) — a reasoned margin, not a benchmarked constant, same status as D115's `TITLE_TAIL_WINDOW`. When OCR'd text lands below threshold, the content is still accepted into `raw_text` (never silently dropped — losing real content is the worse defect) but its ABSOLUTE page number is recorded in a new `low_confidence_ocr_pages` list, returned from `extract_pdf()` and plumbed into `extract_node`'s `lesson_jobs.node_outputs` checkpoint the same additive way `tables_detected`/`docling_pages` already are (`graph.py`) — persisted on the record, visible to a future admin view, not computed and discarded. `confidence=None` (no words detected at all) is a distinct case from a low real confidence and is never coerced into a flag. | 10 new/updated tests, all real, zero mocked-away confidence math: `test_extract_subprocess.py`'s `TestPerPageOcr` (4 new — high-confidence not flagged, low-confidence flagged, exact-threshold boundary, no-words-detected not flagged) + `test_extract_node.py` (1 new — the checkpoint plumbing itself, plus the old-shape backward-compat test extended) + `test_real_world_extraction.py` (the fixture that originally proved the bug now proves the fix: `real_scan_like_rotated` -> all 3 pages flagged; companion `real_scan_like` -> zero pages flagged, proving the check discriminates rather than flagging indiscriminately). Full `tests/unit` regression: **1245 passed, 9 skipped** (1239 prior baseline + exactly these 6 net-new tests), zero regressions. `ruff`/`mypy` clean on every touched file (3 pre-existing, unrelated mypy errors in `openai.py` providers untouched by this fix). **Not yet done:** the fix itself has not been re-verified against a real live run (would spend real money a second time) — the 2026-08-21 live confirmation above is PRE-fix; the flag's real behavior in `lesson_jobs.node_outputs` on a live run remains unit/mock-level verified only. |
| ~~D127~~ | **CLOSED 2026-08-20 — the eval harness (S3-1, 20 `fpdf`-generated PDFs) had zero real-world PDF coverage: every fixture is produced by a programmatic PDF *writer*, which structurally cannot emit a scanned page with no text layer, a rotated scan, a truncated/corrupted file, or a password-locked one — the actual shapes messy real student uploads take. Every OCR-fallback test in the repo monkeypatched the real Tesseract call out; nothing had ever run it against real scanned pixels.** | Med (the exact class of input the pipeline will see from real students, entirely untested against real code, not mocks) | **FIXED — 4 fixtures derived from a real, license-compatible textbook, not new copyrighted content.** `tests/fixtures/generate_real_world_pdfs.py` renders 3 real pages of `d2l.pdf` ("Dive into Deep Learning," CC BY-SA 4.0, already tracked at the repo root) to 300 DPI images (CLAUDE.md's own render-DPI constraint) and re-embeds them with NO text layer (`real_scan_like.pdf`), the same images rotated 90 degrees (`real_scan_like_rotated.pdf`), real `d2l.pdf` bytes truncated a third of the way through (`real_corrupted_truncated.pdf`, a real damaged upload, not hand-written garbage bytes), and a password-protected page via `fpdf2`'s own `set_encryption()` (`real_encrypted_locked.pdf`). No new dependency — `pypdfium2`, `PIL`, and `fpdf2` were all already in use. `EvadingEDR.pdf` (a commercial No Starch Press book, currently untracked) is deliberately never used as source content, for the same copyright reason `D115`'s fixture already only committed a captured JSON snapshot of its extraction output, never the PDF itself. Gitignored like the existing 20 eval PDFs (`apps/api/.gitignore`) — one variant is a multi-MB partial copy of the source book — regenerated on demand from `d2l.pdf`, which is tracked. Two layers: (1) `tests/unit/test_real_world_extraction.py` — free, no credentials, runs by default — calls the REAL `extract_pdf()` (no mocks) against all 4 fixtures, proving the OCR fallback genuinely recovers real text from an upright scan, and that the corrupted/encrypted fixtures fail loud with an identifiable cause (`"data format"`/`"failed to load"`, `"password"`), not silently. (2) `tests/evals/real_world_runner.py` + `tests/evals/test_live_run_real_world.py` — an opt-in tier gated behind `--run-live-eval` (same mechanism as `test_live_run.py`), reusing `run_eval()` from `runner.py` unmodified rather than touching the drift-guarded 20-PDF `_EVAL_PDF_KEYS`/`run_all_evals` contract — the two scan-like fixtures are expected to reach a valid, fully-billed `LessonPackage` (real spend, ~$1-1.30/lesson per D78/D87's measurements); the corrupted/encrypted fixtures are expected to fail for free, before any provider call, mirroring `book_ingest_job`'s already-documented zero-spend-on-ingestion-failure property (D124). **Found while building this, not fixed here:** the rotated-scan fixture's OCR output is silent garbage with no confidence gate — registered separately as **D128 (OPEN)**. Also incidentally committed in the same branch: **D126** (`tests/evals/conftest.py` — restores real credentials before a live eval run; `tests/conftest.py`'s stub Supabase env vars were silently shadowing `.env` for every `--run-live-eval` invocation, first surfaced diagnosing why S3-1's own live run kept failing before reaching any provider). | `tests/unit/test_real_world_extraction.py` — 6/6 passed (drift guard, zero-text-layer confirmation, real-OCR-recovery, the D128 garbage-OCR documentation test, corrupted-fails-loud, encrypted-fails-loud), real Tesseract/pdfium calls throughout, no mocks. Full `tests/unit` regression re-run alongside: **1239 passed, 9 skipped** (1233 passed pre-existing baseline + these 6 new, zero regressions, zero new failures). **UPDATE 2026-08-21 — `tests/evals/test_live_run_real_world.py` run live, PASSED.** `1 passed in 731.73s (12m11s)`. Real results: `real_scan_like` — valid, `slide_quality=1.0`, `quiz_relevance=1.0`, cost `$0.4791`, 419.6s. `real_scan_like_rotated` — valid, `slide_quality=1.0`, `quiz_relevance=1.0`, cost `$0.3505`, 294.0s (see D128's own update — this is the live confirmation that scored a perfect 1.0/1.0 on garbage content). `real_corrupted_truncated` — failed as expected, real `PdfiumError: Data format error`, $0, 13.0s. `real_encrypted_locked` — failed as expected, real `PdfiumError: Incorrect password error`, $0, 1.0s. Total real spend: **$0.8296**, well under the ~$1-1.30/lesson estimate. **This is the first time the content pipeline has ever been observed to run end to end against real OpenAI/Sarvam/Supabase and produce a real, valid `LessonPackage`** — every prior "verified" claim in this register (D43, D124's own closure note) was explicit that no live run had ever happened. That is still true of the 20-PDF S3-1 tier specifically — `test_live_run.py` itself remains unrun — this update covers only the 4-PDF real-world tier. |
| ~~D126~~ | **CLOSED 2026-08-20 — the eval harness's own literal-AC test (`test_live_run.py --run-live-eval`) failed all 20 PDFs in under a second with `Connection refused`, before reaching any provider — a SECOND, undisclosed blocker sitting behind D124, distinct from the Sarvam-credits blocker `docs/dev1-tracker.md`'s S3-1 entry names as the sole cause.** Root cause: `tests/conftest.py`'s module-level `os.environ.setdefault("SUPABASE_URL", "http://localhost:54321")` (and 9 sibling stub credentials) runs at pytest collection time for EVERY invocation in this repo, including `tests/evals/`, and pydantic-settings prioritizes `os.environ` over `.env` — so the real credentials in `.env` were silently never used, for as long as this gate has existed as documented (`pytest tests/evals/test_live_run.py -v --run-live-eval`). | Med (the eval harness's literal AC has never once been satisfiable as documented, for a reason unrelated to the disclosed Sarvam blocker) | **FIXED.** New `pytest_configure()` hook in `tests/evals/conftest.py`, scoped to fire only when `--run-live-eval` is passed: reads `apps/api/.env` directly via `dotenv_values()` and OVERWRITES (not `setdefault`) the stub values already sitting in `os.environ` by that point — `get_settings()` is called lazily inside `run_eval`'s own function body, never at collection time, so this is not too late. Raises loudly if `.env` is missing/empty rather than silently proceeding with stubs a second time. | Manually verified live in this environment: reverting this fix and re-running `pytest tests/evals/test_live_run.py -v --run-live-eval` reproduces the exact `Connection refused` failure on all 20 PDFs in ~3s; restoring the fix is required before any live run can reach a real provider. No dedicated unit test — this is a collection-time pytest hook, not application code; its correctness is the live run itself, same as `test_live_run.py`'s own gate. |
| ~~D125~~ | **CLOSED 2026-08-19 — first real pass at the Sprint 3 PRD item "Prompt iteration from eval results (slides + quiz quality)," which had not been started at all: `slide_generator_node`'s system prompt gave the LLM ZERO guidance on bullet length, while `tests/evals/scoring.py`'s `score_slide_quality` (the eval harness's own quality metric) scores any bullet over `_MAX_BULLET_CHARS=200` chars as a "wall of text" failure. Every existing guard in the node checked only for BLANK titles/bullets — a full-paragraph "bullet" passed every guard and shipped, then failed the eval harness's own scoring on every run. Found by inspecting what the harness actually measures (`scoring.py`), not from a real live eval run — D124 (this register) only just made the harness able to run at all, and no live run has been authorized/executed yet, so this is evidence-based prompt tuning against the SCORER's documented dimensions, not yet against real LLM output distributions.** | Low (quality/eval-score gap, not correctness or cost — no user-facing bug) | **FIXED, two-part (prompt + guard), not prompt-only.** Added `_MAX_SLIDE_BULLET_CHARS = 200` (`graph.py`, next to the pre-existing `_MIN_SLIDES_PER_SEGMENT`/`_MAX_SLIDES_PER_SEGMENT` constants, independently defined to match `scoring.py`'s own `_MAX_BULLET_CHARS` rather than imported — `app/` must not depend on `tests/`, same reason those two constants are already duplicated across the files). (1) The system prompt now explicitly states the exact character limit and instructs the LLM to split a longer idea into multiple bullets instead. (2) A prompt is a request, not a guarantee — a truncation guard was ALSO added, mirroring Story 2-22's own "degrade don't discard" precedent for an over-budget slide COUNT (truncate, don't reject the whole response): any bullet still over the limit after generation is truncated to `_MAX_SLIDE_BULLET_CHARS` chars with a trailing "…", logged as a warning, rather than raising (unlike the pre-existing blank-title/blank-bullet guards, which still hard-reject — a wall-of-text bullet is a lesser defect than a blank one and doesn't warrant discarding the whole segment's work). **AMENDED 2026-08-19 — real off-by-one caught by PR #151 review (Dev 2), before merge:** the original guard sliced to `_MAX_SLIDE_BULLET_CHARS` chars and THEN appended the 1-char ellipsis, producing a string up to `_MAX_SLIDE_BULLET_CHARS + 1` chars long — still `> _MAX_BULLET_CHARS` under `scoring.py`'s own check, so the "fix" still failed the exact "wall of text" test it exists to satisfy. Worse, `test_over_length_bullet_is_truncated_not_rejected` asserted the `+ 1` length as the *expected* value, locking in the bug rather than catching it — the 29/29 green reported below did not surface this. **FIXED:** slice to `_MAX_SLIDE_BULLET_CHARS - 1` before appending the ellipsis, so the final length lands exactly AT the limit. Also added a `# SYNC:` comment (Dev 3 review suggestion, same PR) pointing at `scoring.py`'s constant so the two don't drift silently. | 3 new tests in `test_slide_generator_node.py`: `test_over_length_bullet_is_truncated_not_rejected` (exact truncated length + trailing "…" + content preserved up to the cut, not just "doesn't raise"), `test_bullet_at_exactly_the_limit_is_not_truncated` (boundary case — off-by-one would either truncate a compliant bullet or admit a one-over one), `test_slide_prompt_states_the_bullet_length_limit` (the numeric limit is actually sent to the LLM, not just enforced silently after the fact). **RED-confirmed before GREEN**, not assumed: all 3 verified to fail with `ImportError: cannot import name '_MAX_SLIDE_BULLET_CHARS'` against the pre-fix code (`git stash` of `graph.py` only, tests already written), then verified green again after restoring the fix. Full 29-test `test_slide_generator_node.py` suite green (26 pre-existing + 3 new); adjacent 147-test regression (`test_phase1_economy_nodes`, `test_package_builder_node`, `test_lesson_planner_node`, `test_node_return_shape`) green; `mypy`/`ruff` clean. **Amendment's own coverage:** `test_over_length_bullet_is_truncated_not_rejected`'s assertion corrected to `== _MAX_SLIDE_BULLET_CHARS` (not `+ 1`) and now cross-checks against `scoring.py`'s own `_MAX_BULLET_CHARS` directly, not just the node's mirrored constant; new `test_bullet_one_char_over_the_limit_is_truncated_to_exactly_the_limit` pins the exact edge the bug lived at (smallest input that should still truncate). **RED-confirmed against the pre-amendment code** (both assertions failed `201 == 200`) before GREEN. Full 30-test suite green; adjacent 240-test regression green; `mypy`/`ruff` clean. |
| ~~D124~~ | **CLOSED 2026-08-19 — the eval harness (`tests/evals/runner.py`) has failed instantly on every one of its 20 PDFs since Story 1-13 (chapter-scoped generation) landed, and nobody had noticed: `run_eval()` predates that story and still fabricated a whole-book `lessons` row with no `chapter_id` at all, but `extract_node` was changed to REQUIRE a real, resolvable `chapter_id` and REFUSE to run without one ("Refusing to extract the whole document"). First discovered running the harness live for real (2026-08-18/19) — its own two other real gaps (Redis never initialised outside the FastAPI lifespan; the placeholder all-zeros `user_id` violating `books_user_id_fkey`) were fixed first, and THIS was the third, structural one underneath both.** | Med (the harness — the only instrument for re-measuring real per-lesson cost/quality against the $3.00 ceiling, Story 2-38 — has been unusable for its stated purpose since Story 1-13) | **FIXED, by running the real production ingestion path, not hand-rolled detection.** `run_eval()` now calls `book_ingest_job({}, book_id, storage_path)` (`app/workers/jobs/book_ingest.py`) — the exact function the ARQ worker enqueues, itself a plain `async def` and already called directly the same way by `tests/unit/test_book_ingest_job.py` — immediately after the existing Storage upload and before the `lessons` insert. This runs the real `detect_chapters` ladder and upserts real rows into `chapters`. The harness then queries those rows and picks the chapter with the largest page span (tie-break: lowest `chapter_index`) — deterministic across reruns, and a no-op for fixtures where detection falls through to its R5 whole-document fallback (the likely outcome for all 20 current synthetic fixtures, which carry no real TOC/heading hierarchy per `generate_eval_pdfs.py` — flagged as a residual limitation, not silently assumed fixed). The chosen `chapter_id` is threaded into both the `lessons` insert and the `run_pipeline(..., chapter_id=..., tier="T2")` call, satisfying `extract_node`'s contract exactly as the real `router.py` → `content_pipeline_job` → `run_pipeline` chain does. No change needed to `_cleanup_eval_rows`: `chapters.book_id` is `ON DELETE CASCADE`, so deleting `books` last (unchanged) already removes every chapter row this run created. Makes zero LLM/TTS/image calls (pure PDF parsing + the pure `detect_chapters` ladder) — confirmed by reading `book_ingest.py` and `chapter_detection/`, no new real-spend surface. Two real, pre-existing mypy errors surfaced while touching this file (Supabase's `.data` returning a recursive `JSON` union type, not a plain dict — `tests/` is outside CI's `mypy app` scope, so these had never been caught) were fixed in the same pass using this codebase's own established `rows()`/`single_row()` helpers (`app/core/db.py`), the same pattern `router.py`'s real chapter-fetch code already uses. | `tests/unit/test_eval_runner.py` (4 tests updated: `book_ingest_job` mocked at its source module per this file's own established lazy-import-mocking convention, `_mock_supabase()` extended with a fake `chapters` select response) + `tests/unit/test_eval_cost_capture.py` (`_patches()` extended the same way, 2 tests fixed) — full 35-test regression (`test_eval_runner`, `test_eval_cost_capture`, `test_eval_scoring`, `test_book_ingest_job`) green; `mypy tests/evals/runner.py` clean (0 errors, down from 35 pre-existing across 4 error sites); `ruff check` clean. **NOT verified against a real live run yet** (would spend real money) — Story 2-38's live-eval gate (`pytest tests/evals/test_live_run.py -m live_eval`) is the next real checkpoint for this fix, and per this register's own binding rule 7 this closure should be read as "the structural blocker is fixed, CI-verified" rather than "a real 20-PDF run has been observed to succeed end to end." | Covered entirely by the two updated unit-test files above; `tests/evals/test_live_run.py` (unmodified, still gated behind `--run-live-eval`) is the eventual real-money confirmation this entry's own "NOT verified" caveat names |
| ~~D123~~ | **CLOSED 2026-08-18 — closes D69's node-level half.** Every content-pipeline node ran with zero Langfuse observability of its own: only provider calls (LLM/TTS/image) were ever traced, so the Langfuse UI showed N flat provider spans per lesson with no indication which of the 15 nodes made which call or how long each node itself took end-to-end (setup/parsing/DB writes included, not just the provider call inside it). Surfaced while researching whether to wire Langfuse's `langchain`-dependent `CallbackHandler` for this instead (user asked for a production-ready recommendation) — installing `langchain` for that silently bumped the explicitly pinned `langgraph==1.2.6` to `1.2.11` (`langchain==1.3.15` declares `langgraph<1.3.0,>=1.2.11`), an unacceptable side effect of an observability change against a pin CLAUDE.md says never auto-upgrade. | Low (debuggability, not correctness or cost — same class as D69 itself) | **FIXED, without the `langchain` dependency.** Reverted the `langchain` install first, confirmed `langgraph` back at the pinned `1.2.6`. New `traced_node(node_name, *, seed_key="lesson_id")` decorator (`app/core/langfuse.py`) wraps a node coroutine with a Langfuse span, reusing the SAME `deterministic_trace_context`/`get_langfuse`/`safe_trace` primitives every provider call already uses — seeded by `state["lesson_id"]`, so each node's span lands under the identical trace_id as the provider calls made while it runs, not a second, disconnected trace. Applied via `@traced_node("X_node")` to all 15 content-pipeline node functions in `graph.py` (`extract_node` through `package_builder_node`). Deliberately NOT applied to the tutor FSM (`modules/tutor/state_machine/graph.py`) — its existing `_trace_dispatch()` already traces each dispatch correctly as its own event and its own docstring documents why a shared trace_id across an hour-plus session is wrong there; adding this decorator would be a regression, not an improvement. Deliberately does NOT nest the provider calls made INSIDE each node under that node's own span — doing so needs `parent_span_id` threaded through pipeline state into every provider constructor, the residual scope D69 stays open for (see D69's row, updated). Observability-failure contract preserved: `start_observation`/`span.update`/`span.end` are all `safe_trace`-wrapped, and the node's own exception always propagates after being recorded — a Langfuse outage can never fail the pipeline, same guarantee this file already makes everywhere else. | `test_traced_node_shares_the_pipelines_deterministic_trace_id` (proves the actual point — same trace_id as `deterministic_trace_context` would produce for the same seed, not just "was called") + `test_traced_node_returns_the_wrapped_nodes_state` + `test_traced_node_propagates_the_nodes_exception` (and marks the span ERROR first) + `test_traced_node_ends_the_span_on_success` + `test_traced_node_degrades_gracefully_with_no_seed_in_state` + `test_traced_node_never_raises_when_langfuse_itself_is_down` + `test_traced_node_default_seed_key_is_lesson_id` (`tests/unit/test_langfuse_core.py`) — full existing node-test suite (397 tests across all 15 decorated nodes plus `test_node_return_shape.py`'s source-level scan) re-run and green with the decorator applied, not assumed compatible |
| ~~D122~~ | **CLOSED 2026-08-18 — user-directed stack change, not a defect: primary image provider migrated from `gpt-image-1-mini` to `gpt-image-2`.** Decided by the team (not a silent side effect), for two independently-sufficient reasons: (1) `gpt-image-1-mini` retires 2026-12-01 regardless (D120's own note); (2) `gpt-image-1-mini`'s `size` only offered 3 fixed presets, none exactly 16:9 — every slide needed `_crop_to_16_9` to fix that up after the fact. `gpt-image-2` supports genuinely custom dimensions. | — | **DONE.** `openai_image.py`: model string -> `gpt-image-2`; new `_validate_size()` enforces the real documented constraints (max edge <=3840px, both edges multiples of 16px, long:short ratio <=3:1, total px 655,360-8,294,400) BEFORE any network call, raising a specific reason rather than surfacing an opaque API 400; `quality="medium"` now passed explicitly (was previously unset/ambiguous, undermining the cost table's own accuracy); `COST_PER_IMAGE` reduced to the one size this codebase actually requests, `_DEFAULT_COST_PER_IMAGE` as a genuine placeholder (gpt-image-2 bills per-token, not per discrete cell — this estimate is explicitly flagged unverified, same caveat this file already carries elsewhere). `graph.py`: `_SLIDE_IMAGE_SIZE = "1280x720"` — EXACT 16:9 (1280*9==720*16), a NAMED preset in gpt-image-2's own resolution taxonomy ("1k_wide_16:9"), not just a value that happens to satisfy the constraints; `_crop_to_16_9` is now a proven no-op on the primary path and exists purely as the fallback tier's safety net. **Also fixed in the same pass, found by inspection while migrating:** `imagen.py`'s `_SIZE_TO_ASPECT_RATIO` was a hardcoded `{size: ratio}` dict that had ALREADY silently gone stale once that same day (D120) and would have gone stale again the moment `_SLIDE_IMAGE_SIZE` changed a second time — replaced with `_closest_aspect_ratio()`, which computes the nearest Imagen enum from whatever size string is actually passed, so there is no size-specific table left to forget to update. **Also fixed:** `graph.py`'s cost-lookup fallback (`GPT_IMAGE_COST_PER_IMAGE["1024x1024"]`) would have raised `KeyError` outright the moment the cost table's keys changed — corrected to import and use the provider module's own `_DEFAULT_COST_PER_IMAGE`, a single source of truth instead of two files independently guessing a fallback. `CLAUDE.md`'s Locked Technology Stack table updated to match (source-of-truth doc, per this file's own D36 warning about doc/code drift). | `test_validate_size_*` (6 new: accepts every known-valid size, rejects malformed/over-max-edge/non-multiple-of-16/over-ratio/under-pixel-floor, each isolated to trip exactly one constraint) + `test_generate_raises_before_any_network_call_on_an_invalid_size` + `test_imagen_computes_the_nearest_ratio_for_a_size_never_seen_before` (proves real arithmetic, not a lookup table) + `test_the_real_production_size_is_exact_16_9` + `test_crop_to_16_9_is_a_true_no_op_on_the_real_production_size` + `test_a_non_16_9_landscape_image_is_still_correctly_cropped` (keeps the crop machinery itself proven for the fallback tier) — full existing D118-D121 suite re-run and green against every corrected value |
| ~~D120~~ | **CLOSED 2026-08-18 — found while researching the user's own follow-up question ("is this really 16:9?"): D118/D119's `_SLIDE_IMAGE_SIZE = "1792x1024"` is not a valid `size` for gpt-image-1-mini at all.** "1792x1024"/"1024x1792" are DALL-E 3's landscape/portrait sizes — confirmed directly against `developers.openai.com/api/docs/models/gpt-image-1-mini`, the only valid `size` values for THIS model are `"1024x1024"`, `"1536x1024"`, `"1024x1536"`. Requesting `"1792x1024"` against the real API is an invalid parameter and would fail the call outright on every single slide, cascading to the fallback provider (which per D121, below, is ALSO currently non-functional) on every generation — a regression D118/D119 introduced by trusting a `Literal` type and cost table that had silently carried over DALL-E-3-era values from before this module was rewritten to call `gpt-image-1-mini`, and were never exercised because the code never requested a non-square size until D118. The hardcoded `COST_PER_IMAGE` table was also found to be wrong in the same pass — $0.02/$0.03 (guessed, DALL-E-3-era) vs the real, currently-published $0.011/$0.015/$0.015 at "medium" quality (~45-50% lower). | High (would have shipped a request that fails 100% of the time on the primary provider) | **FIXED.** `ImageSize` `Literal` and `COST_PER_IMAGE` keys in `openai_image.py` corrected to the real values; `_SLIDE_IMAGE_SIZE` in `graph.py` corrected to `"1536x1024"`; `imagen.py`'s `_SIZE_TO_ASPECT_RATIO` keys updated to match (mapped to `"16:9"` rather than the numerically-slightly-closer `"4:3"`, deliberately — see that file's own comment on why). `_crop_to_16_9`'s docstring/tests updated: the REAL crop is materially bigger than what the wrong assumption implied — 15.6% of height trimmed (1536x1024 -> 1536x864), not 1.6%. The primary regression test (`test_crop_to_16_9.py`) now imports `_SLIDE_IMAGE_SIZE` and derives its expected output from it rather than hardcoding a size a second time, specifically so this class of drift cannot recur silently. Quality-tier pricing ($0.011/$0.015/$0.015) is still flagged as unverified against a live billing call, same caveat this codebase already carries for every other cost estimate — not fabricated certainty where none exists. | `test_gpt_images_actual_landscape_size_is_cropped_to_exact_16_9` (rewritten to derive expected dimensions from `_SLIDE_IMAGE_SIZE`, not a literal) + `test_imagen_translates_landscape_size_to_the_matching_aspect_ratio` (corrected size string) + full existing D118/D119 suite re-run and re-verified green against the corrected values, not just re-passed incidentally |
| ~~D121~~ | **OPEN, NOT FIXED — flagged, not silently patched. `ImagenProvider`'s hardcoded endpoint (`imagen-4.0-fast-generate-001`) is shut down and returning hard errors AS OF 2026-08-17 — the day before this was found.** Confirmed directly against Google's own docs (`ai.google.dev/gemini-api/docs/imagen`, fetched 2026-08-18): "Imagen models are deprecated and will shut down on August 17, 2026." This is the SAME failure pattern CLAUDE.md already records once (`DALL-E 3 DEAD (shut down May 2026)`, the reason `gpt-image-1-mini` exists in this codebase at all) — now repeating on the fallback tier of the very chain that replaced DALL-E 3. Google's own recommended replacement, Gemini 2.5/3.1 Flash Image ("Nano Banana"), is documented as "not a simple model-ID swap... different generation interface, accepts more input types and charges by output resolution" ($0.067/image vs Imagen 4's $0.02-0.06) — a real migration, not a one-line change. Practical effect on this codebase RIGHT NOW: every slide image that reaches the Imagen fallback tier (GPT Image failure, or once GPT Image 1 Mini itself retires — see the note in D120's `_SLIDE_IMAGE_SIZE` comment, currently scheduled 2026-12-01, replacement `gpt-image-2`) gets a hard HTTP error, repeated failures trip the circuit breaker (`app/core/circuit_breaker.py`), and the slide degrades to text-only. Nothing crashes — `image_generator_node`'s AC-11 per-slide isolation means this fails silently into "no image" rather than an error anyone sees — which is exactly why this could sit undetected until a student noticed missing slide art. | **High — live, not a future risk** | **Deliberately NOT fixed here.** This is a "Locked Technology Stack" (`CLAUDE.md`) provider decision — which replacement (Gemini Flash Image, following Google's own migration; `gpt-image-2` as the sole provider, dropping the two-tier fallback; or a third vendor entirely, see the research notes this session produced) is a call for the team, not something to change unilaterally as a side effect of a ratio bug fix. **Owner: TBD. Trigger: before this is discovered by a real student's missing slide image, or immediately given this is already live, whichever comes first.** | None yet — this is a research finding, not a code change; a regression test proving the fallback path degrades gracefully when Imagen hard-fails already exists (`test_openai_failure_falls_back_to_imagen` and friends in `test_image_generator_node.py`, all provider-mocked) but nothing in this suite can detect that the REAL endpoint is dead, since nothing here calls it live — that gap is itself worth naming, not just the endpoint's death |
| ~~D119~~ | **CLOSED 2026-08-17 — D118 follow-up, found on direct re-verification when asked to confirm the ratio was really 16:9.** It was not. `_SLIDE_IMAGE_SIZE = "1792x1024"` (D118's fix) is GPT Image 1 Mini's only landscape preset, but 1792/1024 reduces to exactly **7:4 = 1.75:1**, not 16:9 (1.7778:1) — confirmed by direct computation (`Fraction(1792,1024) == Fraction(7,4)`), about **1.56% narrower** than true 16:9. Separately, `ImagenProvider`'s fallback path was mapped to Imagen's own `aspectRatio="16:9"` request parameter, but this environment cannot make a live call to confirm what exact pixel dimensions Imagen 4 Fast actually returns for that preset — so the primary and fallback providers were never verified to agree with EACH OTHER on aspect ratio, only each independently claimed to be "landscape." | Low (cosmetic precision — D118's fix already resolved the actual reported symptom; this closes a ~1.6% imprecision, not a repeat of the 43%+ square-vs-landscape mismatch) | **FIXED.** New `_crop_to_16_9()` (`graph.py`) — a pure, provider-agnostic post-processing step run on every generated image right after `_decode_data_uri`, before upload. Introspects the ACTUAL returned image dimensions via Pillow (already a declared dependency) rather than trusting either provider's declared size, and center-crops to the nearest exact 16:9 that fits inside them: at GPT Image's real 1792x1024 output this trims 16px of total height (8px/edge, 1.6% of the image) to land on exactly 1792x1008 — confirmed `1792*9 == 1008*16`. Never upscales, never pads. An already-exact-16:9 input is returned byte-identical (no-op, no re-encode). Any image PIL cannot open degrades to the ORIGINAL bytes unchanged rather than raising — a cosmetic-normalization failure must never fail an otherwise-good slide (AC-11's spirit, applied one level more conservatively: it doesn't even degrade the slide, only the ratio guarantee). This makes the two providers' outputs provably identical in aspect ratio to each other and to a strict 16:9, closing the verification gap rather than leaving it as an assumption. | `test_crop_to_16_9.py` (5 tests: GPT Image's real 1792x1024→1792x1008 exact-16:9 crop with the precise expected pixel values computed and asserted, not guessed; an already-16:9 image returned byte-identical; a square image correctly crops HEIGHT not width, with the exact expected 1024x576 computed; corrupt bytes and empty bytes both degrade to the original input rather than raising) + `test_d118_uploaded_bytes_are_actually_cropped_to_exact_16_9` (`test_image_generator_node.py` — proves the crop is really wired into the node's real upload path using an actual PNG, not just correct in isolation; the node's other tests use an opaque, non-image `_FAKE_DATA_URI` that would make the crop step silently no-op via its degrade-on-error path, so this needed its own dedicated real-image test). |
| ~~D118~~ | **CLOSED 2026-08-17 — every generated slide image was a fixed 1024x1024 SQUARE, regardless of the slide layout, which visually reads as a cropped cutout even though no CSS ever crops it.** `SlideRenderer.tsx`'s `SlideImage` renders every image with `object-contain` inside a wide (`w-full`, `max-h-[38vh]`) panel — sized from the image's own intrinsic ratio, per a prior review fix (its own comment: "object-cover + aspect-video was CROPPING the image... browser sizes the element from its own intrinsic ratio... so the full image is always visible, never cropped"). That fix was correct and is untouched — the actual defect is one layer upstream: `image_generator_node`'s `_generate_image_with_fallback()` called `OpenAIImageProvider(lesson_id).generate(prompt)` with no `size` argument, defaulting to `"1024x1024"` even though `"1792x1024"` (a landscape preset close to the panel's implied 16:9 shape) was already a declared, priced `ImageSize` option — defined and never used. `ImagenProvider` (the fallback) was worse: its docstring stated outright that `size` was "accepted for interface compatibility" only and silently discarded — Imagen's REST body never carried an `aspectRatio` field at all, so even a caller that DID ask for landscape could not get one from the fallback path. Net effect: every slide image renders as a small square block marooned in a wide panel — not cropped, but visually indistinguishable from a fixed square cutout, exactly as reported. | Med (visible on every lesson with a generated image, not an edge case) | **FIXED.** New `_SLIDE_IMAGE_SIZE = "1792x1024"` constant (`graph.py`), threaded through `_generate_image_with_fallback()`'s new keyword-only `size` param to BOTH provider calls. `ImagenProvider._generate_inner()` now translates `size` to Imagen's real `aspectRatio` parameter via a new `_SIZE_TO_ASPECT_RATIO` map (`"1792x1024"` → `"16:9"`), defaulting to `"1:1"` for an unrecognised size rather than raising (this is the fallback provider — wrong shape, right content, beats a hard failure). The per-image cost lookup in `graph.py` was ALSO silently wrong before this fix and is fixed alongside it: it looked up `GPT_IMAGE_COST_PER_IMAGE.get("1024x1024", ...)` unconditionally — a hardcoded literal that would have under-reported real spend against the $3.00/lesson ceiling the moment the size changed, had this not been caught in the same pass. Now keys on `_SLIDE_IMAGE_SIZE` itself, so the two numbers cannot drift apart. Cost per slide image rises modestly ($0.02 → $0.03 on the GPT Image path) — noted, not separately gated; a lesson's slide count is small enough (single digits) that this cannot meaningfully threaten the cost ceiling, and `check_ceiling()` already runs proactively before every image call regardless. | `test_d118_requests_a_widescreen_size_not_the_old_default_square`, `test_d118_imagen_fallback_also_requests_the_widescreen_size`, `test_d118_cost_accumulated_matches_the_size_actually_requested` (`test_image_generator_node.py`) + `test_imagen_translates_landscape_size_to_the_matching_aspect_ratio`, `test_imagen_unrecognised_size_degrades_to_square_rather_than_raising` (`test_image_providers.py`) |

---

### OPEN — found by the 2026-08-04 Story 1-14 review (book-scale Phase 6 endpoints)

Every `file:line` below was verified by opening the file on the day of writing. Ids allocated
after re-reading the highest in use (D43) per the collision rule at the top of this file.

| ID | Defect | Sev | Decision | Enforcement |
|----|--------|-----|----------|-------------|
| **D44** | **`chapters.lesson_id` is a dead column carrying a live `ON DELETE CASCADE`.** Nothing writes it: its only writer was deleted in Story 1-13, and `book_ingest_job` now writes `"lesson_id": None` explicitly (`apps/api/app/workers/jobs/book_ingest.py:97`). But dead is not harmless — the FK is still there. `chapters.lesson_id uuid REFERENCES public.lessons(lesson_id) ON DELETE CASCADE` (`supabase/migrations/20260611000000_initial_schema.sql:132`; `20260803000000_chapters_book_scoped.sql:52-58` drops `NOT NULL` and says in terms that the constraint and its cascade are untouched), and `chunks.chapter_id ... ON DELETE CASCADE` (`20260611000000:147`) cascades from the chapter. So the *obvious* implementation of a generate endpoint — insert the lesson, point the chapter at it, delete the lesson when the enqueue fails — **deletes the chapter and every chunk and embedding under it**. A whole book's ingestion destroyed by one failed generation. Story 1-14 forbids reading or writing the column (AC14) rather than relying on nobody being helpful. | **High** (latent; destructive on first write) | **Do not write, backfill, or read the column.** Dropping it is the durable fix and is a **frozen-contract migration** (CLAUDE.md §16, four-dev review), so it waits until legacy rows carrying a non-null value are purged and no reader remains. **Trigger: the legacy purge** — at that point drop the column in the same migration that removes the constraint. Until then the guards below are the only thing between us and the outage. | `tests/unit/test_pipeline_writes_no_books.py` — the scan is widened from `PIPELINE_DIR` to all of `app/modules/content/` for `chapters` (`:49-61`), so a `chapters` write anywhere in the module fails CI; plus `tests/integration/test_generate_rollback_postgres.py` (`-m postgres`), which **demonstrates the cascade on real Postgres** — `test_writing_chapters_lesson_id_makes_the_same_rollback_destroy_the_chapter:329` and the premise test `test_the_cascade_edges_this_file_depends_on_are_present:135` reading `confdeltype` so the file cannot pass for the wrong reason |
| **D45** | **The `(chapter_id, tier)` idempotency check is TOCTOU-racy and there is nothing in the database to lean on.** Story 1-14 AC9 pre-checks `lessons` for an existing non-failed lesson before inserting (`apps/api/app/modules/content/router.py:1050-1072`), then inserts. Between the two, a concurrent request can run the same check and see the same nothing. **Verified: no UNIQUE constraint exists on `chapters.lesson_id`, on `lessons.chapter_id`, or on `(chapter_id, tier)` in any of the ten files under `supabase/migrations/`** — the only index on that column is `lessons_chapter_id_idx`, deliberately non-unique (`20260803000000:77`) because one chapter is *meant* to carry a lesson per tier. So two concurrent requests both insert, both enqueue, and **both bill**. | Low–Med | **Accepted for Phase 6, bounded not fixed.** Exposure is capped by AC12's `max_concurrent_generations_per_user` (3) and AC13's `3/minute;20/hour` limiter, so the worst case is a small multiple of one lesson's spend, not an unbounded one — and the caller sees two lesson ids rather than data loss. The durable fix is a partial unique index `(chapter_id, tier) WHERE status <> 'failed'`, which is a **frozen-contract migration** and out of scope for Story 1-14. Explicit regeneration (`?force=true`) is likewise registered, not built. **Trigger: the first observed duplicate pair in `lessons`, or the India-region migration adding a second API replica** (which also removes D49's per-replica limiter as a mitigation). | DISCIPLINE — the race is stated plainly in `generate_chapter_lesson`'s docstring so nobody reads the pre-check as a guarantee. No test can prove a race absent; the index is what would |
| **D46** | **The page-span gate stops catastrophe, not truncation — a chapter can pass every gate and still be built from a quarter of itself.** `settings.structure_max_sections = 15` (`apps/api/app/config.py:332`) × `_get_section_body(..., max_chars=6000)` (`apps/api/app/modules/content/pipeline/graph.py:1942`) means **~90,000 characters is the entire LLM-visible window regardless of page count** — 32–39 pages at the 2,296–2,816 chars/page measured across the Phase 1 corpus (tracker § Phase 1 Observed result). `max_chapter_pages = 200` (`config.py:156`) therefore admits the corpus's largest real chapter (D2L Appendix A, 138 pages), which is then built from roughly **28 %** of itself. **Story 3-39 (2026-08-11, Round 2 on 2026-08-12) fixed the "nothing surfaces it" half of this entry** — previously the only trace was a `logger.warning` per truncated section, read by nobody. `_get_section_body` now returns `was_truncated`/`original_chars`/`capped_chars`, all 6 Phase 1 economy nodes append a `section_truncations` entry to a fan-out-safe `PipelineState` channel, and `package_builder_node` writes it into `lesson_jobs.node_outputs` as a sibling of `package_builder_degraded` — an explicit, persisted, surfaced degradation. Round 2 review (2 of 4 parallel reviewers, independently) found the surfacing itself silently dropped out on an ARQ retry: a section whose Phase 1 checkpoint already existed took the cache-hit early-return path, which skips `_get_section_body` entirely, so `section_truncations` for that section vanished from the aggregate on any retry after its own checkpoint landed — closed by persisting the truncation signal into its own retry-durable checkpoint entry (`_persist_section_truncation_checkpoint` / `_read_section_truncation_checkpoint`), reconstructed on every cache-hit rather than only computed fresh. **The root cause — the ~90,000-char window itself — is unchanged and remains open**, and because the lesson is *cheap and wrong* rather than expensive, the $3.00 ceiling still never fires on it. Any AC phrased as "the cost ceiling protects us here" would still be false. | Med (quality, silent — visibility half now fixed) | **Surfaced, not hidden and not fixed** (still true for the root cause). Story 1-14 made the chapter/endpoint level visible (`truncation_expected` on the 202 response); **Story 3-39 made the per-section, per-Phase-1-node level visible too** (`section_truncations` in `lesson_jobs.node_outputs`). The real fix for the underlying cap is still chapter sub-segmentation (feed `lesson_planner` a section-level split rather than a capped window), which is a pipeline change, not an endpoint one, and remains out of scope for both stories. **Trigger: Phase 7's "no truncation warning" acceptance assertion firing on a chapter we intended to be whole, or the first user complaint about a lesson that skips the back half of its chapter.** | DISCIPLINE for the underlying truncation (cap value unchanged). The *visibility* is guarded two ways now: `truncation_expected` is parametrised over 35/40/41/138/200/201/1151 in `tests/unit/test_generate_lesson_endpoint.py` (chapter level), and `TestSectionTruncationSurfaced` in `tests/unit/test_phase1_economy_nodes.py` (Story 3-39, section level) asserts both the non-empty and always-present-empty-list shapes, so neither flag can silently stop firing |
| **D47** | **A rung-5 whole-document book cannot be generated from at all.** When every detection rung fails, Phase 3 emits one chapter spanning the entire PDF with `boundary_confidence='fallback'` (tracker § Phase 3, R5 row at `docs/book-scale-phase-tracker.md:384`). That single chapter's span exceeds `max_chapter_pages` **by construction** for any book over 200 pages, so `POST .../chapters/{id}/lessons` returns `422 chapter_too_large` for the book's only chapter — a browsable book with nothing generatable in it. This is deliberate: the alternative is the 1,151-page single-chapter lesson this entire effort exists to eliminate. It is registered because it is a **user-visible dead end**, not because the gate is wrong. | Low (today) | **Accepted.** 0 of the 8 books in the Phase 1 corpus reached rung 5 — every no-bookmark book yielded at least one usable signal (tracker § Phase 1 Observed result), so the frequency is measured at zero rather than assumed. The 422 already returns `boundary_confidence` in its detail specifically so the client can distinguish "this chapter is absurd" from "detection found nothing", which is the copy a user needs. **Trigger: the first book that actually lands on rung 5** — at which point the answer is a manual/assisted chapter split at ingest, not a higher cap. | DISCIPLINE — the `boundary_confidence` field in the `chapter_too_large` detail (`router.py:1080-1088`) is what makes the case diagnosable when it happens |
| ~~D48~~ | **CLOSED 2026-08-11 (Story 3-35, branch `sprint3/s3-35-env-config-fixes`) — option (b), deleted.** `max_daily_spend_per_user_usd` had zero call sites — dead config that reads like a cost control. Was defined at `apps/api/app/config.py:150` (default `10.00`), documented as a real control in `.env.example:58`, `docs/dev1-tracker.md:332` and `.claude/commands/check-costs.md:18`. A repo-wide grep for `max_daily_spend_per_user_usd` / `MAX_DAILY_SPEND` returned **only those four declarations and this register** — nothing read the setting, no code path compared anything to it. The only spend controls that actually run are the per-lesson `max_lesson_cost_usd` ceiling and Story 1-14's per-user concurrency cap; daily per-user spend was, and remains, unenforced — now **documented as such** instead of silently implied otherwise. Same shape as D29 and D18: the artefact existed, the enforcement was never built. | Med (spend) | **Deleted rather than implemented** — a new Redis-backed daily-spend control is a distinct, larger unit of work with its own Scale & Load answers; smuggling it into a config-correctness story was explicitly rejected. Field removed from `config.py`; doc references removed/rewritten to state plainly that daily spend is not enforced. **Correction (Story 3-35's own review round):** the original close-out claimed "3 doc references," which itself undercounted — a 4th, `docs/stories/1-14-generate-lesson-from-chapter.md:353`, was found by an independent adversarial pass and annotated closed too. If daily-spend limiting is still wanted, open a fresh, separately-scoped story. | `apps/api/tests/test_env_example_consistency.py::test_max_daily_spend_per_user_usd_has_a_real_reader_or_does_not_exist` — asserts the field either has a real reader outside `config.py` or does not exist at all; passes today because it no longer exists. RED-confirmed (zero readers) before the fix. |
| **D49** | **`RATE_LIMIT_STORAGE_URL` unset ⇒ `memory://` ⇒ every rate limit multiplies by replica count.** `limiter = Limiter(..., storage_uri=os.environ.get("RATE_LIMIT_STORAGE_URL", "memory://"), ...)` (`apps/api/app/core/rate_limit.py:69`). In-process storage means N replicas enforce N × the configured ceiling, and a restart resets every counter. Known since the limiter landed; **newly load-bearing** because Story 1-14 attaches `@limiter.limit("3/minute;20/hour")` (`router.py:931`) to an endpoint that **spends real money** — at $3.00/lesson the hourly ceiling is ~$60/user/hour per replica, so the number that was chosen to bound spend is only correct at exactly one replica. Redis is already a locked dependency (CLAUDE.md), so the fix is configuration, not architecture. | Med (was Low) | Set `RATE_LIMIT_STORAGE_URL` to the Railway Redis URL in every deployed environment and assert it is set when not in debug. Note this **does not** subsume D45 or D48 — a rate limit caps request *rate*, `max_concurrent_generations_per_user` caps concurrent pipelines, and neither caps daily spend. **Trigger: the first deploy with more than one API replica, or the India-region migration** (CLAUDE.md requires it before Sprint 3), whichever lands first. | DISCIPLINE — recorded in `generate_chapter_lesson`'s docstring per AC13. **This row asserted that record before it existed** (caught by the 2026-08-04 AC-completeness layer: `memory://` appeared only in `core/rate_limit.py` and in a test docstring). Added to the handler 2026-08-04. *(to add — a startup assertion that `storage_uri != "memory://"` outside debug)* |
| **D50** | **300-DPI page rendering and image upload have no count cap and are outside `cost_tracker`'s view entirely.** `pdfium_page.render(scale=300 / 72)` runs once per OCR'd page (`apps/api/app/modules/content/pipeline/nodes/extract_subprocess.py:411`) and once per page carrying kept images (`:473`); every produced PNG is then uploaded to Supabase Storage in a bounded-concurrency fan-out (`graph.py:423`, `:471-479`) whose **bound is on concurrency, not on count** — the loop uploads one object per image found, however many that is. Nothing anywhere caps images per page, per chapter or per lesson. Meanwhile `accumulate_cost` is called from exactly four places — the LLM provider (`providers/llm/openai.py:343`), the embeddings provider (`providers/embeddings/openai.py:175`), and the TTS and image-generation sites in `graph.py:3465` and `:3719`. **Render CPU/RAM and Storage egress are invisible to the $3.00 ceiling**, so a figure-dense chapter can consume real resources while the ceiling reports the lesson as cheap. | Med | **Accepted for Phase 6** — Story 1-14 adds no PDF parsing and the page-span gate (D46) is a partial bound, since fewer pages means fewer renders. The durable fix is a per-lesson image cap enforced in the subprocess (where the memory actually lives) plus a Storage line item in `cost_tracker`. **Trigger: the Phase 7 acceptance run's measured storage/runtime figures, or the first worker OOM on a figure-dense chapter.** Do not raise the render DPI to "fix" quality before this is bounded — 300 DPI is a hard floor (CLAUDE.md), not a target. | *(none — a per-lesson image-count assertion in the subprocess and a `cost_tracker` category for Storage would close it)* |
| ~~**D51**~~ | **CLOSED 2026-08-04.** Seven ACs in Story 1-14 need real Postgres or real PostgREST, and only one of the two harnesses runs in CI — the other skips invisibly.** CI *does* have a gating `-m postgres` step (`.github/workflows/ci.yml`, the `Migration tests` step), and the migration-replay suite spins its own throwaway container, so `[PG]` work is genuinely covered. **`[PGRST]` is not.** `tests/integration/test_book_select_lists_against_postgrest.py` is `pytestmark = pytest.mark.postgres`, but its session fixture skips unless a PostgREST is already listening on `POSTGREST_URL` (`:62-78`), and that container is **hand-started from prose in the fixture docstring** (`:68-73`) — there is no compose file and no CI step that starts it. Worse, the step's own anti-vacuum guard is `grep -qE "^[0-9]+ skipped\|no tests ran"` (the pre-2026-08-04 grep, since replaced), which only matches an **all**-skipped run: a summary line reading `N passed, M skipped` does not match, so the PostgREST tests skip in CI **today, green, unreported**. A harness that skips in CI guards nothing — `FIXED-UNGUARDED` under binding rule 7, and precisely the shape that let D37 sit open. | Med (process) | **OPEN DECISION — must be closed in writing, with an owner, before Phase 7 exits.** Two options, and this register recommends **(a)**: *(a)* add PostgREST as a CI **service container** alongside the existing Postgres step, so `[PGRST]` gates on every PR like `[PG]` already does; *(b)* keep it a local pre-merge gate with a **named owner** who runs it and records the pass in the PR. (b) is the status quo with a name attached and depends on someone remembering, which BD-2/BD-7 say does not hold here. Independently and regardless of which is chosen, **tighten `ci.yml:84` to fail on ANY skip in the postgres step**, not only a total skip — the current grep is why this was invisible. **Trigger: the Phase 7 acceptance run** — it cannot be signed off while a `[PGRST]` AC is discharged by a skip, because *a skip does not satisfy an AC*. | `.github/workflows/ci.yml` — the `-m postgres` step fails on any `skipped`/`no tests ran`, truth-tabled over four summary shapes, and installs `postgresql-client` explicitly rather than assuming the runner image ships it Closing enforcement under (a) is the CI service-container step itself; under (b) it is the tightened `-rs` check plus the named owner's recorded pre-merge run. The suite already skips **visibly** (`:62-78`), so no result is silently wrong — it is simply absent |
| ~~**D52**~~ | **CLOSED 2026-08-04 (fixed + guarded).** The rate limiter was keyed by IP, not by user — every authenticated caller shared one bucket.** `_get_user_key` (`apps/api/app/core/rate_limit.py:26-46`) decoded the bearer token to read `sub`, but passed no `audience=` while **every Supabase token carries `aud: "authenticated"`**. PyJWT raises `InvalidAudienceError` in exactly that case, the bare `except Exception` swallowed it, and the function returned `get_remote_address(request)`. So one caller exhausting `3/minute` on the generate endpoint locked out every other user behind the same egress IP — and behind a proxy or CDN that is all of them. Present since `upload_lesson`'s `5/minute` was written; the new money-spending endpoint is what made it matter. Found by the Story 1-14 live gate, which expected a 404 for another user's book and got a **429** — **no unit test could have caught it**, because the failure is a swallowed exception logged at DEBUG. | High | **FIXED 2026-08-04** — `verify_aud: False` (signature verification is untouched; `get_current_user` still enforces expiry and audience before any handler runs), and the fallback log raised DEBUG → WARNING because reaching it is a security-posture change, not a detail. | `tests/unit/test_rate_limit_key.py` — 8 tests, mutation-checked (removing the flag reddens 3), including `test_the_aud_claim_is_really_present_and_really_breaks_a_naive_decode`, an executable premise assertion for binding rule 3 |
| ~~D53~~ | **CLOSED 2026-08-13 (Story 3-48, branch `sprint3/s3-48-d53-stale-lesson-reaper`) — the register's own two-part fix description had only ever had its first half built.** A lesson stuck in `generating` was a permanent, unrecoverable dead end for that user: neither the idempotency pre-check nor the concurrency count bounded `generating` by age, and no code path ever transitioned a lesson out of `generating` except the worker itself. **Verified by direct code read before writing anything (not assumed from this row's own text, which was stale):** `router.py`'s `_generating_cutoff_iso()` and both call sites (Gate 5 idempotency, Gate 7 concurrency) already carried the staleness-predicate half of the fix, with the function's own docstring stating exactly what was still missing — *"there is no reaper... The durable fix is the D53 reaper plus a real `started_at`."* So the query-level workaround worked (a user could eventually regenerate), but the ORIGINAL stuck row itself never actually became `failed` — `GET /lessons/{that exact id}` and `list_lessons` showed a phantom `generating` entry forever, which is why D53 stayed open and live in production. | High (live in production until this fix) | New ARQ cron job `reap_stale_generating_lessons` (`apps/api/app/workers/jobs/reap_stale_lessons.py`), scheduled every 10 minutes via `WorkerSettings.cron_jobs`. Finds `lessons.status='generating'` rows past the SAME staleness bound the query-level workaround uses (`router._generating_cutoff_iso()`, imported directly — not duplicated, so the two can never silently drift apart on what counts as stale) and reaps each one through `content_pipeline.py`'s own `_update_lesson_status` helper — the exact path every other failure transition in this codebase already goes through, so D86's real-cost persistence is inherited for free rather than reimplemented. One row's reap failure does not stop the batch. Query bounded at `.limit(100)` per pass (Scale Contract Q4) — a real stuck-row count anywhere near that in one 10-minute window is its own incident, not something to silently drain unbounded in a single pass. **Deliberately not fixed here** (a separate, non-production-blocking known limitation, explicitly "accepted for now" in the pre-existing code's own comment): a real `started_at` clock, so the staleness bound covers queue-wait time as well as run time. | `tests/unit/test_reap_stale_lessons.py` — 5 tests: a stale row is reaped via `_update_lesson_status` with a D53-attributed error (RED-GREEN verified by moving the implementation file aside and confirming `ModuleNotFoundError`, then restoring); zero stale rows is a pure no-op; the query filters on `status='generating'` and the real `_generating_cutoff_iso()` value (not a locally re-derived cutoff); the query is bounded (`.limit()` asserted directly); one bad row's reap failure does not stop the batch. Full repo-wide regression: 52 failed/2103 passed/86 skipped — the established baseline, zero new failures. |
| ~~D54~~ | **CLOSED 2026-08-13 (Story 3-50, `sprint3/s3-50-d54-force-regenerate`) — D53's own escape hatch, landing the same day D53 closed.** Story 1-14 AC9 returned the existing lesson for any `generating`/`ready` match on `(chapter_id, tier, user_id)`; only a `failed` match regenerated. A student whose lesson was poor, truncated (D46), or previously stuck (D53) had no route to a new one. | Med | Added `GenerateLessonRequest.force: bool = False`. When `True`, Gate 5's idempotency early-return (the branch that hands back an existing `generating`/`ready` lesson instead of creating a new one) is skipped entirely — a new `lessons` row is always created and enqueued. Gate 6 (page-span) and Gate 7 (concurrency) remain fully unconditional; `force=true` can still 422/429. No "mark superseded" logic was needed — confirmed by reading `_latest_lesson` before implementing: it already picks `max(created_at)` fresh on every read, so a new row automatically becomes latest the instant it's inserted. | 4 new tests in `test_generate_lesson_endpoint.py`: `force=true` creates a new lesson despite an existing `generating`/`ready` one (parametrized); `force` omitted is unchanged from the pre-fix default; `force=true` still respects the concurrency cap. RED-GREEN verified via the Edit tool. Full repo-wide regression: 54 failed/2102 passed/85 skipped before, same failed set/2107 passed after — zero new failures. |
| ~~D55~~ | **CLOSED 2026-08-12 (fixed + guarded, on `sprint3-master`, independently of D53/D54's closures above which landed on `main`).** A session started AFTER a lesson is already `ready` got no `lesson_package` cache entry. `pubsub.py` writes `lesson_package:{session_id}` at publish time, for the sessions waiting *then*. A student who opens an already-`ready` lesson creates a session with no entry, so `_seed_learner_tier` (`apps/api/app/core/websocket.py:279`) silently returned and learner tier / `qa_phase` were never seeded, and `_segment_intervention_messages` (`apps/api/app/modules/tutor/service.py:253`) returned `{}` — the Sprint 3 intervention hot path found nothing, for virtually every real session (this is the overwhelmingly common case — a re-attempt of an existing lesson, not the rare first-generation-watching session). **Found live 2026-08-12** via the trigger this row already named: a real full-lesson playthrough with a real webcam. CES correctly dropped into the "Low" band on sustained (>10s) distraction, the FSM correctly transitioned to `INTERVENING`, but no `tutor_intervene` message ever reached the client. This is the residue of D34: the routing was fixed, the *timing* was not. | Med | **FIXED 2026-08-12** — exactly the durable fix this row prescribed: `_fetch_and_cache_lesson_package()` added to `core/websocket.py`, called from `_seed_learner_tier` as a read-through fallback on a cache miss. Fetches `sessions.lesson_id` → `lessons.content` (the same column `content/router.py:get_lesson` reads) and writes it back under the SAME `lesson_package:{session_id}` key `pubsub.py` uses, with the same 24h TTL — so `_segment_intervention_messages`'s later read hits Redis, not Supabase, for the rest of that session's lifetime. Degrades to a no-op (never raises) on any Supabase failure or a session/lesson that doesn't resolve, matching this function's existing never-crash-the-handshake contract. | `tests/test_websocket_session.py` — 5 new tests (`test_g13`–`test_g17`): cache-miss-falls-back-and-seeds-tier, fallback-populates-the-cache-for-the-next-read, fallback-session-not-found-writes-no-tier-keys, fallback-supabase-error-does-not-raise, and an end-to-end proof (`test_g17`) that `_segment_intervention_messages` — the actual intervention hot path — reads real messages after the fallback runs, not the `{}` it always returned before. All 59 pre-existing `test_websocket_session.py` tests plus these 5 pass; full suite re-run: same 24 pre-existing unrelated failures (D40's dna_growth leak, D58's eval_runner, 3 pubsub tests needing local Redis), 1812 passed (was 1807), no new failures. |
| **D56** | **`lessonService.getLesson` and `updateProgress` are mock-backed and unreachable.** `apps/web/src/services/lesson.service.ts:8,11` delegate to `lessonApi` from the hand-rolled `src/mocks/` layer; a repo-wide search finds **no caller** outside the file and its own tests. They are the only mock imports left on the book-scale service surface (`getLessonPackage`, the player's actual content fetch, is real). Not deleted: `src/__tests__/services/lesson.service.test.ts:70,76` deliberately pin the delegation with the note "no real backend yet", so removing them would delete documented, tested state rather than dead weight. | Low | Delete both methods, their tests and the `lessonApi` import together, **or** implement them against real endpoints — as one deliberate change, not by attrition. **Trigger: the first real caller for either (a dashboard lesson card needing the card-shape read, or progress persistence), or the removal of `src/mocks/`.** Owner: Dev 2. | `apps/web/src/__tests__/guards/no-mocks-in-book-scale-path.test.ts` — the book-scale entry points are guarded transitively, so these two cannot spread into the render path unnoticed |
| ~~**D57**~~ | **WITHDRAWN 2026-08-05 — DUPLICATE of D31.** Registered during the Story W4 pass for `NEXT_PUBLIC_API_URL` omitting `/api`; D31 already covers exactly that, and is marked live-in-production with Dev 1 as owner. The line numbers differ only because `ci.yml` moved (`:126` → `:188`) — same defect, same file, same fix. **This is the ID-collision the banner at the top of this file exists to prevent, committed by the person who had just corrected the same mistake for D52.** The rule is: re-read the highest allocated id from `main` immediately before allocating — and then check whether the defect is already registered under an older one. Use **D31**. | — | See D31 | See D31 |
| **D58** | **The evaluation harness writes a success-shaped result file when every PDF crashed.** `apps/api/tests/evals/runner.py:277-316`. This is the incident quoted as the justification for the phase tracker's entire GATE RULE (`docs/book-scale-phase-tracker.md:79`) — *"the evaluation harness crashed on all five test PDFs and wrote a success-shaped result file anyway"* — and it has **never had a register ID**, which is precisely the failure binding rule 5 names: a documented limitation without an ID is a defect wearing a decision's clothes. **Directly in Sprint 3's path:** S3-1 is *"Eval harness expanded to 20 PDFs"*, and expanding a harness that reports success on crash multiplies the blast radius by four. | High | Fix the result writer to record per-PDF outcomes and fail loudly on any crash, **before** S3-1 expands the corpus. **Trigger: S3-1 starting.** Owner: Dev 1. | *(to add — a test that a crashing eval run produces a FAILING result file, mutation-checked)* |
| D59 | **Two unbounded reads on request paths, enumerated as the `test_unbounded_queries.py` baseline. Part (a) CLOSED 2026-08-13 (Story 3-51, `sprint3/s3-51-d59a-admin-cost-bounded`); part (b) still OPEN, Dev 3's.** (a) ~~`apps/api/app/modules/admin/router.py` materialised **every** `lesson_jobs` row joined to `lessons` for the reporting period and grouped in Python, because PostgREST has no server-side GROUP BY — fine at 23 lessons, and it was Sprint 3's own S3-4 ("Admin panel: job status, cost tracking") that made it real.~~ (b) `apps/api/app/modules/analytics/service.py:54` selects every `sessions.session_id` for a user with no bound, growing without limit as a student uses the product — still open, the shape Scale Contract Q4 names: fine in testing, wrong in production. | Med | (a) **Fixed:** added `_COST_REPORT_ROW_LIMIT = 10_000` and `.limit()` on the query, plus a new `CostReport.truncated: bool` field set `True` only when the fetch hits that ceiling exactly (a real signal, not a guess) — an explicit surfaced degradation per this project's binding "silent truncation is never acceptable" rule, especially load-bearing here since silently dropping rows would UNDER-report real spend. The `admin/router.py` entry removed from `_KNOWN_UNBOUNDED` — an allow-listed query that is now actually bounded is the "matches existing accepted pattern" ratchet binding rule 6 names, not a real fix. (b) **Still enumerated, not silenced** — `_KNOWN_UNBOUNDED`'s `analytics/service.py` entry unchanged. **Owner: (b) Dev 3, trigger = the first user with >1,000 sessions.** | (a) 3 new tests in `test_admin_router.py`: the query carries `.limit()`; `truncated=False` under the limit; `truncated=True` exactly at the limit. `test_unbounded_queries.py` re-verified green with the allow-list entry removed (10/10). RED-GREEN verified via the Edit tool. Full repo-wide regression: 54 failed/2102 passed/85 skipped before, same failed set/2105 passed after — zero new failures. (b) `tests/unit/test_unbounded_queries.py` — mutation-checked, unchanged. |
| **D60** | **S3-07 (Notifications UI) has three missing infrastructure pieces: no `PATCH /api/users/notifications` endpoint, no `user_notification_preferences` DB table, and a mock-only frontend.** (1) A repo-wide grep for `@router.patch` in the `users` module returns zero results — the module itself does not exist (`apps/api/app/modules/` has no `users/` directory). The endpoint was listed as an S3-07 deliverable but was never scoped to a developer. (2) A search of all nine migration files under `supabase/migrations/` finds no `user_notification_preferences` table. Dev 3's Story 3-33 documents the expected schema for Dev 1's reference. (3) Dev 2's notification preferences state is backed by an in-memory mock; it cannot survive a page reload and has no backend write path. This is the shape of D29 and D18: an interface everyone references but nobody built. | Med | **Dev 3 complete (Story 3-33, 2026-08-06).** **Dev 1 complete (migration 20260806000000, 2026-08-06).** **Dev 4 complete (Story 4-23, 2026-08-06): `PATCH /api/auth/notifications` in `apps/api/app/modules/auth/router.py`, JWT-guarded, read-merge-upsert pattern, 6-layer adversarial review applied, 16 tests green.** Remaining: Dev 2 — wire frontend to the real endpoint and remove the in-memory mock. **Trigger: the first sprint that adds email delivery to the assessment module — Dev 3's read-helper will silently opt everyone back in until Dev 2 closes this.** | Dev 3: `notification_prefs.py` + 12 tests ✅. Dev 1: migration 20260806000000 ✅. Dev 4: `auth/router.py` `patch_notifications` + `apps/api/tests/test_notifications_endpoint.py` — 16 tests ✅. Dev 2: frontend wiring ⏳. |
| ~~D61~~ | **CLOSED same-day (fixed + guarded). PR #126 (beta-access allowlist) gated `POST /lessons` behind `require_approved_user`, verified only against its own branch's test suite — which predates book-scale's `test_generate_lesson_endpoint.py` entirely (that branch forked from an older `main`). Merging into current `main` broke 9 of that file's tests** (`test_upload_tier_422_names_the_path_read_off_the_registered_route`, both parametrizations of `test_source_pdf_path_reconstructs_the_real_storage_key` / `test_upload_sanitises_the_filename_before_it_reaches_the_storage_key`), all 403ing with "This account does not have access yet" — `app.dependency_overrides` only patched `get_current_user`, and `require_approved_user`'s own internal `Depends(get_current_user)` resolves through that same override, but the allowlist itself was still empty by default, so the fail-closed design correctly rejected the fake test user. **Exactly the failure mode binding rule 1 names: verified against "touched files" (the PR's own isolated branch), not CI scope (current `main`'s full test surface).** | Med (process) | **FIXED same-day** — both call sites (`test_upload_tier_422_names_the_path_read_off_the_registered_route`, the shared `_upload_and_capture` helper) now also override `require_approved_user` alongside `get_current_user`, matching the auth shape these tests already simulate. Full suite re-run: 23 failed (all pre-existing, unrelated: D40's dna_growth leak ×19, D58's eval_runner, and 3 pubsub tests needing a local Redis this environment doesn't have) / 1755 passed — the 9 new failures are gone, no others appeared. | `tests/unit/test_generate_lesson_endpoint.py` — same 81 tests, all green; would redden immediately if the override were removed since the route's dependency chain hasn't changed, only the test's simulated auth |
| ~~D62~~ | **CLOSED 2026-08-11 (Story 3-35, branch `sprint3/s3-35-env-config-fixes`, fixed together with D31 as planned).** `.env.example:41` said `LANGFUSE_HOST=http://localhost:3010`, implying Langfuse is self-hosted. It is not — `config.py:87-88` defaults to `https://cloud.langfuse.com` and the live `.env` uses Cloud. **This directly caused an architecture-review question** ("is Langfuse still a 3rd service?"), i.e. the wrong template value propagated into someone else's mental model of the deployment topology. **Renumbered from D38, then D61, on repeated 2026-08-06 merges** — this ADR-001 branch (opened 2026-07-30) first allocated D38, which collided with `main`'s own independent D38 (book-scale migration verification, closed 2026-08-03); renumbered to D61, which then collided with a same-day `main` commit fixing a live test regression (also D61); this is the third id this single defect has carried without ever being wrong about anything — a live demonstration of why this file has no allocator. | Low | **Fixed.** Template corrected to `https://cloud.langfuse.com`. | `apps/api/tests/test_env_example_consistency.py::test_env_example_matches_settings_defaults_or_is_a_documented_exception` — general-purpose guard: every `.env.example` key with a corresponding `Settings` default must match it, or be an explicitly documented exception (`REDIS_URL` is the one whitelisted case, a real Railway-format example rather than the localhost dev default). RED-confirmed (`LANGFUSE_HOST` was the only mismatch found), GREEN after. |

**Note (the "Sprint 3 CES v2 audit" section below independently reused D61 and D62 for two
different, unrelated CES defects, detected 2026-08-12).** The D61 above (this row's neighbour,
beta-access allowlist test regression, closed) is **not** the same D61 as §"D61 — `session_start_ts`
missing silently disables fatigue for entire session" further down this file — that is Dev 4's,
still open. Likewise this D62 (`LANGFUSE_HOST`, closed) is **not** the same D62 as §"D62 —
`compute_ces_from_session_aggregates` is dead code" further down — that is Dev 3's, still open.
Two more ids from the same audit round collide with entries already in this file; see the D63
note immediately below and the D64 row's note further down. Both entries above stay closed under
their existing numbers; the CES-audit section keeps D61–D65 too, per this file's convention of
reconciling by note rather than renumbering after the fact (see the ⚠️ fourth-occurrence banner
at the top of this file).
| ~~D63~~ | **CLOSED same-round 2026-08-11 (Story 3-36, branch `sprint3/s3-36-package-builder-defensive-fixes`) — found by D32's own `/bmad-code-review` (Scale & Load Hunter, independently confirmed in part by Edge Case Hunter and Blind Hunter), not by a separate audit.** D32's defensive-skip fix converts a malformed `slides`/`quiz_questions`/`glossary` entry from a crash into a silent `continue`. Two consequences neither D32's original scope nor its 3 new tests caught: **(a)** `_group_by_segment_id`'s callers never fed the "entries existed but were all malformed" signal into `degraded_segment_ids` — a segment whose quiz/jargon were all dropped is indistinguishable from one that legitimately has none (`Segment.quiz`/`Segment.jargon` have no `min_length`); **(b)** far more severe — a segment whose ONLY slide entry is malformed falls into the pre-existing zero-slides drop path (`graph.py:4165-4172`, predates this story) and is removed from `segments_out` entirely, but `metadata.total_segments` was `lesson_plan.get("total_segments", len(segments_out))` — and `lesson_plan` almost always HAS that key (frozen at planning time, before any segment can be dropped), so the fallback essentially never fired. The shipped, student-facing package could claim more segments than it contained — **the exact "reports success while being wrong" shape the book-scale 4%-of-the-book defect exists in this register to name, now at segment granularity.** The admin-only `package_builder_degraded.total_segments` (same node, a few lines below) already computed the real count correctly — the two counts diverged in one write. | High (scale-contract) | **Fixed in the same commit as D32.** `_group_by_segment_id` now returns `(grouped, fully_dropped_segment_ids)`; callers add `"quiz"`/`"jargon"` to `degraded_segment_ids` when a segment's entries were all dropped, and a new `dropped_due_to_malformed_slides` list feeds a new `package_builder_degraded.dropped_segment_ids` field (distinct from `segment_ids`: dropped-entirely vs shipped-degraded). `metadata.total_segments` now always reads `len(segments_out)` — the real, just-built count — never the stale planning-time value. | 3 new tests: `test_segment_with_only_slide_malformed_drops_segment_and_total_segments_matches_shipped` (RED-confirmed by reverting `graph.py` alone and re-running — real `2 == 1` assertion failure reproduced, not assumed), `test_all_quiz_entries_for_a_segment_malformed_is_degraded_not_silently_empty`, `test_all_jargon_entries_for_a_segment_malformed_is_degraded_not_silently_empty`. All 45 tests in `test_package_builder_node.py` green after. |

**Note (Dev 4's D63 is a DIFFERENT, unrelated defect** — Story 4-24 / PR #129's INTERVENING
one-way-trap fix, on `sprint4/s4-6-intervention-recovery`. Two independent teams allocated D63
for two different things before either saw the other's work; this row is Dev 1's D63
(package_builder), already closed and merged. Dev 4's D63 has since merged too — confirmed
present in `sprint3-master`'s `state_machine/graph.py` as of the 2026-08-13 `main` merge (atomic
Lua distraction guard + `intervention_deadline_at` timeout self-heal), so both this row and Dev
4's D63 are now closed under the same number, which happened to land without a numeric conflict
only because it was reconciled by note instead of renumbered.

**Update, 2026-08-13 — THIRD meaning found.** The "Sprint 3 CES v2 audit" section further down
this file (§"D63 — `_get_distraction_count` is dead code") independently allocated D63 a third
time, for yet another unrelated defect (Dev 3's, `dna_fusion.py` wiring gap). Same root cause as
above: the audit didn't re-read the highest id on `main` first (see the ⚠️ fourth-occurrence
banner at the top of this file). That section keeps its own D63 rather than being renumbered —
readers must use the surrounding section heading, not the bare id, to tell these three apart.)**

---

### OPEN — found by the 2026-08-11 six-layer review of PR #129 (Story 4-24 / D63)

| ID | Defect | Sev | Decision | Enforcement |
|----|--------|-----|----------|-------------|
| **D64** | **Confusion-type interventions (fired via `teachback_failed`) have no cap at all.** `intervening_node` (`apps/api/app/modules/tutor/state_machine/graph.py`) only tracks two of the three intervention types: `distraction` increments `tutor_distraction_count` (capped by `max_distraction_per_session`) and `fatigue` sets a once-only flag (`tutor_fatigue_fired`) — the `confusion` branch (reached via `teachback_failed`) has neither a counter nor a once-only guard. A student who fails teach-back repeatedly in one session can be routed into `INTERVENING` with `intervention_type="confusion"` an unbounded number of times. Named only in prose in Story 4-24's own Scale & Load §1 ("unbounded by count today — pre-existing, not introduced here") with no register ID — exactly the shape binding rule 5 forbids. Found by the Acceptance Auditor layer of the Story 4-24 / D63 six-layer review (2026-08-11), not by an incident. | Low–Med (pre-existing; not introduced by D63) | **Deferred, not fixed.** Distraction and fatigue are capped because CLAUDE.md §10 states those caps explicitly; §10 says nothing about a confusion/teach-back-failure cap, so this may be intentional (teach-back is explicitly never-gating per CLAUDE.md's "never gate lesson progress on teach-back score" rule) rather than an oversight — needs a product decision, not just a code fix. **Owner: Dev 4. Trigger: the first session observed with repeated teach-back failures in quick succession, or Sprint 4 hardening — whichever comes first.** | *(to add — a test proving `intervention_type="confusion"` fires unboundedly within one session, once the cap decision is made)* |

**Note (the "Sprint 3 CES v2 audit" section further down this file independently reused D64 for
a different, unrelated defect** — "Per-signal Redis history keys ... have no TTL"
(Dev 3/Dev 4's, detected 2026-08-12). This row (confusion-type interventions uncapped, Dev 4's,
still genuinely open) keeps its number; the audit section keeps its own D64 too, per this file's
reconcile-by-note convention (see the ⚠️ fourth-occurrence banner at the top of this file).
**Also found while reconciling, 2026-08-13:** the audit section's D64 fix is not actually
outstanding — `redis.expire(key, _CES_WINDOW_TTL)` is present for all three keys
(`behavioral_history`/`head_pose_history`/`blink_history`) in the current `tutor/service.py`
(each call tagged `# D64`), confirmed on `sprint3-master` post the 2026-08-13 merge of `main` and
`throwaway/lesson-planner-batch-fix`. That section's own "Status: OPEN" for its D64 appears
stale relative to code and should be reviewed by its owner (Dev 3/Dev 4) — left as OPEN here
rather than flipped unilaterally, since closing an entry is this file's convention for the named
owner to do.)**

---

### Found by the L1 pre-flight check (Story 3-41, 2026-08-12)

**Note (merge-time, 2026-08-12): id allocated as D67, not D64** — Dev4's Story 4-24 (PR #129)
independently claimed D64 on `main` for a different, unrelated defect (confusion-type
intervention cap, above) between when this branch forked and when it merged.

| ID | Defect | Sev | Decision | Enforcement |
|----|--------|-----|----------|-------------|
| ~~D67~~ | **CLOSED same-round 2026-08-12 (Story 3-41).** `settings.sarvam_voice_id` defaulted to `"meera"` (`config.py:67-69`, set in Story 2-8), which is **not a valid Sarvam Bulbul v2 speaker** — confirmed via a real, live call to `api.sarvam.ai/text-to-speech`: `400 invalid_request_error`, `"Speaker 'meera' is not recognized. Available speakers are: anushka, abhilash, manisha, vidya, arya, karun, hitesh, aditya, ritu, priya, neha, rahul, pooja, rohan, simran, kavya, amit, dev, ishita, shreya, ratan, varun, manan, sumit, roopa, kabir, aayan, shubh, ashutosh, advait, anand, tanya, tarun, sunny, mani, gokul, vijay, shruti, suhani, mohit, kavitha, rehan, soham, rupali"`. Every real TTS call through the primary provider would 400 on the first attempt, fall through the full retry budget (non-retryable per Sarvam's own `invalid_request_error` classification — this is a config defect, not a transient failure, so retrying it burns nothing but wall-clock), then degrade to the Azure fallback on every single lesson — silently paying Azure's rate for 100% of narration instead of Sarvam's, and never actually exercising the primary provider this project chose. Found during L1 (`docs/LESSON-DELIVERY-TRACKER.md`) pre-flight, where it would otherwise have first surfaced as a confusing "why is every lesson using the fallback TTS provider" during the sprint's real-money acceptance run. | High (silent, 100% of narration cost misrouted, first-real-run blocker) | Default changed to `"anushka"` — verified via a second live call (not assumed): `200 OK`, real audio returned (122,940 base64 chars, non-empty). Chosen for parity with the existing Azure fallback default (`azure_tts_voice = "en-IN-NeerjaNeural"`, an Indian-English voice) — `anushka` is one of Sarvam's Indian-English-appropriate speakers, matching this product's target market. **2026-08-12 L1 run decision: Azure TTS credentials remain unconfigured (`AZURE_TTS_KEY`/`AZURE_TTS_REGION` both empty) — proceeding Sarvam-only + browser-last-resort for this acceptance run, explicit product decision, not a silent gap. See L1's Observed result in `docs/LESSON-DELIVERY-TRACKER.md` for the real-world evidence (this morning's pre-fix run: 100% of segments fell through to `browser`, zero real audio, confirming this defect's real-world impact).** | `tests/unit/test_config_settings.py::test_sarvam_voice_id_default_is_a_documented_valid_speaker` — pins the default against the literal speaker list from Sarvam's own `400` response body, so a future Sarvam API change that drops `anushka` fails CI instead of silently degrading every lesson to the fallback provider again |

---

### Found by the Langfuse-skill self-audit (Story 3-40, 2026-08-12)

**Ids allocated as D68–D70, not D64–D66** — this branch's own numbering (D64–D66, chosen looking
ahead from D63 while still unmerged) collides with `main`'s REAL D64 (Story 4-24, confusion-type
intervention cap, above) and with D67 (Story 3-41, merged just before this branch). Renumbered at
merge time per this file's own collision rule — the third such renumbering event visible in this
file (see D62's own history above), each one a live demonstration of why this register has no
central allocator.

| ID | Defect | Sev | Decision | Enforcement |
|----|--------|-----|----------|-------------|
| ~~D68~~ | **CLOSED same-round 2026-08-12 (Story 3-40).** Tutor FSM tracing (`modules/tutor/state_machine/graph.py:_trace_dispatch`) had been silently no-op for every dispatch since the Langfuse 4.x upgrade — it called the client-level `.trace()` method, which 4.x removed, and the resulting exception was swallowed at **DEBUG** level (this project's own convention elsewhere is WARNING for a swallowed observability failure, precisely so it stays visible — this site didn't follow it). No sprint's review caught it because nothing asserted the tutor FSM actually produced Langfuse traffic; every existing test mocked `get_langfuse()`. A **second**, distinct defect was found fixing the first: the initial fix (still pre-Story-3-40) reused `deterministic_trace_context(langfuse, session_id)`, copying the pipeline's `lesson_id` pattern onto a session that can run 60+ minutes with dozens of dispatches — forcing the whole tutor session into one ever-growing trace, the opposite of Langfuse's own documented "one trace per turn" guidance (best-practices.md, fetched fresh). | Med (observability gap, not user-facing) | Switched to `create_event(...)` (this pinned SDK's real event API — confirmed via `inspect.signature`, not docs, since the docs describe a newer version's capability this one lacks). Session semantics fixed: each dispatch is now its own trace, grouped via `propagate_attributes(session_id=session_id)` — the SDK's only documented mechanism for the first-class `session_id` attribute, verified against the real pinned signature. | `tests/unit/test_langfuse_sdk_contract.py::test_client_has_create_event_with_provider_kwargs` / `test_propagate_attributes_accepts_session_id` pin the pinned SDK's real signatures; `test_tutor_state_machine_source_has_no_v2_calls_and_uses_create_event` source-scans `state_machine/graph.py` itself (docstrings/comments stripped first, so it can't false-positive on this entry's own prose) for a real `.trace(` call vs. real `create_event(`/`propagate_attributes(` usage — RED-verified by temporarily reintroducing `langfuse.trace(...)` and confirming the test fails, then restoring. **Round 2 correction (2026-08-12, independent adversarial review):** this row previously claimed the guard existed when the referenced test file had none of these tests — a real overclaim, caught before merge, not after. `tests/unit/test_langfuse_core.py` separately covers `deterministic_trace_context`/`safe_trace` behaviorally (8 new tests) and fixed one pre-existing broken test (`test_get_langfuse_uses_settings` didn't know about the new `environment` kwarg — a second real gap this same review round surfaced). Two real (non-mocked) Langfuse Cloud traces fetched back and inspected post-fix (Story 3-40 AC-4) |
| D69 | **PARTIALLY CLOSED 2026-08-18 by D123 (node-level span half only) — see D123.** No span/parent hierarchy — every provider call across an 11-node pipeline run lands as a flat sibling directly under its trace root, not nested under a per-node or per-phase span. best-practices.md is explicit this is a real gap ("a tool call should nest under the agent/span that orchestrates the step... instead of leaving tool calls dangling at the trace root"). | Low (debuggability, not correctness or cost) | **DECISION 2026-08-12: not fixed in Story 3-40.** Needs a `parent_span_id` threaded through LangGraph state from graph-invocation down into every provider constructor — a real architecture change (state schema + every call site), not a tracing-call edit; Phase 1's `Send()`-based fan-out means there is no shared call stack to propagate an "active span" through implicitly. **D123 (2026-08-18) closes the node-level half** — every node now emits its own span sharing the pipeline's deterministic `lesson_id` trace_id, giving node-level timing/grouping in the Langfuse UI. **Still open, residual scope:** provider calls made INSIDE each node still land as trace-root siblings, not nested under that node's span — the `parent_span_id` threading described above is still needed for that, and is still a separately-scoped, larger change. **Trigger: a dedicated follow-up story**, scoped to the state-threading change alone. | DISCIPLINE — noted in Story 3-40's Dev Notes; `test_traced_node_*` (D123, below) pins the node-span-shares-trace-id behavior; nothing yet pins the still-open provider-nesting gap |
| D70 | **No `user_id` attribution on any trace.** Only `lesson_id` is threaded into providers; no provider receives which student triggered a lesson. best-practices.md recommends `user_id` for per-user cost/performance views — CLAUDE.md's own analytics/Learner-DNA ambitions depend on exactly this kind of per-student breakdown. | Low (product-analytics gap, not correctness) | **DECISION 2026-08-12: not fixed in Story 3-40.** Same threading-scope issue as D69 — `user_id` would need to flow through the same state path a `parent_span_id` fix would add, so the two are natural to fix together. **Trigger: same follow-up story as D69**, or the first time per-student Langfuse cost breakdown is actually requested. | DISCIPLINE |

---

### Found by `sprint3-master`, merged into `main` 2026-08-13

**Ids allocated as D74–D76, not D63–D65** — `sprint3-master`'s own numbering (allocated while
unmerged) collides with `main`'s real D63 (two independent defects already sharing that number,
see the note above) and D64 (confusion-type intervention cap). Renumbered at merge time per this
file's own collision rule — the same shape as the D62/D68–D70 renumbering events above, and
exactly the situation the banner at the top of this file exists to catch. `sprint3-master`'s own
D62 (Langfuse Cloud vs. self-hosted `.env.example` template) is the **same defect**, independently
fixed identically on both branches (Story 3-35 on `main`, an equivalent fix on `sprint3-master`) —
kept once, under `main`'s existing D62 entry above; not duplicated here.

| ID | Defect | Sev | Decision | Enforcement |
|----|--------|-----|----------|-------------|
| ~~D74~~ | **CLOSED 2026-08-05 (fixed + guarded; renumbered to D74 on the 2026-08-13 merge into `main` — was D63 on `sprint3-master`, previously renumbered from D58, then D60, on repeated 2026-08-06 merges** — first collided with `main`'s independent D58 (eval harness); renumbered to D60, which then collided with `main`'s independent D60 (S3-07 notifications gap) on this same-day merge into `sprint3-master`. Fourth id-collision instance recorded in this file, same underlying cause each time: a long-lived branch allocating ids without re-reading `main`'s latest first. `extract_text_only`'s TOC parsing crashed on any real PDF with a bookmark. `apps/api/app/modules/content/pipeline/nodes/extract_subprocess.py:780,786` read `item.page_index`/`item.title` as attributes on a `pypdfium2.PdfBookmark`, but that class exposes neither — the page index lives on the `PdfDest` returned by `get_dest()` (itself possibly `None`), and the title is read via `get_title()`. Every local test fixture (`short.pdf`, the eval corpus) has **zero bookmarks**, so `get_toc()` always returned `[]` and this line never executed in CI or any dev run. Found live: a real 2,000+-page textbook (`d2l.pdf`) uploaded through the UI hit it on the first real book with an outline, and `book_ingest_job` failed every ingestion with `AttributeError: 'PdfBookmark' object has no attribute 'page_index'` — "We couldn't read this PDF" in the UI for a perfectly valid file. | **High** (100% of book_ingest for any PDF with an outline) | **FIXED 2026-08-05** — `item.get_dest()` then `.get_index()` (both `None`-checked) for the page index, `item.get_title()` for the title. `.level` is a real attribute and was already correct. | `tests/unit/test_extract_subprocess.py::TestExtractTextOnlyToc` — 2 tests with a `_FakeBookmark`/`_FakeDest` shaped to the REAL pypdfium2 API (no `.page_index`/`.title` attributes at all), so a regression back to attribute access fails with the same `AttributeError` production raised, rather than silently passing the way a loose `MagicMock` would — which is exactly how this shipped untested the first time |
| ~~D75~~ | **CLOSED 2026-08-05 (fixed + guarded; renumbered to D75 on the 2026-08-13 merge into `main` — was D64 on `sprint3-master`, previously renumbered from D59, then D61, on repeated 2026-08-06 merges** — same collision chain as D74/D58/D60 above; `main` independently allocated both D59 (unbounded queries) and, later, D61 (test regression fix) the same day. The rate limiter's per-user key silently stopped keying by user for any Supabase project using asymmetric "JWT Signing Keys" — the exact D52 bucket-sharing bug, reopened by a later, unrelated change. `dependencies.get_current_user` (`apps/api/app/dependencies.py:83-100`) branches on the token's `alg` header: `HS256` verified against the shared secret, `ES256`/`RS256` verified via JWKS. `_get_user_key` (`apps/api/app/core/rate_limit.py`) kept its own **separate, independent** decode call hardcoded to `algorithms=["HS256"]` only — never updated when the JWKS branch was added — so any ES256 token raised `InvalidAlgorithmError`, was swallowed by the bare `except`, and fell back to `get_remote_address`, sharing one bucket across every authenticated user again. Found live in the same session as D74, via the real worker's log output. | High (same severity class as D52) | **FIXED 2026-08-05** — `_get_user_key` now branches on `alg` exactly like `dependencies.get_current_user`, reusing that module's `_get_jwks_client` rather than a third, independent JWKS client. **Do not let this drift a third time**: the two decoders are duplicated by design (rate_limit.py cannot import from a route-level dependency without circularity) — any future change to `get_current_user`'s algorithm handling must be mirrored here by hand. | `tests/unit/test_rate_limit_key.py::test_an_es256_token_keys_the_bucket_by_user_not_ip` — a real ES256 token signed with a generated EC key, JWKS client mocked at `app.dependencies._get_jwks_client` |
| ~~D76~~ | **CLOSED same-day (fixed + guarded; renumbered to D76 on the 2026-08-13 merge into `main` — was D65 on `sprint3-master`). Same class as D61, different file.** Merging PR #126's frontend beta-access gate (`proxy.ts`) into `sprint3-master` broke `proxy.test.ts`'s `'does not gate a sibling route sharing the /books prefix, e.g. /bookstore'` test — that test's mocked user was `{ id: 'u1' }` with no `email` field, written before the beta gate existed (it only needed to exercise the onboarding-gate's exact-segment matching). The new gate checks `getApprovedEmails().has((user.email ?? "").toLowerCase())`, so a missing email always fails the allowlist check regardless of the shared `beforeEach`'s `vi.stubEnv('APPROVED_EMAILS', 'u1@example.com')` — every one of the other ~11 tests in the file already sets `email: 'u1@example.com'` on its mock user for exactly this reason; this one was the sole holdout. | Low (test-only) | **FIXED same-day** — added the missing `email: 'u1@example.com'` to the mock user, matching every other test in the file. 837/837 web tests passing, `tsc`/`eslint` clean. | `src/__tests__/proxy.test.ts` — same test, now green; would redden immediately if the email field were removed again |

---

### OPEN — accepted, with a named trigger

Not "documented limitations". Each carries an explicit condition that reopens it.

| ID | Defect | Sev | Decision | Enforcement |
|----|--------|-----|----------|-------------|
| D66 | **`MODEL_ASSET_URL` (`apps/web/src/hooks/useAttentionMonitor.ts`) points at a floating `float16/latest/face_landmarker.task` tag, not a pinned MediaPipe model version.** Previously only documented as `DEFER-012` in `docs/deferred-work.md`, with no register ID — exactly the shape binding rule 5 exists to catch: a comment pointing at a doc, not an ID. **First allocated as D63, colliding with the pre-existing closed D63 below** (this story's PR targets `sprint3-master`, not `main` — the re-check was mistakenly performed against `main`'s max of D62 instead of `sprint3-master`'s actual max of D65). **Corrected 2026-08-11 in code review to D66**, re-checked against `sprint3-master`. | Low | **Not fixed — deliberately deferred.** Pinning requires confirming a real, currently-valid versioned MediaPipe model URL against official docs; not safely guessable without live verification, and the wrong guess (a nonexistent pinned URL) is worse than the current floating tag. **Owner: Dev 2. Trigger: the first live browser verification of L6 (attention capture) — pin the version actually confirmed working at that time.** | DISCIPLINE — comment at `useAttentionMonitor.ts`'s `MODEL_ASSET_URL` site, cross-referencing this id |
| D67 | **`GET /api/media/signed-url` (`apps/api/app/modules/media/router.py`) has no rate limiting.** Previously "DORMANT... zero callers" (its own docstring); Story 2-45 gives it its first real, unattended, automatic caller (a client-side per-asset signed-URL re-sign, one attempt per asset, but with no aggregate cap across tabs/users/the shared 8-hour `_EMBEDDED_MEDIA_EXPIRY_S` expiry boundary). Unlike the two other rate-limited routes in `content/router.py` (`@limiter.limit("5/minute", ...)` / `"3/minute;20/hour"`), this endpoint has no `@limiter.limit` decorator at all. Found by the Scale & Load Hunter layer during Story 2-45's review — out of scope for that story (frontend-only, no `apps/api` changes). | Med | **Deferred, not fixed.** Requires an `apps/api` change (add a `@limiter.limit` decorator matching the existing pattern in `content/router.py`), which is Dev 1's module. **Owner: Dev 1. Trigger: before real students are resuming lessons past the 8-hour signed-URL expiry at any meaningful concurrency** — i.e. before or during Sprint 3/4 load testing (`docs/SCALE-CONTRACT.md`). | *(to add — a test asserting the route is rate-limited, mirroring the existing `content/router.py` rate-limit tests)* |
| D21 | Embed truncation assumes ~4 chars/token. Measured `cl100k_base`: English 6.0, **Hindi 1.06, Tamil 0.71** | Deferred | **DECISION 2026-07-29: English-only for now.** Fix is one line and already specified (plan 6.3): `enc.decode(enc.encode(text)[:cap])`. **Trigger: the first Indic-language lesson.** | DISCIPLINE — comment at `graph.py` truncation site |
| D28 | **`detect_headings` ranks a chapter BELOW its own subsections.** `candidates` is keyed by text and every writer is guarded by `if text not in candidates`, so the **font strategy always beats the regex strategies that run after it** — an explicit `Chapter N:` prefix loses to a relative font-size band. Visible in the golden baseline: `Chapter 1` at `level: "topic"`, its own `1.1`/`1.2` at `"section"`, while `_LEVEL_RANK = {chapter: 0, section: 1, topic: 2}`. | Med | **DECISION 2026-07-29: pinned, NOT fixed.** Changing precedence is a detection *behaviour* change, and Story 2-34's entire premise is that removing an inert LLM call is behaviour-**preserving**. **Trigger: the Sprint 3 docling migration**, per the decision to park structure detection. | `test_font_strategy_wins_over_the_chapter_regex_inverting_the_hierarchy` pins the WRONG behaviour with re-capture instructions — the fix cannot land silently |
| D24 | **CI's new test steps land ADVISORY (`continue-on-error: true`), not gating.** `pytest tests` surfaces 22 pre-existing failures (Dev 3: 19, Dev 4: 3). Gating it on day one turns `main` red for all four developers over failures this PR did not introduce. **Applies to the api job only** — `pnpm test` was measured green (D25) and gates from day one. | Med | **DECISION 2026-07-29: land advisory, ratchet later.** `tests/unit` + `tests/integration` **do** gate (and `-x` is gone, so CI now enumerates rather than stops at one). **Trigger to drop `continue-on-error`:** the 22 reach zero. Until then the number is *visible*, which it has never been. **Concrete instance, 2026-07-29:** the D23 fix (PR #113) broke `test_lesson_ready_pubsub.py`, which lives in root `tests/` and asserted the pre-D23 contract. Dev 1 measured "no regression" against the GATING scope only and merged it. The advisory step would have shown 23 instead of 22 — visible, but not blocking. This is the cost of the compromise, paid within the hour, and it is the strongest argument for the trigger. | DISCIPLINE — the trigger is in a comment at both step definitions in `.github/workflows/ci.yml` |
| D26 | **CI has failed on 60 of its last 60 runs — zero successes — and merges proceeded anyway.** The API job dies at `ruff check .` (31 errors: Dev 3 22, Dev 4 9), which is *before* any test step, so "CI skipped the root `tests/` directory" understates it: **CI never reached a test step at all.** The web job died even earlier (D25). | SELF/BW | A red gate that everyone routes around is worse than no gate — it trains the team to ignore it. Gating scope must be a set that is green *today* (`tests/unit` + `tests/integration`: 743 pass) and grows by ratchet. | **PARTIALLY RESOLVED 2026-07-29, run 30470590163.** The **web job passed completely for the first time in the repo's history** — install, lint, type-check, 506 tests, and `build`. The API job now clears `ruff check` and `ruff format --check` (Dev 4 #a1ebbbe, Dev 3 #115, Dev 1 #117) and dies at the **next** gate, `mypy app`: 24 errors — Dev 3 19, Dev 4 5. **That is now the single thing between this repo and a fully green CI.** Each gate cleared reveals the next; the count was never one number |

---

## CLOSED — Sprint 3 (2026-08-12)

**A fifth real ID collision, found on merge (2026-08-14):** this section's own D72 (S3-42, CES
breakdown hardcoded zeros, closed 2026-08-12) collided with a second, unrelated D72 registered
independently on a parallel branch (Story 3-55, stale "TransformED" brand, closed 2026-08-13) —
both closed, both real, just the same number. Renumbered this section's D72 to **D108** across
the register, `test_s3_42_ces_breakdown_accuracy.py`, `test_session_report_endpoint.py`,
`stories/3-42-ces-breakdown-accuracy.md`, and one line of `dev3-assessment-tracker.md` — the
Story 3-55 D72 (stale brand) below is untouched, since it's already cross-referenced by this
session's own earlier renumbering work. Also note: `D1` (informal, pre-register marker for the
Story 2-0 "signed URLs only" decision, referenced in `storage.py`/`main.py`) is reused by
`assessment/ces.py` and `tutor/service.py` for an unrelated CES redistribution note — not fixed
here since `D1` was never a formal register row and both usages are comment-only, but flagged for
awareness.

| ID | Defect | Class | Decision | Enforcement |
|----|--------|-------|----------|-------------|
| ~~D108~~ | **[Renumbered from D72 on merge (2026-08-14) — collided with the OTHER D72 below (Story 3-55, stale "TransformED" brand), independently allocated on a parallel branch that merged first. Per the collision rule, the more-recently-merged/less-cross-referenced set was renumbered — this one.] CLOSED 2026-08-12 (S3-42, branch `sprint3/s3-42-ces-breakdown-accuracy`).** `get_session_report` in `apps/api/app/modules/assessment/service.py` hardcoded `"behavioral": 0.0`, `"head_pose": 0.0`, `"blink": 0.0` in the `ces_breakdown` dict (comment: "Sprint 2: behavioral/head_pose/blink contributions deferred to Phase 3"). This meant the reported `ces_breakdown` was always wrong for any session that had attention signals — its five values did not sum to `ces_score`, and behavioral/head_pose/blink were always zero regardless of actual attention quality. Root cause: `process_attention_signal` stored only the composite CES value in Redis (`session:{session_id}:ces_history`); the individual signal components were never persisted, so `get_session_report` had nothing to read. | BW | Two coordinated changes: (1) `tutor/service.py:process_attention_signal` now `lpush`es the three signal components to separate Redis history lists (`session:{session_id}:behavioral_history`, `head_pose_history`, `blink_history`), trimmed to `_CES_HISTORY_MAX=10` and TTL'd identically to `ces_history`. (2) `assessment/service.py:get_session_report` now accepts `redis: Any = None`, reads those three history lists via an `_signal_avg` closure, and computes weighted contributions using `settings.ces_weight_*`. Router call site updated to pass `redis=get_redis()`. The existing Sprint-2 test asserting hardcoded-zero behavior was updated to assert zero-when-no-redis-data (correct for the right reason). | `tests/test_s3_42_ces_breakdown_accuracy.py::test_ces_breakdown_no_hardcoded_zero_for_behavioral` — source-inspection CI guard that fails if `"behavioral": 0.0` is re-introduced into `get_session_report` |

---

## Part 3 — Process defects

| ID | Defect | Decision | Enforcement |
|----|--------|----------|-------------|
| ~~P1~~ | **CLOSED 2026-07-29.** CI skipped the root `tests/` dir and used `-x`. | Now `pytest tests -q`. **Clear-eyed: this would not have caught one of the 17** — those tests are mock-shaped too. Observability only. | `.github/workflows/ci.yml` |
| ~~P2~~ | **CLOSED 2026-07-29.** The web job never ran tests; 41 story lines claimed "488 tests green" that no machine had verified. | `pnpm test` added before `build`. | `.github/workflows/ci.yml` |
| P3 | `docs/deferred-work.md` is 6 lines with **0 items**, against 131 defer markers | Superseded by this file. Deferral is only valid with an ID, an owner and a **trigger condition**. | DISCIPLINE + review checklist |
| P4 | `CLAUDE.md` mandates 5 review layers; the skill ships 3. Every PR to date shipped 3 under a document saying REJECT under 5. | Add Story Quality + Process Integrity to the skill, or amend CLAUDE.md to match reality. **Do not leave the two disagreeing.** | *(to add)* |
| P5 | Every AC said "no new findings on any **touched** file"; CI checks repo-wide. Both were satisfiable while 78 repo-wide errors accumulated. | **ACs must state repo-wide numbers.** Already applied from Story 2-33 onward. | Review checklist |
| P6 | RC-1. The anti-deadlock mocking rule has no expiry clause. | See Part 4. | — |

---

## Part 4 — Binding decisions going forward

These are the rules. Each says how it is enforced.

> **Scale has its own contract: `docs/SCALE-CONTRACT.md`** (established 2026-08-05). It states
> the six questions every story's `## Scale & Load` section must answer, and its Enforcement
> table names **this register — BD-9 below** as the mechanism for any scale limitation we ship
> knowingly. Read it before opening a story; it is binding on every story, PR and review.

**BD-1 — Verification scope must equal CI scope.**
An AC may not scope a gate to touched files. State the repo-wide number and `main`'s number.
*Enforcement: review checklist. `DISCIPLINE` until a lint on story files exists.*

**BD-2 — A test may not assert only on a mock it constructed.**
Every test asserts an observable outcome, or is marked `# MOCK-CONTRACT:` naming the
real-dependency test that covers the same path. This is RC-1, which explains 12 of 17.
*Enforcement: `DISCIPLINE` today. Becomes machine-checkable once BD-3 lands.*

**BD-3 — One CI job runs against real dependencies.** *(highest leverage — do this first)*
`postgres:16 + pgvector` with all 9 migrations applied, no Supabase fake, and provider tests
raising genuine `openai` / `httpx` / `redis` exception objects.
It is the only change that attacks RC-1 and RC-2 at once. It kills D9's whole class (43
sites), D19, D20, and would have caught D3, D4, D5, D6, D8.
*Enforcement: the job itself.*

**BD-4 — Any `except SomeLib.Error` requires an executable premise assertion.**
D3, D6, D19 and D20 are all the same defect: we assumed a type hierarchy. `test_retry.py`
already models this with `test_openai_exceptions_are_not_httpx_derived`.
*Enforcement: named test per library.*

**BD-5 — A documented limitation is not an accepted one.**
A `KNOWN LIMITATION` / `TODO` / `FIXME` comment must carry a `D-nn` register ID. A comment
without an ID is a defect, not a decision. D11 sat behind such a comment for multiple
sprints and survived every review *because it was known*.
*Enforcement: grep gate in CI — `DISCIPLINE` until written.*

**BD-6 — Golden fixtures pin their environment and name their capture commit.**
D13/D14. A baseline that moves with an env var is not a baseline, and one that freezes a
defect is worse than none.
*Enforcement: convention + review. `DISCIPLINE`.*

**BD-7 — "Matches existing accepted pattern" is not a justification.**
It is the ratchet that took `{**state, ...}` from 1 site to 18. If the pattern is wrong at
site 19, it was wrong at site 1; open a register entry.
*Enforcement: `DISCIPLINE` — review checklist.*

**BD-8 — The mocking rule gets an expiry.**
CLAUDE.md's *"each dev mocks the other's interface"* stays for scheduling, but every mocked
cross-dev contract needs one real integration test before Sprint 3 real-student launch. D18
is what happens without it: three developers, three green suites, one broken product.
*Enforcement: BD-3 + a per-contract checklist.*

**BD-9 — Scale is a stated constraint, not an assumption.**
*(This is the entry that `docs/SCALE-CONTRACT.md`'s Enforcement table and `CLAUDE.md`'s
Core Architectural Principle 8 both call **"binding rule 8"** — i.e. the eighth rule of
`CLAUDE.md`'s **### Binding rules** list, which mirrors this file and runs 1–7 today. In this
file's own sequence it is **BD-9**, because **BD-8 is already the mocking-expiry rule**. Two
numberings, one rule, stated here so nobody "fixes" the mismatch by renumbering BD-8 — that is
how D29/D30 and D57 happened.)*
Every story carries a `## Scale & Load` section answering the contract's six questions. A bare
`N/A` is a missing answer, not an answer, and **review checks the answers, not the presence of
the heading** — the section is only worth what the sixth review layer reads in it. Any scale
limitation shipped knowingly is a register entry with a `D-nn` ID, a **named owner** and a
**trigger** — exactly the bar BD-5 sets for every other documented limitation, and for the same
reason: **D58** (the eval harness writes a success-shaped result file when every PDF crashed)
was quoted verbatim in `docs/book-scale-phase-tracker.md:79` as the justification for the
tracker's entire GATE RULE and still **had no register ID** until 2026-08-04.

The failure this rule catches does not look like slowness — it looks like success.
`settings.structure_max_sections = 15` (`apps/api/app/config.py:332`) ×
`_get_section_body(..., max_chars=6000)` (`graph.py:1942`) is a ~90,000-character LLM-visible
window **regardless of input size**, so a 1,151-page book produced a lesson built from **4 %**
of itself: nothing errored, nothing warned above a `logger.warning` nobody reads
(`graph.py:1952-1960`), and the `$3.00/lesson` ceiling never fired because the failure was
*cheap*, not expensive (**D46**). Any AC phrased as "the cost ceiling protects us here" is
false for this whole class.

---

## D61 — `session_start_ts` missing silently disables fatigue for entire session

**Status:** OPEN · **Owner:** Dev 4 · **Detected:** 2026-08-12 (Sprint 3 CES v2 audit)

If Redis is unavailable when `_init_session_state` runs (WS connect), the
`session:{id}:session_start_ts` key is never written. `process_attention_signal` checks
`if session_start_ts_raw is not None` — a missing key silently skips the entire fatigue
detection block for the rest of the session. No error is raised; CES reports look normal.

**Fixed partially:** A `logger.warning` is now emitted when the key is absent during
TEACHING-state processing (`tutor/service.py`). Full fix requires falling back to
`sessions.started_at` from the DB when the Redis key is absent.

**Enforcement:** `logger.warning` output is not machine-checked — DISCIPLINE.
Register **D-nn** when the DB-fallback is implemented and tested.

---

## D62 — `compute_ces_from_session_aggregates` is dead code; `_finalize_session` has divergent inline implementation

**Status:** OPEN · **Owner:** Dev 3 · **Detected:** 2026-08-12 (Sprint 3 CES v2 audit)

`assessment/service.py:compute_ces_from_session_aggregates` is defined (story S3-49) but
never called in production. `_finalize_session` in `graph.py` has its own inline
averaging that uses `lrange(0, 9)` and returns `0.0` for empty history (instead of `None`
for fewer than 5 windows). A student who ends their session with fewer than 5 CES windows
gets `ces_final = 0.0` — indistinguishable from zero engagement with no diagnostic signal.

**Fix:** Either wire `_finalize_session` to call `compute_ces_from_session_aggregates`
(handling the `None` → `0.0` mapping explicitly with a warning), or delete the dead
function and document the `0.0` artifact in the session report schema.

**Enforcement:** DISCIPLINE (no CI guard prevents the divergence from widening).

---

## D63 — `_get_distraction_count` is dead code; `dna_fusion.py` wiring was never completed

**Status:** OPEN · **Owner:** Dev 3 · **Detected:** 2026-08-12 (Sprint 3 CES v2 audit)

`assessment/service.py:_get_distraction_count` is defined (story S3-36 Task 3.2–3.3) but
`apps/api/app/modules/assessment/dna_fusion.py` contains no import or call to it. The
story specified it should be wired into `fuse_learner_dna()`. The function is tested in
isolation but the production call site was never connected.

**Enforcement:** DISCIPLINE (import graphs are not CI-checked).

---

## D64 — Per-signal Redis history keys (`behavioral_history`, `head_pose_history`, `blink_history`) have no TTL

**Status:** OPEN · **Owner:** Dev 3/Dev 4 · **Detected:** 2026-08-12 (Sprint 3 CES v2 audit)

`process_attention_signal` writes `behavioral_history`, `head_pose_history`, and
`blink_history` keys to Redis (S3-42) but sets no TTL. `ces_history` correctly receives
`expire(_CES_WINDOW_TTL)` at line 315 of `tutor/service.py`. The per-signal history keys
persist until Redis eviction. On Railway Redis with a 64 MB limit, accumulated orphaned
keys from sessions that never reached SESSION_END accumulate indefinitely.

**Fix:** Add `await redis.expire(f"session:{session_id}:{signal}_history", _CES_WINDOW_TTL)`
after each `ltrim` call in `tutor/service.py`, matching the `ces_history` pattern.

**Enforcement:** DISCIPLINE.

---

## D65 — Entire distraction trigger path (Scenarios 8, 10, 11, 12, 18) has zero unit test coverage

**Status:** PARTIALLY FIXED (S3-52) · **Owner:** Dev 4 · **Detected:** 2026-08-12 (Sprint 3 CES v2 audit)

The distraction trigger (`tutor/service.py:344–411`) — 2 consecutive low-CES windows →
`dispatch_event("distraction_detected")` — has no unit test in the CES v2 test suite. The
Lua atomic guard (`_can_intervene_distraction`), cooldown check, distraction cap, and D4
gap/stale-history check are all live code with zero test coverage. The same applies to the
"distraction blocks fatigue in same window" scenario (Scenario 12 in the coverage matrix).

**S3-52 fix (2026-08-12):** Added `tests/test_s3_52_ces_production_hardening.py` covering:
- D4 timestamp gap-check non-dispatch path (stale entries > 2×cadence → no dispatch)
- dispatch_event.assert_not_called() for QUIZZING and INTERVENING states with low CES
- per-signal lpush not called in QUIZZING state

**Remaining gap (Dev 4):** The positive distraction trigger path (TEACHING + 2 consecutive
low-CES + gap_ok → `_can_intervene_distraction` → dispatch) still has no unit test.
Write `tests/test_distraction_trigger.py` covering that positive path and the Lua guard behavior.

**Enforcement:** DISCIPLINE (remaining test file is absent, not a failing test).

---

## D109 — No durable per-session CES history exists beyond the last 10 Redis-capped windows

**Status:** OPEN · **Owner:** Dev 3 · **Detected:** 2026-08-13 (Story 2-46 / S3-05, attention
timeline chart pre-implementation analysis)

**Renumbered from D77 on the 2026-08-13 merge of `main` into `sprint3-master`.** `main`'s own
history independently allocated D77 to a different, already-closed defect (lesson_planner
per-batch echo retry, Story 3-44) — and, separately, `main` has at least three internal
collisions of its own on D74–D77 and D87 between different review passes on the same day,
none of which this merge attempts to renumber (too many already-shipped code comments and
docs reference those specific numbers on `main`'s side; see the reconciliation note in the
Scorecard section below). This entry's own footprint was small and fully under this branch's
control, so it was the one renumbered — to the next free id after this merge, D109.

`session:{id}:ces_history` is the only place any per-window CES value with a timestamp is
kept, and it is `ltrim`'d to the last `_CES_HISTORY_MAX=10` entries (`tutor/service.py`) —
by design, to bound Redis memory, matching the `BOUNDED` convention. At the default 5-second
cadence that is **the last ~50 seconds of the session**, for a session of any length. Nothing
else — no DB table, no JSONB column, no other Redis structure — persists the full sequence of
CES windows anywhere. A student who studies for 20 minutes has exactly the same 10 stored
windows available afterward as one who studied for 50 seconds.

This surfaced while scoping Story 2-46 (S3-05, `docs/dev2-sprint-tracker.md`)'s "area chart of
CES over session time" — the report endpoint (`GET /api/session/{id}/report`) can only ever
expose what Redis still has, i.e. a recency window, never the true full-session timeline the
story's acceptance criteria describe. Exposing the capped 10 entries through the API *as if*
they were the whole session would be the exact silent-truncation shape CLAUDE.md's Scale
Contract forbids — cheap and wrong, not loudly broken.

**Fix:** Persist every CES window durably as it's computed — e.g. append to a
`sessions.ces_timeline jsonb` column (mirroring how `ces_final` is written once at
`_finalize_session`, but incrementally per window) or a new `ces_windows` table
(`session_id`, `window_index`, `ces`, `created_at`) — so a full-session query is possible
without depending on Redis retention. Out of scope for Story 2-46, which instead exposes the
existing capped Redis data honestly labelled as a recency window, not a full-session view.

**Trigger:** the first support request or design review that expects the attention timeline
chart to show more than the last ~50 seconds of a session, or a future story that persists
CES windows for another reason (e.g. cross-session trend analysis) and can absorb this at the
same time.

**Enforcement:** DISCIPLINE — no test can fail for data that doesn't exist yet; the fix is a
new persistence path plus a test that a >10-window session still returns its earliest windows
after finalization.

---

Six open entries are this rule stated after the fact, and are the evidence for it —
**do not re-register them under new ids, cite them**: **D45** (check-then-insert on
`(chapter_id, tier)` with no UNIQUE constraint anywhere to fall back on — two concurrent
requests both insert and both bill), **D49** (`RATE_LIMIT_STORAGE_URL` defaults to `memory://`,
so every ceiling silently multiplies by replica count), **D50** (300-DPI renders and Storage
uploads bounded on *concurrency, not count*, and outside `cost_tracker`'s view entirely),
**D52** (limiter keyed by IP because a swallowed `InvalidAudienceError` dropped the `sub`
lookup — one caller locked out everyone behind the same egress IP), **D53** (nothing ages a
`generating` row out, so three of them lock a user out of all generation permanently), and
**D58**. Scope is part of the answer: a limit whose scope — per user, per instance, per
deployment — is unstated is a limit that is wrong on the second replica (D49) or the second
user (D52).
*Enforcement: `## Scale & Load` is a required section of the story template; a sixth mandatory
review layer (Scale & Load) in the review gate; and `tests/unit/test_unbounded_queries.py` — a
source scan failing on any request-path `.select()` carrying no `.limit()`, `.range()`,
`count=` or `# BOUNDED:` justification. **Verified 2026-08-05: that file does not exist under
`apps/api/tests/` or `tests/`.** Until it is written BD-9 is `DISCIPLINE`, and is counted as
such in the scorecard — per binding rule 7, a rule without a guard is not enforced, it is
remembered.*

### DECISION 2026-08-05 — scale becomes an explicit, enforced constraint on all future work

Sprints 1 and 2 and Learner Mode were built, reviewed and merged **green**. Every one of them
assumed a small PDF, one user, one instance. None of those assumptions was written down, so
none was ever checked — and the check that would have caught them (BD-3's real-dependency job,
the `$3.00` ceiling, the 5-agent gate) could not, because nothing was failing.

The cost of not writing them down, paid in full and recorded honestly:

| | |
|---|---|
| Unplanned work | The **entire book-scale sprint**, Phases 1–7, whose only purpose was to retrofit scale into shipped, green work |
| Commits | **~50** |
| Register entries created | **~14 (D41–D58)** — of which D45, D46, D49, D50, D52, D53 and D58 are scale defects proper |
| Delivered | A 1,000-page book that works. Nothing a user asked for. |
| Still open from it | D46 (silent truncation, DISCIPLINE), D50 (uncapped renders, no guard). **D53 closed 2026-08-13 (Story 3-48)** |

**The decision.** Scale is now a stated constraint on every story, not a property discovered
afterwards: `docs/SCALE-CONTRACT.md`'s six questions, BD-9 above, the required `## Scale & Load`
story section, and the sixth review layer. This is deliberately structural rather than advisory
— Part 1 of this file establishes by evidence that prose guidance does not hold here (Dev 1
wrote `DEV1-FIX-PLAN.md` and deviated from it four times in one day; every deviation was caught
by review, none by a machine), so a paragraph asking people to think about scale would have
already failed on the day it was written.

**The one-line test, from the contract:** *"What input makes this silently wrong rather than
loudly broken?"* If a story cannot answer it, the story is not finished. Every defect this
project has found — without exception — was something that reported success without being
checked.

| ~~D71~~ | **CLOSED 2026-08-13 (Story 3-55).** `process_onboarding()` Step 4 (`generate_onboarding_profile()`) had no `try/except`. Fix landed in Story 3-54 (`service.py:1166-1199`): wraps the call in `try/except Exception`, deletes the 20 orphaned `onboarding_responses` rows on failure, then raises `HTTPException(503)` so the router's existing cleanup fires and the Redis lock is released. Confirmed fixed: 7/7 `test_onboarding_llm_failure.py` tests pass; register entry closed in Story 3-55 after verification. | High → CLOSED | Fixed Story 3-54. Register entry closed Story 3-55. | `tests/test_onboarding_llm_failure.py` (7 tests — all pass). |
**A fourth real ID collision, found on merge (2026-08-14):** `sprint3/s3-55-learner-dna-production-gaps`
independently allocated **D92–D97** for six defects found by its own Story 3-55 review — landing
on exactly the range this register had just assigned, in the renumbering above, to six *different*
Dev 3 defects (originally D74–D80, renumbered to D92–D101 earlier the same day). Per the collision
rule: the incoming, more-recently-merged set was renumbered — **D92→D102, D93→D103, D94→D104,
D95→D105, D96→D106, D97→D107** — leaving this register's own D92–D101 untouched, across the
register, `dna_fusion.py`, `schemas.py`, `test_unbounded_queries.py`,
`test_session_create_schema.py`, `dev3-assessment-tracker.md`, and `stories/3-55-...md`.

| **D73** | **No TTL on Redis `onboarding_key` — process crash between SET NX and HTTPException cleanup leaves key permanently set.** `router.py:250`: `redis.set(onboarding_key, "1", nx=True)` has no `ex=` or `px=` TTL argument. The two deletion paths are `router.py:265` (on `HTTPException`) and `router.py:271` (on success). A process crash, SIGKILL, or unhandled non-`HTTPException` (e.g. `asyncio.CancelledError`, `MemoryError`) occurring after line 250 and before either deletion path executes leaves `onboarding_key` set permanently. All future submissions from that `user_id` receive 409 "already submitted" with no re-assessment flag set and no self-healing path. Surfaced by Scale & Load Hunter during Story 3-54 review. | Low (crash-path only; normal flow and HTTPException path correctly release the lock) | Pre-existing; not introduced by Story 3-54. **Fix:** Add `ex=86400` (24h TTL) to `redis.set(onboarding_key, ...)` as a safety net for crash cases, or wire a health-check job to detect orphaned keys. **Owner: Dev 4 (owns Redis buffer/lock patterns). Trigger: any real-user crash report of permanent onboarding 409.** | None currently — would need a test that simulates a crash between SET NX and the cleanup. |
| **D92** | **`_compute_dimension_scores` silently skips unrecognised `question_id` values, inflating remaining sub-dimension scores.** `service.py:1073-1075`: when `QUESTION_SUBDIMENSION_MAP.get(ans.question_id)` returns `None`, the response is silently dropped — no error, no warning. Example: a typo in the map (`c_1` instead of `c1`) drops one of three `pattern_recognition` questions; correct mean = 55.56, actual mean = 83.33 — 27.77-point inflation with badge awarded incorrectly. Pre-existing behaviour inherited from Story 3-18; introduced to the register during T18 Demo BMAD review (2026-08-13). Scale Contract Q2 applies: this is a fixed budget (20 responses mapped to 9 dims) meeting variable input (any `question_id` string) with silent truncation on mismatch. | Low (trigger requires a map typo or schema change, not normal usage) | **Deferred.** Fix requires a map-validation guard (raise on unknown `question_id`) and AC update across the onboarding test suite. **Owner: Dev 3. Trigger: any onboarding map edit or schema extension.** | No CI guard currently; would need a test with an invalid `question_id` that asserts an error is raised. |
| ~~D72~~ | **CLOSED 2026-08-13 (Story 3-55).** Stale "TransformED" brand in `DPDP_DISCLAIMER` and `ONBOARDING_PROFILE_SYSTEM_PROMPT`. Fix landed in Story 3-54: `prompts.py:120` now reads "HIE"; migration `20260813000000_learner_dna_rebrand.sql` backfills existing `learner_dna.profile_text` rows. Confirmed fixed: `test_dpdp_disclaimer_uses_hie`, `test_system_prompt_uses_hie`, `test_migration_sql_has_rebrand_update` all pass. Register entry closed in Story 3-55 after verification. | Med → CLOSED | Fixed Story 3-54. Register entry closed Story 3-55. | `tests/test_onboarding_llm_failure.py::test_dpdp_disclaimer_uses_hie` + `::test_system_prompt_uses_hie` + `::test_migration_sql_has_rebrand_update`. |
| **D93** | **`fuse_learner_dna` session_count is a Python-side read-modify-write — two concurrent ended-session events for the same user silently drop one session's full EMA contribution.** `dna_fusion.py` Step 3 reads `old_session_count = N` from Supabase. Step 5 upserts `session_count = N + 1` computed in Python. If two `fuse_learner_dna` calls for the same `user_id` execute concurrently (e.g. two sessions end within milliseconds, or ARQ retries overlap), both read `N`, both compute `N+1`, and the second upsert (`on_conflict="user_id"`) silently overwrites the first. Result: `session_count = N+1` (not `N+2`), and one session's quiz accuracy, teachback scores, and event counts never reach the Learner DNA profile. The profile appears updated (no error, no warning), but the student's profile is biased by the dropped session for all subsequent EMA computations. Surfaced by Scale & Load Hunter (Q6) during Demo T19 review 2026-08-13. | Low (requires concurrent requests for the same user — rare in normal use; concurrent ARQ retries are more likely) | **FIXED-GUARDED 2026-08-13 (`demo/defect-fixes-d74-d76-d77-d78`).** Migration `20260813000001_dna_session_count_atomic_increment.sql` creates `increment_learner_dna_session_count(uuid)` Postgres function that does `UPDATE ... SET session_count = session_count + 1`. `session_count` removed from `upsert_payload` in `dna_fusion.py` Step 5; RPC called after upsert in a non-fatal try/except. | `test_dna_fusion.py::test_async_session_count_incremented` — asserts `session_count` absent from upsert payload AND `supabase.rpc` called with `increment_learner_dna_session_count`. |
| **D94** | **`fuse_learner_dna` event aggregation path (lines 289–306) has zero integration test coverage with non-empty `event_rows`.** All 5 `fuse_learner_dna` integration tests across `test_dna_fusion.py` and `test_dna_fusion_real_session.py` pass `event_rows=[]` to the Supabase mock. The production code at lines 289–306 fetches `session_events` rows and counts them by `event_type`; this counting loop is exercised with an empty list only. A regression in the counting loop (e.g. wrong `event_type` key, missing default, off-by-one in the aggregation) would not be caught by any test. The `_compute_signals` unit tests (AC1, AC2, AC4, AC6) pass `event_counts` directly and bypass the DB-fetch path entirely. Surfaced by Scale & Load Hunter (Q4) during Demo T19 review 2026-08-13. | Low (test-coverage gap only — the production counting loop is correct; no live user impact) | Pre-existing since Story 3-25. **Fix:** Add one `fuse_learner_dna` integration test with `event_rows=[{"event_type": "jargon_hover"}, ...]` (non-empty) and verify the resulting `curiosity_index` EMA in the captured upsert payload. **Owner: Dev 3. Trigger: Demo T20 — closes this defect.** | `apps/api/tests/test_dna_fusion_event_aggregation.py` — 6 tests, all GREEN (Demo T20, 2026-08-13). |
| **D95** | **3 tests in `test_dna_fusion.py` and `test_dna_growth.py` use `asyncio.get_event_loop().run_until_complete()` which fails in Python 3.12 with `asyncio_mode=auto`.** Affected: `test_dna_fusion.py::test_positional_args_raise_type_error`, `test_dna_growth.py::test_positional_args_raise_type_error`, `test_dna_growth.py::test_record_dna_growth_inserts_9_rows_for_all_dims`. All three use the deprecated event-loop API removed in Python 3.12's strict asyncio mode. Pre-existing from Stories 3-25/3-27; surfaced by Demo T20 Process Integrity review (2026-08-13). | Low (test suite failure only; not a production runtime issue) | **FIXED-GUARDED 2026-08-13 (`demo/defect-fixes-d74-d76-d77-d78`).** All 18 affected `def test_*` functions in `test_dna_fusion.py` and `test_dna_growth.py` converted to `async def` + `await coro`. | `asyncio_mode=auto` in `pyproject.toml` enforces this: any sync `def` test that calls the deprecated event-loop API is rejected immediately by pytest-asyncio. 75/75 tests GREEN post-fix. |
| **D96** | **`session_events` SELECT in `dna_fusion.py` has no `.limit()`.** `supabase.table("session_events").select("event_type").eq("session_id", session_id).execute()` (line 292) fetches all rows for a session with no row cap. A misbehaving frontend event loop firing at 60 Hz over a 60-minute session produces ~216,000 rows. All are materialised into memory inside `asyncio.to_thread`; the thread pool thread blocks for the full transfer duration (~5–30 s); function returns HTTP 200 with correct dimensions (signal caps clamp output, not input). No error raised. Scale Contract Q4 finding; invisible to `test_unbounded_queries.py` CI guard (see D99, renumbered from D78 — see the register's collision note). Same pattern in `quiz_attempts` (line 266) and `teachback_attempts` (line 279) — both naturally bounded by lesson structure but lack `# BOUNDED:` comments. Surfaced by Scale & Load Hunter during Demo T20 review (2026-08-13). | Med (no data loss; latent thread-pool exhaustion under concurrent session-end calls at large event volume) | **FIXED-GUARDED 2026-08-13 (`demo/defect-fixes-d74-d76-d77-d78`).** `.limit(10_000)` added to session_events SELECT; `logger.error` emitted if cap is hit (surfaced degradation per Scale Contract Q2). `# BOUNDED:` comments added to quiz_attempts and teachback_attempts queries. All mock chains updated across 4 test files. | `test_unbounded_queries.py` — now scans `dna_fusion.py` (D99, renumbered from D78, fixed simultaneously). Session_events cap verified by mock chain in 75/75 GREEN tests. |
| **D97** | **`SessionCreate.lesson_id: str` has no `min_length` — an empty string passes Pydantic, reaches the Supabase `lessons` table (UUID-type PK), and returns HTTP 500 instead of 422.** `POST /sessions` with `{"lesson_id": ""}` passes Pydantic validation, reaches `create_session`, and executes `supabase.table("lessons").select(...).eq("lesson_id", "").maybe_single()`. PostgREST casts the empty string to a UUID column, raises `invalid_text_representation` — FastAPI's default handler converts it to HTTP 500. Surfaced by Scale & Load Hunter during Demo T26 review (2026-08-13). | Low | **FIXED-GUARDED 2026-08-13 (`demo/defect-fixes-d79-d80`).** `SessionCreate.lesson_id` changed to `Field(min_length=1)` in `schemas.py`. Empty string now returns 422 at the Pydantic layer before any DB call. | `tests/test_d79_d80_schema_guards.py::test_sessions_empty_lesson_id_returns_422` + `::test_sessions_null_lesson_id_returns_422` — both GREEN. |
| **D98** | **`TeachbackSubmission.response_text: str = Field(min_length=1)` counts characters, not content — a whitespace-only string (e.g. `"   "`) passes Pydantic, reaches `grade_teachback`, triggers a real GPT-4o-mini call, and returns HTTP 200 with a TeachbackResult scored on substantively empty content.** The failure is cheap, not expensive — the `$3.00/lesson` ceiling never fires; the student receives a silently biased `ces_contribution`. Surfaced by Scale & Load Hunter during Demo T26 review (2026-08-13). | Med | **FIXED-GUARDED 2026-08-13 (`demo/defect-fixes-d79-d80`).** `@field_validator("response_text")` added to `TeachbackSubmission` — strips and rejects whitespace-only content with 422. Covers `" "`, `"\t"`, `"\n"`, and mixed whitespace variants. | `tests/test_d79_d80_schema_guards.py::test_teachback_whitespace_only_response_text_returns_422` (5 whitespace variants) + `::test_teachback_single_char_response_text_accepted` (guards over-eager validation) — both GREEN. |
| **D99** | **`test_unbounded_queries.py` CI guard excludes `dna_fusion.py` and all non-`router.py`/`service.py` assessment sub-modules from its request-path scan.** `REQUEST_PATH_FILENAMES = ("router.py", "service.py")` at line 121 means any unbounded `.select()` in `dna_fusion.py`, `dna_growth.py`, `ces.py`, `ces_baseline.py`, `dna_profile.py` passes CI green. The positive-control test (lines 487–496) proves detection would work — the pattern is correct, the scope is wrong. `dna_fusion.py` is a request-path module (called by the router on session end) that the guard was never pointed at. Surfaced by Scale & Load Hunter during Demo T20 review (2026-08-13). | Med (CI guard has a named blind spot; any future unbounded read added to these files passes unseen) | **FIXED-GUARDED 2026-08-13 (`demo/defect-fixes-d74-d76-d77-d78`).** `REQUEST_PATH_FILENAMES` in `test_unbounded_queries.py` expanded to include `dna_fusion.py`, `dna_growth.py`, `ces.py`, `ces_baseline.py`, `dna_profile.py`. Deployed simultaneously with D96 (was D77) so new scope finds no violations. | `test_unbounded_queries.py` in CI — now a true enforcer for all assessment sub-modules. Any future unbounded `.select()` in covered files fails CI. |
| **D100** | **[Duplicate of D97 — same underlying defect, registered a second time from an earlier-drafted review pass that was never reconciled against the fix; kept as its own row rather than silently merged, since that reconciliation is Dev 3's content call, not a mechanical renumbering one.] `SessionCreate.lesson_id: str` has no `min_length` — an empty string passes Pydantic, reaches the Supabase `lessons` table (UUID-type PK), and returns HTTP 500 instead of 422.** `schemas.py` declares `lesson_id: str` with no `min_length` constraint. `POST /sessions` with `{"lesson_id": ""}` passes Pydantic validation, reaches `create_session`, and executes `supabase.table("lessons").select(...).eq("lesson_id", "").maybe_single()`. PostgREST casts the empty string to a UUID column, raises `invalid_text_representation`, and `create_session` has no exception handler for this path — FastAPI's default handler converts it to HTTP 500. Dev 2 receives a 500 with an opaque error instead of a clean 422. Surfaced by Scale & Load Hunter during Demo T26 review (2026-08-13, one-line-test question). | Low (Dev 2 would only hit this on a routing bug; empty string lesson_id is not a normal operation — but the contract says 422 for bad input, not 500) | **OPEN — Fix:** Add `min_length=1` to `SessionCreate.lesson_id` in `apps/api/app/modules/assessment/schemas.py`. This is a production code change forbidden by Demo T26's scope (`router.py`/`schemas.py` are read-only for that story). **Owner: Dev 3. Trigger: next story touching assessment schemas, or a dedicated one-line PR.** | DISCIPLINE until the schema fix lands; add `test_sessions_empty_lesson_id_returns_422` to `test_t26_api_contract_dev2.py` as part of the schema fix PR. |
| **D101** | **[Duplicate of D98 — same underlying defect, registered a second time from an earlier-drafted review pass that was never reconciled against the fix; kept as its own row rather than silently merged, since that reconciliation is Dev 3's content call, not a mechanical renumbering one.] `TeachbackSubmission.response_text: str = Field(min_length=1)` counts characters, not content — a whitespace-only string (e.g. `"   "`) passes Pydantic, reaches `grade_teachback`, triggers a real GPT-4o-mini call, and returns HTTP 200 with a TeachbackResult that looks valid but was scored on substantively empty content.** The failure mode is the Scale Contract signature: it is cheap, not expensive (`grade_teachback` for a 3-space string costs ~$0.0001), so the `$3.00/lesson` cost ceiling never fires. The student receives a low-quality rubric score from the LLM; the session's `ces_contribution` is silently biased downward; nothing errors; nothing warns. Surfaced by Scale & Load Hunter during Demo T26 review (2026-08-13). Documented in `test_t26_api_contract_dev2.py::test_teachback_whitespace_only_response_text_accepted` as a contract-fact test (Dev 2 must add client-side trim guard). | Med (every whitespace submission silently costs a GPT-4o-mini call and produces a low-quality rubric; at demo scale this is noise; at real-student scale it is a systematic CES bias) | **OPEN — Fix:** Add `@field_validator("response_text")` to `TeachbackSubmission` in `schemas.py` that calls `.strip()` and raises `ValueError` if the result is empty. This is a production code change forbidden by Demo T26's scope. **Owner: Dev 3. Trigger: next story touching assessment schemas.** The client-side guard (trim before submit) is already documented in `test_teachback_whitespace_only_response_text_accepted` — Dev 2 must add this regardless of the server-side fix. | DISCIPLINE until the validator lands; `test_teachback_whitespace_only_response_text_accepted` currently asserts HTTP 200 (the buggy behaviour) — flip it to assert 422 when the fix is deployed. |

---

### Found live during the L1 acceptance run (Story 3-42, 2026-08-12/13)

| ID | Defect | Sev | Decision | Enforcement |
|----|--------|-----|----------|-------------|
| ~~D74~~ | **CLOSED same-round 2026-08-13 (Story 3-42).** Two compounding defects in `SarvamTTSProvider._synthesize_inner`, both found live while investigating why every real narration segment fell through Sarvam → Azure (unconfigured) → browser during L1, AFTER D67 (invalid voice default) was already fixed. **(1)** Sarvam hard-limits each `inputs[]` string to 500 characters (real 400: `"String should have at most 500 characters"`) — every real narration segment observed runs 1,351–4,069 chars, so D67 alone could never have produced real Sarvam audio. **(2)** `audio_bytes = response.content` (pre-fix) captured the raw JSON response body, not audio — Sarvam's real response is `Content-Type: application/json`, `{"request_id": ..., "audios": ["<base64 WAV>", ...]}`. Confirmed live: `resp.content[:50]` is JSON text; the real audio only exists at `base64.b64decode(resp.json()["audios"][0])`. This has been broken since Story 2-8 and never caught because every existing test mocked `SarvamTTSProvider` at the call site — the pre-fix unit test for this exact function hardcoded `content=b"FAKEAUDIO"` and asserted the code returns it verbatim, literally encoding the bug as expected behavior. A **third** constraint (`inputs[]` also caps at 3 items/request, real 400: `"List should have at most 3 items"`) was found live while designing the fix, requiring batched requests, not assumed from docs. | High (Sarvam has never once produced real, playable audio in this project's history — not a voice-id issue, not an edge case, the universal case) | Added `_chunk_narration_text` (sentence-boundary splitting, word-boundary fallback for oversized single sentences), `_batched` (≤3 items/request), and `_concatenate_wav_clips` (real PCM-frame concatenation via the stdlib `wave` module — naive byte-concatenation of complete WAV files produces an invalid multi-header file). Verified live end-to-end against the real Sarvam API before writing tests: a 2,406-char segment → 5 chunks → 2 batched requests → one valid 130.45s WAV file, confirmed by reading it back with `wave`. | `tests/unit/test_sarvam_chunking.py` (15 tests: chunk-boundary respect, sentence-boundary preference, no dropped/duplicated words, oversized-sentence fallback, batch-size respect, real multi-clip WAV concatenation vs. a naive-join contrast test proving the naive approach is wrong) + `tests/unit/test_tts_providers.py`'s corrected success test (asserts DECODED audio, not raw `response.content`) and a new multi-batch integration test. RED-verified: reintroduced the old `response.content` bug and confirmed both the decode test and the multi-batch test fail; restored and confirmed GREEN. |

---

### Found preparing a stakeholder demo (Story 3-43, 2026-08-13)

| ID | Defect | Sev | Decision | Enforcement |
|----|--------|-----|----------|-------------|
| ~~D75~~ | **CLOSED same-round 2026-08-13 (Story 3-43).** `lesson_planner_batch_size` defaulted to 15, EQUAL to `structure_max_sections` (also 15) — Story 2-16's own comment (`graph.py:1450-1453`) assumed a chapter coalesced to exactly the max would still "fit a single planner call" safely, so the batching logic Story 2-16 built (to prevent a single completion from "collapsing" a large segment-id echo list — its own documented 44-in/10-out example) never actually triggered at the default config. Disproven live: two real L1 acceptance-run attempts on the same 15-segment chapter returned 5 and 12 segments — `lesson_planner segment count mismatch — expected 15, got 5` / `got 12`. **The obvious fix (`<=` to `<` at `graph.py:1420`) is a no-op at these defaults** — verified by hand before writing any code: a 15-item list split into batches of 15 still produces one batch of 15, identical to the single-call path. | High (blocks any real generation on a chapter coalescing to `structure_max_sections`, which the coalesce logic is specifically designed to produce as the common case for a dense chapter) | Lowered `lesson_planner_batch_size` default `15` → `10`, strictly below `structure_max_sections`, so the maximal chapter always genuinely batches (10+5) instead of silently taking the single (unreliable) call path. No documented "safe" threshold exists — 10 is a reasoned conservative margin, not a proven number, tunable via env var. | `tests/unit/test_coalesce_sections.py::test_config_defaults_and_planner_batch_invariant` (flipped to the new deliberate relationship), `tests/unit/test_lesson_planner_node.py::test_planner_batch_boundaries` (updated boundary table) + new `test_planner_batches_at_structure_max_sections_boundary` (uses the real `settings.structure_max_sections` value, not a hardcoded 15) + `tests/integration/test_howto_pipeline_e2e.py` (its own 20-step how-to already coalesced to exactly `structure_max_sections`, per its own comment "exercises the MAX-count cap" — now genuinely exercises real batching for the first time; its assertion updated from "one call sees every segment" to "the union of all calls covers every segment exactly once, no duplicates within or across calls"). RED-GREEN verified: reverted to batch_size=15, confirmed the new test and the flipped invariant fail with the predicted messages; restored, confirmed green. |
| ~~D76~~ | **CLOSED same-round 2026-08-13 (Story 3-43).** `max_narration_chars_per_lesson` (Story 3-37) defaulted to 10,000, sized against `docs/decisionupdate.md` §8's ~1,600 chars/min speed assumption — stale. Real measured Sarvam Bulbul v2 rate (live test, same session as D74): **18.44 chars/sec = 1,106.6 chars/min**. At the real rate, 10,000 chars ≈ 9 real minutes, not the ~5 min `decisionupdate.md` assumed — capping every lesson below a 15-minute stakeholder-demo target regardless of source content length. | Med (blocked the stated sprint demo goal; not a correctness bug, a mis-sized budget) | Raised default `10,000` → `17,000` (15 real minutes needs ≈16,600 chars + margin). **Cost re-derived, not assumed**: at `COST_PER_CHAR = 0.00002`, 17,000 chars costs ≈$0.33 in TTS / ≈$0.40 total lesson cost (derived from `decisionupdate.md`'s own "TTS is 67–73% of total" claim) — ≈13% of the $3.00 ceiling. Cost was never the real constraint; the speed assumption was. **Deliberately not fixed here**: the separately-disagreeing word-count-based time ESTIMATE (`narration_words_per_minute=150`) — the real, final duration is already measured from actual audio via Story 3-38's `tinytag` integration, not this estimate; a real, separate inconsistency, out of this fix's minimal scope. | 5 tests in `tests/unit/test_tts_node.py` that construct fixtures crossing the literal 10,000-char boundary now pin an explicit test-local cap via a new `_mock_settings_with_narration_cap()` helper + `patch("app.config.get_settings")`, decoupling them from whatever the production default happens to be, rather than rewriting their fixture arithmetic to a new magic number. |
| ~~D77~~ | **CLOSED same-round 2026-08-13 (Story 3-44) — found running D75's own fix for real, the first live test of Story 3-43's Phase 4.** Two consecutive real demo-generation attempts against the MERGED D75 fix still failed: `expected 15, got 14`, then `expected 15, got 12`. Verified live (not assumed) via the real Langfuse trace before writing any code: `create_trace_id(seed=lesson_id)` → `GET /api/public/traces/{id}` on the second failure showed batching genuinely engaged exactly as D75 designed — one real LLM call carrying 10 `segment_id` refs, a second carrying 5. **D75's fix works correctly; the residual gap is that a real LLM can still occasionally under-echo even a correctly-sized 10- or 5-item batch** — the same 1:1-echo unreliability Story 2-16 first found at 44 items, reduced in frequency by smaller batches but not eliminated by batch-sizing alone. | Med (D75 already closed the severe case; this is the residual lower-frequency tail, but still real and observed twice in two attempts) | `_run_planner_batch` now retries the SAME batch's own completion (not the whole node, not other already-correct batches) up to `_PLANNER_BATCH_MAX_ATTEMPTS = 3` times when the response doesn't echo every input `segment_id` in that batch exactly once — targets the actual observed failure mode directly instead of guessing an even smaller "safe" batch size with no more evidence than D75 already had. A `None` response still raises immediately, unchanged (different, already-immediate failure mode, out of scope). When every retry exhausts, the existing assembled-response guard in `lesson_planner_node` still raises its own contextual error exactly as before — retries are a recovery layer in front of the existing guarantee, not a weakening of it. | `test_planner_retries_same_batch_on_echo_mismatch_and_recovers` (a batch that fails once then succeeds — asserts the RECOVERED response is used, not just that the eventual guard fires) + `test_planner_batch_retry_exhausts_and_still_raises_via_existing_guard` (a permanently-broken batch still raises after exactly 3 attempts, no more/fewer). Both pre-existing guard-preservation tests (`test_planner_batched_dropped_id_still_rejected`, `..._duplicate_id_count_preserved_still_rejected`) re-verified to still assert the correct final outcome with retries running underneath them. RED-GREEN verified via the Edit tool directly (a fragile string-replace script failed silently earlier in this same investigation — switched to a more reliable method after catching that). Full repo-wide regression: 54 failed/2062 passed/85 skipped — the established baseline, zero new failures. |
| ~~D78~~ | **CLOSED same-round 2026-08-13 (Story 3-45) — found inspecting the first real, fully successful demo lesson produced after D75+D76+D77 (lesson `abe4e438`, an entirely ordinary 29-page/15-section chapter).** D76 raised `max_narration_chars_per_lesson` 10,000 → 17,000, explicitly sized against "a real 15-minute lesson" — but 15 minutes was only ever a demo illustration, not a real product requirement, and the user explicitly corrected this framing before the fix shipped further. The real lesson proved the framing wasn't just mis-described, it was actively harmful: 43,793 real narration chars crossed the 17,000 cap and `_apply_narration_char_cap` zeroed the narration script for segments 6–14 (9 of 15). Zeroed scripts have nothing to synthesize, so `tts_node` degraded all 9 straight to the `browser` fallback (no server-synthesized audio at all) — a complete loss of real TTS audio for 60% of the lesson, not a truncation of it. `package_builder`'s D32/D33 defensive recovery correctly restored the readable *text* for those segments (working exactly as designed, not itself a bug), but the *audio* experience for the majority of the lesson silently fell back to whatever speech synthesis the viewer's browser happens to have. Real recorded cost for this same lesson: ≈$0.88 (43,793 chars × `COST_PER_CHAR=0.00002`) — **29% of the $3.00 ceiling**, nowhere near binding; the char cap cut in more than 3× more conservatively than the real, already-enforced dollar ceiling ever would have. | Med (not a crash, and D32/D33 prevented a broken package — but a materially degraded stakeholder-facing deliverable, confirmed live, not hypothetical) | Raised `max_narration_chars_per_lesson` default `17,000` → `120,000`, re-sized against real cost headroom instead of any duration: 120,000 chars ≈ $2.40 Sarvam TTS spend = 80% of the $3.00 ceiling, leaving 20% for the LLM + image spend the same ceiling already tracks. The cap's original job (Story 3-37 / `decisionupdate.md` §8: "bound the dominant cost driver before it's incurred") is unchanged — only the value moved, from a demo-duration guess to a re-derived cost backstop, per Scale Contract Q5. Lesson length is now driven by the chapter's real content; only real cost (this backstop, and the already-enforced per-segment `check_ceiling`) can shorten it, and both still degrade explicitly (a persisted `narration_cap_applied` / cost-downshift record), never silently. | New `test_production_default_does_not_truncate_a_real_world_sized_lesson` in `tests/unit/test_tts_node.py` — uses the REAL `get_settings()` (not a mocked test-local cap, mirroring D75's `test_planner_batches_at_structure_max_sections_boundary` pattern) against the exact 15-segment character distribution from lesson `abe4e438`'s real production run (43,793 chars total) and asserts every segment reaches Sarvam unmodified. RED-GREEN verified: failed against the pre-fix 17,000 default with the exact predicted zeroing (`sec_6` 0 of 3251 chars survived), passed after the config change. All 21 pre-existing `test_tts_node.py` tests (including both other narration-cap boundary tests) unaffected — they already pin their own test-local cap via `_mock_settings_with_narration_cap` (D76), independent of the production default. Full repo-wide regression re-baselined directly against this same commit (not the stale D77 story number, which had already drifted): 52 failed/2062 passed/86 skipped before this change, 52 failed/2063 passed/86 skipped after — zero new failures, +1 for this story's new test. |

---

### Found watching the first real lesson play in a browser (Stories 3-46/3-47, 2026-08-13)

| ID | Defect | Sev | Decision | Enforcement |
|----|--------|-----|----------|-------------|
| ~~D85~~ | **FULLY CLOSED 2026-08-13 — Round 1 (Story 3-46) fixed the allocation mechanism; Round 2 (D87, Story 3-49) fixed the values that mechanism allocates. Both required; neither alone.** `_tier_slide_budget_per_segment` divided each tier's fixed total-lesson slide band (`_TIER_TOTAL_SLIDE_BAND`: T1=(20,25), T2=(12,15), T3=(6,8)) evenly by segment COUNT via ceiling division, returning ONE `(min,max)` pair shared by every segment regardless of how long each segment's narration actually runs. Confirmed live watching the D78-fixed demo lesson play in a browser: at `structure_max_sections=15` (the common/maximal real case), T2 and T3 both collapse to `(1,1)` for all 15 segments — `ceil(12/15)=1`, `ceil(6/15)=1` — so a 3.48-minute segment and a 1.23-minute segment got the identical single static slide. Fixed the ALLOCATION to be proportional to each segment's share of estimated lesson duration (already computed by the lesson_planner LLM as `duration_min`, previously discarded) instead of flat count division — verified correct via a T1 assertion, which DOES now differentiate (wider band gives real headroom). **Honestly re-verified against the exact real 15-segment dataset, not assumed fixed**: T2 and T3 STILL produce `(1,1)` for every segment even under the new proportional mechanism, because both tiers' total_max (15 and 8) is `<=` the segment count (15) — proportionally redistributing a total that's already at-or-below the segment count has nothing to redistribute; every segment is already pinned to the structural floor of 1. This is not an implementation bug (the mechanism is proven correct where there's headroom) — it's that `_TIER_TOTAL_SLIDE_BAND`'s values were never re-derived against `structure_max_sections=15`, the same class of stale-inherited-cap issue as D75/D76/D78. **The user's actual observed complaint (T3, the tier used for the demo) is therefore NOT YET fixed by this story alone** — flagging honestly rather than reporting D85 as fully closed. | Med (mechanism now correct and will differentiate as soon as the band values are re-derived; the demo-visible symptom persists until that follow-up lands) | Changed `_tier_slide_budget_per_segment(tier, segment_count: int) -> tuple[int,int]` to `_tier_slide_budget_per_segment(tier, segment_durations_min: list[float]) -> list[tuple[int,int]]`, allocating each segment's own `(min,max)` proportional to `duration/total_duration`, clamped to the structural `[1,8]` band, with a fallback to the old flat-division formula when total duration is `<=0` (malformed/all-zero input, not a basis for a real distribution). `lesson_planner_node`'s call site updated to pass real per-segment durations (from the same `llm_segment_by_id` lookup already used for title/summary) and index each segment's own budget instead of reusing one shared tuple. **Follow-up required, not yet scheduled**: re-derive `_TIER_TOTAL_SLIDE_BAND`'s actual values against real segment counts (the old numbers were sized with no visibility into a real 15-segment lesson) — a product/cost tradeoff decision (more slides per tier = more `image_generator` calls = real added cost), not a pure code fix, needs explicit confirmation before implementing. | `tests/unit/test_lesson_planner_node.py::test_slide_budget_proportional_to_real_d85_durations` (the real 15-segment dataset from the demo lesson; asserts all 15 entries within `[1,8]`, asserts the T2 `(1,1)`-for-both-extremes finding explicitly rather than hiding it, and asserts T1 DOES differentiate on the same data) + `test_slide_budget_zero_total_duration_falls_back_to_flat_division` (proves the fallback reproduces the exact old formula, no `ZeroDivisionError`) + 5 existing tests updated for the new signature/behavior. RED-GREEN verified via the Edit tool (old code: `TypeError` on the new signature-based tests, stale flat-tuple mismatches on the updated ones). Full repo-wide regression: 52 failed/2013 passed/86 skipped/3 collection errors before, 52 failed/2015 passed/86 skipped/3 collection errors after (worktree-local baseline) — zero new failures. |
| ~~D87~~ | **CLOSED 2026-08-13 (Story 3-49, `sprint3/s3-49-d87-slide-budget-duration-target`) — the D85 follow-up, explicitly deferred at D85's own close.** D85 fixed HOW `_tier_slide_budget_per_segment` allocates a tier's slide budget (proportional to duration, not flat count division) but not WHAT total it allocates: `_TIER_TOTAL_SLIDE_BAND` was still a FIXED lesson-wide count (`T1=(20,25)`, `T2=(12,15)`, `T3=(6,8)`) sized with no visibility into a real `structure_max_sections=15` lesson. At 15 segments, T2's and T3's totals were already `<=` the segment count, so D85's proportional allocator had nothing left to redistribute — every segment stayed pinned to the structural floor of 1, identical to the pre-D85 bug. Re-verified directly against the real 15-segment demo-lesson dataset before designing a fix (not assumed): T2 and T3 both still produced `(1,1)` for every segment; only T1 (real headroom above 15) differentiated. **Confirmed before implementing, not assumed**: `slide_generator_node`'s prompt sends the literal per-segment instruction `"(produce {min} to {max} slides for this segment)"`, so a `(1,1)` budget produces exactly 1 slide because that is the explicit instruction the model is given — not a separate LLM-compliance bug, meaning widening the budget alone is sufficient. **Real cost checked before proposing a fix, not assumed**: from the real T3 demo lesson, image+LLM spend was ≈$0.38 for 15 images (~$0.025/image) against total lesson cost ≈$1.27 — a few additional slides adds cents, nowhere near the $3.00 ceiling. | Med (same severity class as D85 — a materially degraded stakeholder-facing deliverable, confirmed live, not a crash) | Replaced the fixed `_TIER_TOTAL_SLIDE_BAND` with `_TIER_MINUTES_PER_SLIDE_BAND` (`T1=(0.8,1.2)`, `T2=(1.2,1.8)`, `T3=(2.0,3.0)` minutes/slide) — the tier's total now scales with the lesson's REAL estimated total duration (`sum(segment_durations_min)`, already computed) instead of a number that needs re-deriving every time segment counts or lesson lengths change. D85's per-segment proportional-allocation loop is otherwise unchanged — same `share`-based math, same structural `[1,8]` clamp (the real safety ceiling, unaffected). Verified on the real dataset: T3's longest segment (3.48 min) now gets `(1,2)` instead of `(1,1)`; T2's gets `(2,3)`; T1's gets `(3,4)` — all three tiers differentiate now, not just T1. The zero-total-duration fallback (already proven unreachable through `lesson_planner_node` itself by D85's own test) simplifies to a flat, tier-independent lean floor `(1,1)` — there is no fixed per-tier total left to fall back to. | `tests/unit/test_lesson_planner_node.py::test_slide_budget_proportional_to_real_d85_durations` rewritten to assert the NEW differentiating behavior for all three tiers (not just T1) on the exact real dataset; `test_slide_budget_zero_total_duration_falls_back_to_lean_floor` (renamed, asserts the new flat floor); 4 existing tier-framing tests updated with the new duration-scaled expected values (computed by direct execution, not hand math); one test (`test_tier_t3_five_segments_never_undercuts_total_min`) REMOVED with an explanatory comment left in place — its entire premise (a fixed per-tier lesson-wide total_min the fallback must never undercut) no longer exists once the fixed total was deleted, not silently dropped. RED-GREEN verified via `git stash`/pop on the implementation file only: the updated/new tests failed against pre-D87 code with the exact predicted `(1,1)`-for-both-extremes / stale-fixed-value mismatches; restored, all green. Full repo-wide regression: 52 failed/2102 passed/86 skipped both before and after (one fewer test collected from the deliberate removal, zero new failures). |
| ~~D86~~ | **CLOSED 2026-08-13 (Story 3-47, `sprint3/s3-47-cost-persist-lesson-jobs`).** `lesson_jobs.cost_usd` (`numeric(10,4)`, frozen initial schema, meant to be the durable post-hoc cost record `docs/handoffs/lesson-delivery-dev1.md`'s own L1 checklist requires — "Record: measured cost per lesson") was never written by `content_pipeline_job` at any terminal transition, confirmed by zero `cost_usd` references anywhere in the file and by `clear_lesson_cost()`'s own docstring promising "the cost has been persisted to the DB" before deleting the Redis running total — a promise never implemented. Confirmed live on two real, real-money lesson generations this session (both with confirmed real OpenAI + Sarvam spend, verified by downloading and validating real audio files): both show `lesson_jobs.cost_usd = 0.0000`. The real, live-enforced $3.00 ceiling itself (Redis-backed `check_ceiling`/`accumulate_cost`) was never wrong — only the durable record of what a completed lesson actually cost. | Med (the $3.00 ceiling enforcement itself was never affected — this is a reporting/calibration gap, but it's the exact number this project's own L1 acceptance criteria requires and has never once been recorded) | The success path now reads `get_cost(lesson_id)` (BEFORE `clear_lesson_cost` deletes the Redis key) and folds `cost_usd` into the same `lesson_jobs` completion update — no second DB round trip. `_update_lesson_status` (the shared helper behind all 4 failure/cancellation transitions) extended with the same fetch-and-persist behavior whenever `status == "failed"`, so a lesson that fails partway through still records its real partial spend instead of 0 — arguably the more important case for debugging a cost-ceiling-triggered failure. Both read sites degrade to leaving `cost_usd` unset (never a fabricated 0) on a Redis read failure, matching this codebase's established try/except-and-degrade pattern (`_update_lesson_status`'s own existing Supabase-write guards, Langfuse's `_safe_trace`) — a secondary tracking concern must never break an otherwise-successful or otherwise-correctly-failing pipeline run. | 6 new tests across `tests/test_lesson_ready_pubsub.py` (3, success path: real cost included, read-before-clear ordering, survives a `get_cost` failure) and `tests/unit/test_timeout_contract.py` (3, failure path: generic-exception failure includes real cost, survives a `get_cost` failure, the `"running"` transition does NOT fetch cost since nothing has accumulated yet). RED confirmed via the Edit tool (3 of 6 failed against pre-fix code with real, sensible errors — `KeyError: 'cost_usd'`, wrong call order); restored, all 6 green. Full repo-wide regression: 54 failed/2090 passed/85 skipped before, 54 failed/2096 passed/85 skipped after (identical failure set, verified by name) — zero new failures. |

---

### Found watching the real lesson play, and running a second real generation (Stories 3-53/3-52/3-54, 2026-08-13)

| ID | Defect | Sev | Decision | Enforcement |
|----|--------|-----|----------|-------------|
| ~~D88~~ | **CLOSED 2026-08-13 (Story 3-53, `sprint3/s3-53-d88-d90-player-ui-fixes`).** A real stakeholder watching the real D78-fixed lesson play found slide content overflowing off the visible screen. Root cause: `apps/web/src/app/lesson/[id]/layout.tsx` used `min-h-screen` (a minimum, free to grow) instead of a viewport-fixed height, and `Player.tsx`'s slide-area container (`<div className="relative flex-1">`) was missing `min-h-0` — the classic CSS flexbox bug where a flex item's default `min-height: auto` means it will not shrink below its content's natural height even inside a `flex-1` parent, so tall slide content grew the whole page past the viewport instead of scrolling inside its own box. `SlideRenderer.tsx` already had the correct `overflow-y-auto overscroll-y-contain` internal scroll but never received a bounded parent height to actually engage against. **This is normally Dev 2's file territory** — implemented directly since the exact root cause was already identified, flagged explicitly for Dev 2's visibility in the story, not silently claimed. | Med (a real, stakeholder-visible layout defect, not a crash) | `layout.tsx`: `min-h-screen` → `h-dvh` (dynamic viewport height — more correct than `h-screen` on mobile browsers whose chrome changes the visible viewport). `Player.tsx`: added `min-h-0` to the slide-area container. `SlideRenderer.tsx` untouched — its overflow handling was already correct. | A `min-h-0` class-presence assertion in `Player.test.tsx` (D88). Full `apps/web` suite: 768 passed, 5 pre-existing failures in unrelated files (`proxy.test.ts`, `contract.test.ts`) confirmed identical on an unmodified baseline — zero regressions. `npm run type-check`/`npm run lint` clean. |
| ~~D89~~ | **CLOSED 2026-08-13 (Story 3-52, `sprint3/s3-52-d89-sarvam-pace`).** The same stakeholder reported generated-lesson narration as "very fast." Verified against Sarvam's real Bulbul v2 API docs (not assumed): a `pace` parameter exists (default `1.0`, valid range `0.3`-`3.0`), and `apps/api/app/providers/tts/sarvam.py`'s synthesize request payload never sent it — every lesson synthesized at Sarvam's raw, unmodified `1.0` default. | Med (real, stakeholder-reported UX defect) | Added `settings.sarvam_narration_pace` (default `0.85`, `ge=0.3`, `le=3.0`, tunable via env var without a code change, matching `sarvam_voice_id`'s existing pattern), read once in `SarvamTTSProvider.__init__` and sent as `"pace"` in every synthesize request. Pace only — no other Sarvam parameter touched. | `test_sarvam_synthesize_request_includes_configured_pace` in `test_tts_providers.py`. RED confirmed (reverting the `"pace"` line produced `KeyError: 'pace'`), restored GREEN. Full repo-wide regression: 52 pre-existing failures (unrelated, verified identical on a clean baseline worktree) — zero new failures, +1 passing. |
| ~~D90~~ | **CLOSED 2026-08-13 (Story 3-53, `sprint3/s3-53-d88-d90-player-ui-fixes`).** The same stakeholder asked for captions — confirmed via repo-wide grep (`caption\|subtitle\|transcript`, case-insensitive) that zero caption/subtitle UI existed anywhere in `apps/web/src`. Word/sentence-level SYNCED captions are not possible yet — the Sarvam TTS integration deliberately returns empty word-level timestamps (a separate, larger follow-up) — so this is deliberately scoped to a non-synced, always-visible panel. **Same Dev 2 ownership note as D88.** | Med (a real, requested, and buildable-now UX gap) | New `apps/web/src/components/player/CaptionOverlay.tsx`, mounted in `Player.tsx`'s slide area, receiving `segment?.narration.script ?? null`. Renders nothing when null/empty (mirrors `SlideImage`'s existing "render nothing" pattern). Styled to match this codebase's existing dark-overlay conventions. No show/hide toggle in this first pass — explicitly noted as a reasonable near-term enhancement, out of scope here. | `CaptionOverlay.test.tsx` (4 tests: renders script, renders nothing when null/empty) + a `Player.test.tsx` integration test (D90). Same full-suite/typecheck/lint verification as D88 (one combined story). |
| ~~D91~~ | **CLOSED 2026-08-13 (Story 3-54) — D53's own deliberately-deferred follow-up, triggered by live evidence, not a fresh discovery.** D53's reaper used `lessons.created_at` (row-insert time, before the job was even enqueued) as its only staleness signal — its own docstring already named this exact gap ("the durable fix is the D53 reaper plus a real `started_at`") and deferred it as an accepted limitation. **Observed live, not hypothetical**, running a second real generation (ch5/T1, lesson `b0a96211`): an ARQ retry was delayed ~1925s (~32 min) before even being dequeued — likely the same event-loop-blocking pattern from earlier this session, compounded by a real OpenAI Image Generation timeout. The reaper (10-minute cadence) saw the row as stale under the old logic and marked it `failed` while the job was still actually executing, leaving `lessons.status='failed'` and `lesson_jobs.status='running'` permanently inconsistent for that lesson — the exact harm a false-positive reap causes. Same run also surfaced a real, external, non-code blocker worth recording here for context even though it required no fix: **Sarvam TTS returned `402 Payment Required`** (account out of credits, same shape as the L0 OpenAI-credits blocker) and Azure TTS returned `401 Unauthorized` (a pre-existing, already-known gap, never configured) — every segment in that run fell through to the free `browser` fallback as a result; this is an operational/billing matter, not a defect, and needed no code change. | Med (the reaper's own correctness — a false-positive reap actively harms an otherwise-fine job, worse than the original D53 problem in the specific case it happens) | `content_pipeline.py`'s `_update_lesson_status` now writes a real `lesson_jobs.started_at` on every `"running"` transition (overwritten fresh on every retry attempt — correct, since a fresh attempt deserves a fresh staleness clock). The reaper now queries `lesson_jobs` directly (not `lessons`) for `status IN ('pending','running')`, bounded by a generous outer cutoff (`arq_job_timeout_s × 2`, covering legitimate queue-wait + run time), then refines precisely in Python: a job with a recent real start is never reaped regardless of row age; a job that never started running is reaped once past the generous bound. `_generating_cutoff_iso()` and Gate 5/Gate 7 in `router.py` are deliberately UNCHANGED — their conservative `lessons.created_at`-based check errs in the safe direction (not letting a duplicate request through); only the reaper's error direction (falsely killing a live job) needed the more precise signal. | `test_does_not_reap_a_job_with_a_recent_real_start_despite_old_created_at` — the core fix, reproducing the exact live false positive directly. Plus `test_reaps_a_job_whose_real_start_time_is_past_the_run_cutoff`, `test_reaps_a_never_started_job_past_the_generous_queue_cutoff`, `test_no_candidates_is_a_pure_noop`, `test_reap_query_targets_lesson_jobs_status_and_is_bounded`, `test_one_bad_row_does_not_stop_the_batch` (all in `test_reap_stale_lessons.py`, rewritten for the new query shape) + `test_running_transition_writes_a_real_started_at` (new, `test_timeout_contract.py`, using the file's existing multi-table mock helper to correctly attribute the write to `lesson_jobs` and not the mirrored `lessons` write). RED-GREEN verified via `mv`-aside (implementation) and `git stash`/pop (`_update_lesson_status` extension). Full repo-wide regression: 52 failed/2113 passed/86 skipped — established baseline, zero new failures. |

---

### Found by the 2026-08-13 Story 4-26 six-layer review (tutor router endpoints)

| ID | Defect | Sev | Decision | Enforcement |
|----|--------|-----|----------|-------------|
| **D79** | **IDOR — tutor router has no session ownership check.** `GET /state` and `POST /intervene` validate JWT but never compare `current_user["sub"]` to the session's owner. Any authenticated user can read or modify any session. `POST /intervene` with `force=true` has no admin-role gate. WS `manager.send` delivers to whoever is connected on `session_id` — no ownership verification. Explicitly deferred in Story 4-26 out-of-scope (tied to `fix/4-17-jwt-es256-verification`). | High (any authenticated user can trigger interventions in another user's session) | **Deferred** — must be implemented before real-student launch (Week 10). Session ownership validation + admin-role gate on `force=true`. **Owner: Dev 4. Trigger: `fix/4-17-jwt-es256-verification` story or real-student launch milestone.** | Integration test asserting a second authenticated user's `GET /state` returns 403. |
| **D80** | **Concurrent POST `/intervene` double-fire race.** Two simultaneous non-`force` POSTs that both arrive before the cooldown TTL is written can both pass the cooldown guard and both call `dispatch_event`. Two concurrent `force=true` POSTs both delete the same key (idempotent) then both dispatch. The distraction cap (default 3) bounds total firings per session, so the invariant holds, but the count can overshoot by 1 in the concurrent window. Documented in Story 4-26 Scale & Load Q6 as an accepted documented limitation for admin/demo use. | Low (bounded by distraction cap; admin/demo endpoint only, not a hot path) | **Deferred** — the distraction cap provides a natural bound; this is acceptable for an admin endpoint. **Owner: Dev 4. Trigger: any real-user report of unexpected double-fire.** | DISCIPLINE (cap provides natural bound; no machine check). |
| **D81** | **MemorySaver `thread_id=session_id` scope not documented in `graph.py`.** `dispatch_event` reuses `thread_id=session_id` across all calls for the tutor FSM — this is intentional for state continuity. The CLAUDE.md binding rule "unique thread_id per pipeline attempt" was written for the content pipeline (ARQ retry isolation); Story 4-26 Scale & Load Q5 incorrectly claimed "the per-attempt nonce prevents contamination" — amended in story. The distinction is nowhere documented in `graph.py`, so future developers may apply the content pipeline rule to the tutor FSM (as the Edge Case Hunter did during this review). | Low (no production impact; documentation gap only) | **Deferred** — add an explanatory comment in `graph.py:dispatch_event` distinguishing tutor FSM intent from content pipeline rule. **Owner: Dev 4. Trigger: any future graph.py edit that adds a nonce to `thread_id`.** | DISCIPLINE (comment to be added). |
| **D82** | **Full LessonPackage JSON deserialized on every intervention call.** `service.py:_segment_intervention_messages` reads and fully deserialises the entire lesson package from Redis on every `POST /intervene` call. For a large lesson package this is O(package size) per intervention. Pre-existing defect in `service.py`, not introduced by Story 4-26. | Low (intervention is a low-frequency admin/demo endpoint; package size is bounded by lesson size) | **Deferred** — cache the parsed package per request lifecycle or switch to a Redis hash (HGET per field). **Owner: Dev 4. Trigger: any report of slow `POST /intervene` response.** | DISCIPLINE (low real-world impact). |
| **D83** | **`TutorSessionState.current_slide_index` and `.last_intervention_type` always `null`.** These fields are exposed in the API response schema but are never populated — neither value is persisted in Redis. A caller that branches on these fields gets a silent-wrong-result (null ≠ absent). Explicitly deferred in Story 4-26 out-of-scope. | Low (schema documents the null contract; fields are optional) | **Deferred** — either add Redis persistence for both fields (owned by Dev 2 and Dev 4 respectively), or remove from schema with a version bump. **Owner: Dev 4. Trigger: any frontend feature that consumes either field.** | Docstring added to `TutorSessionState` fields noting the null contract (DISCIPLINE until persisted). |
| **D84** | **Intervention message always index `[0]`, silent wrong message after first segment.** `_segment_intervention_messages` returns `messages[0]` from the first segment's interventions regardless of the current segment. After the first segment the message will be wrong. Pre-existing defect in `service.py:_segment_intervention_messages`. | Low (intervention overlay shows a message; the *wrong* message is better than no message for demo use) | **Deferred** — track current segment index in Redis and use it to select the correct message. **Owner: Dev 4. Trigger: any complaint about wrong intervention message content.** | DISCIPLINE (pre-existing; bounded — there are only 3 intervention types). |

---

### Found by the 2026-08-13 Demo T28 six-layer review (Learner DNA display contract)

| ID | Defect | Sev | Decision | Enforcement |
|----|--------|-----|----------|-------------|
| **D87** | **Reassessment bypass is three non-atomic Redis operations.** `POST /onboarding/submit` (`router.py:258-264`) checks the reassessment bypass via `GET(reassessment_key) → DELETE(onboarding_done) → SET NX(onboarding_done)` as three independent Redis operations with no Lua transaction or pipeline. If the `reassessment_key` TTL expires between the `GET` (which evaluates to truthy) and the `DELETE(onboarding_done)` call, the idempotency lock is released without a valid reassessment still in effect, allowing one unauthorised resubmission. The window is narrow (TTL expiry coinciding with mid-request execution) but unbounded across concurrent users. Pre-existing in `router.py:258-264`, not introduced by Demo T28. | Low (narrow race; reassessment window is admin-set, not user-triggered; no financial or safety consequence) | **Deferred.** Fix: replace the three operations with a Lua script or Redis pipeline that atomically checks `reassessment_key`, deletes `onboarding_done`, and sets the new `onboarding_done` flag only when the reassessment key is still live. **Owner: Dev 4 (Redis buffer/lock patterns). Trigger: any reported case of an unexpected second onboarding submission succeeding without an explicit reassessment flag.** | None currently — DISCIPLINE. |

---

### Found by Story 3-55 assessment API production-readiness audit (2026-08-13)

| ID | Defect | Sev | Decision | Enforcement |
|----|--------|-----|----------|-------------|
| ~~D102~~ | **[Renumbered from D92 on merge — collided with this register's own D92, independently assigned to a different Dev 3 defect during this same review cycle. See the collision note above the D92 row.] CLOSED 2026-08-13 (Story 3-55, patched further by Story 3-55 review).** `fuse_learner_dna()` in `dna_fusion.py` SELECTs `session_events` with no `.limit()`. Fixed by adding `.order("created_at").limit(10_000)` — ordering ensures the OLDEST events are omitted at the cap (not a non-deterministic heap-order subset); logger.warning fires when the cap is hit so the admin has visibility (Scale Contract Q2: explicit surfaced degradation). Typical per-session event volume is ~100; 10,000 is a 100× safety margin. The `quiz_attempts` and `teachback_attempts` SELECTs in the same function also had no `.limit()`; they received `.order("created_at").limit(10_000)` in the same review pass (see D106, D107, renumbered from D96/D97, for their accuracy-computation follow-ups in Sprint 4). | Low (event count per session is small in MVP; the defect grows as sessions do) | Fixed Story 3-55. `.order("created_at").limit(10_000)` on all three SELECTs; truncation warning fires at cap for `session_events`. | `tests/unit/test_session_create_schema.py::test_dna_fusion_session_events_is_bounded` (AST walk asserts `.limit()` is present on the `session_events` chain). |
| ~~D103~~ | **[Renumbered from D93 on merge — same collision as D102 above.] CLOSED 2026-08-13 (Story 3-55).** `apps/api/app/modules/assessment/dna_fusion.py` was excluded from `test_unbounded_queries.py`'s CI scanner because `REQUEST_PATH_FILENAMES = ("router.py", "service.py")` did not include `"dna_fusion.py"`. `dna_fusion.py` is on the request path (`service.py` imports and calls `fuse_learner_dna` on every session-end), so any new unbounded SELECT added to it would be invisible to CI. The scanner's own `_function_scope_bounds` leniency masked D102's unbounded read even after D102 was fixed (the function contains `.maybe_single()` on a different SELECT, which the leniency treats as bounding the whole function). D103's fix adds `"dna_fusion.py"` to `REQUEST_PATH_FILENAMES` so future additions are scanned. The dedicated D102 AST-walk test handles the current function's leniency gap independently. | Low (CI gap only; no production impact on its own) | Fixed Story 3-55. `"dna_fusion.py"` added to `REQUEST_PATH_FILENAMES` in `test_unbounded_queries.py`. Premise test `test_request_path_modules_are_where_we_think_they_are` extended with `"assessment/dna_fusion.py" in names` assertion. | `tests/unit/test_unbounded_queries.py::test_request_path_modules_are_where_we_think_they_are` (extended assertion). |
| ~~D104~~ | **[Renumbered from D94 on merge — same collision as D102 above.] CLOSED 2026-08-13 (Story 3-55).** `SessionCreate.lesson_id: str` had no UUID validator. Any non-UUID string (including a 1-char typo like `"x"`, an empty string, or a correct-length but wrong-format string) passed Pydantic validation without error, reached the Supabase `.eq("lesson_id", ...)` call, and 500'd at the Postgres UUID cast with an opaque `invalid input syntax for type uuid` error — no actionable information to the caller, no 422. Fixed by adding `@field_validator("lesson_id", mode="before")` that calls `uuid.UUID(str(v))` and returns `str(parsed)` (lowercased RFC 4122 canonical form). Type is preserved as `str` so `body.lesson_id` in `router.py` needs no changes. | Med (silent 500 on any malformed lesson_id — one typo permanently breaks a student's session-start flow) | Fixed Story 3-55. `@field_validator("lesson_id", mode="before")` added to `SessionCreate` in `schemas.py`. | `tests/unit/test_session_create_schema.py::test_session_create_validates_uuid_format` + `::test_session_create_accepts_uppercase_uuid`. |
| ~~D105~~ | **[Renumbered from D95 on merge — same collision as D102 above.] CLOSED 2026-08-14 — confirmed stale duplicate of D93.** EMA `session_count` read-modify-write race in `fuse_learner_dna()`. This row described the exact same defect as **D93**, which was already **FIXED-GUARDED 2026-08-13**: (1) `dna_fusion.py`'s `upsert_payload` (lines 380-383) does NOT contain `session_count` — a comment at 376-379 explicitly states why (`"D93 (was D74): session_count is intentionally absent..."`); (2) Step 5b (lines 413-427) calls `supabase.rpc("increment_learner_dna_session_count", {"p_user_id": str(user_id)})` in a non-fatal try/except; (3) migration `20260813000001_dna_session_count_atomic_increment.sql` creates the `increment_learner_dna_session_count(p_user_id uuid)` Postgres function doing `UPDATE public.learner_dna SET session_count = session_count + 1 WHERE user_id = p_user_id` — a single atomic statement, serializing concurrent calls at the DB level. D93's FIXED-GUARDED status is accurate. Verified and closed 2026-08-14 per Dev 3 handoff from Dev 1 (dev3-handoff-2026-08-14.md). | Low → **CLOSED** | Closed as duplicate of **D93** (FIXED-GUARDED 2026-08-13). The residual `old_session_count + 1` arithmetic at lines 448 and 469 is used only for the Redis reassessment-flag threshold check and a log line — never for the persisted DB value. | `test_dna_fusion.py::test_async_session_count_incremented` — asserts `session_count` absent from upsert payload AND `supabase.rpc` called with `increment_learner_dna_session_count` (same guard as D93). |
| **D106** | **[Renumbered from D96 on merge — same collision as D102 above.] `quiz_attempts` DNA accuracy computed over entire retake history, not latest attempt per question.** `fuse_learner_dna()` SELECTs all `quiz_attempts` rows for the session. `UNIQUE (session_id, question_id, attempt_number)` permits unlimited retakes; `service.py` increments `attempt_number` with no ceiling. `_compute_signals` computes accuracy as `correct / len(quiz_rows)` across ALL attempts including retakes — a student who failed 9 times then passed on the 10th attempt scores 10% accuracy, not 100%. The `.limit(10_000)` cap added in Story 3-55 bounds the read but does not fix the denominator. Full fix: filter to latest `attempt_number` per `(session_id, question_id)` before computing accuracy (Postgres `DISTINCT ON` or client-side max filter). **Owner: Dev 3. Trigger: before real-student launch (Week 10) or when multi-attempt sessions enter production testing.** | Med (silent wrong result — student's Learner DNA `pattern_recognition` and `logical_deduction` silently deflated by retake history; no error, no warning, HTTP 200) | **Deferred — Sprint 4.** `.limit(10_000)` on the read added by Story 3-55 review. | DISCIPLINE until Sprint 4. |
| **D107** | **[Renumbered from D97 on merge — same collision as D102 above.] `teachback_attempts` DNA signals computed over unlimited retakes; each retake incurs an uncapped GPT-4o-mini call.** `fuse_learner_dna()` SELECTs all `teachback_attempts` rows for the session. No server-side cap on retake count exists in `service.py`, `schemas.py`, or the DB schema — `UNIQUE (session_id, segment_id, attempt_number)` permits unlimited retakes, and `service.py` increments `attempt_number` without a ceiling. `_compute_signals`'s persistence signal is computed over all retakes; the score used for EMA is the last attempt's score, but the retry-count grouping is distorted by unlimited rows. Additionally, every retake triggers a synchronous GPT-4o-mini call via `score_teachback()` with no per-session cost ceiling (the `$3.00/lesson` ceiling covers pipeline costs, not assessment calls). Fix requires: (1) a `max_teachback_attempts_per_segment` setting enforced in `service.py`; (2) a corresponding DB check constraint or application guard; (3) per-session assessment LLM cost tracking. **Owner: Dev 3. Trigger: before real-student launch (Week 10).** | Med (dual impact: silent wrong DNA persistence signal + unbounded per-session LLM cost with no ceiling) | **Deferred — Sprint 4.** `.limit(10_000)` on the read added by Story 3-55 review. | DISCIPLINE until Sprint 4. |
| **D113** | **[Renumbered from D109 on this merge — collided with this register's own already-established D109 (no durable full-session CES history), independently allocated by `main`'s 2026-08-14 retroactive BMAD review the same day. D113's footprint was a single newly-added row with no other references, so it was the one renumbered per this file's own collision rule.] Narrow residual TOCTOU race survives the S3-58 retry-endpoint concurrency mitigation.** Found by a retroactive 8-layer BMAD review of Stories 3-56/3-57/3-58 (2026-08-14) — the Scale & Load Hunter layer identified that `admin/router.py`'s `retry_job` had no protection against concurrent retries, letting `content_pipeline.py`'s unconditional `clear_lesson_cost()` silently bypass the $3.00 ceiling (the story's own original claim that this was merely "a cost nuisance" was wrong — confirmed a real ceiling bypass, patched same day). The patch rejects a retry if another `lesson_jobs` row for the same `lesson_id` is already `running`/`pending` — but the check-then-act sequence itself (SELECT for concurrent jobs, then UPDATE to pending) still has no lock between them. Two admin retry calls arriving within the same narrow window could both pass the concurrent-check before either write lands, both enqueue, and reproduce the original bypass. The mitigation closes the realistic trigger (an admin re-clicking retry, or a retry racing the original still-finishing run) but not a genuinely simultaneous double-click. | Low (requires two admin retry calls for the identical job within the same DB round-trip; the realistic trigger is already closed) | **Deferred.** A full fix needs either a Postgres advisory lock keyed by `lesson_id` around the check-then-act, or a `UNIQUE` partial index on `lesson_jobs (lesson_id) WHERE status IN ('running','pending')` to reject the second write at the DB level instead of relying on an app-layer pre-check. **Owner: Dev 1. Trigger: before real-student launch (Week 10), or if this exact race is ever observed in production.** | DISCIPLINE until fixed — no automated guard currently proves the race is closed; a concurrency integration test simulating two simultaneous retry calls against a real (not mocked) DB would be the real guard. |

---

### Found by the 2026-08-14 CI reliability investigation (Fly.io migration prep, continued)

| ID | Defect | Sev | Decision | Enforcement |
|----|--------|-----|----------|-------------|
| **D114** | **CLOSED 2026-08-14 — the "Migration tests (gating, real Postgres)" CI step's own D51 anti-vacuum guard could never pass on ANY run, ever, because it treated the expected, by-design "N deselected" summary line as a failure signal.** `ci.yml`'s guard checked `grep -qE "[0-9]+ (xfailed|xpassed|deselected|error)"` and failed the step if matched. But the step runs `pytest tests/integration -q -m postgres`, and marker-filtered pytest runs ALWAYS report a "N deselected" count for every collected test that doesn't carry the `postgres` marker — that line appears on every invocation of this exact command regardless of whether anything is wrong, by pytest's own designed behavior, not as a symptom of a real problem. So this specific step never reached a passing state on any commit, on any branch, for as long as this line existed — masked the whole time by upstream failures (ruff/mypy/pnpm/httpx2/etc., all fixed earlier the same day) that failed the job before this step's own logic was ever actually exercised to completion. Found the moment this step FIRST ran end-to-end with a fully clean 79/79 pass and still failed here. Exactly the pattern the D51 fix itself was written to prevent (a guard reporting success — or in this case, failure — without the underlying signal meaning what the guard assumes), just inverted: a false negative instead of a false positive, on a check that exists specifically to prevent false positives. | **Med** (this is a CI-reliability defect, not a product/runtime defect — no student-facing impact — but it silently made this step un-passable, which meant the postgres-marked migration/RLS suite has effectively never gated a single merge since this guard line was added, defeating D51's own purpose) | **FIXED 2026-08-14.** Removed `deselected` from the failure-pattern regex; `xfailed`/`xpassed`/`error` remain checked (those ARE genuine non-pass outcomes pytest can report, unlike an expected marker-filter side effect). | This step itself, now that it can actually pass: confirmed a real 79/79 green run with the corrected regex reaches the `EXPECTED_PG_TESTS` exact-count check afterward instead of dying first on the false-positive `deselected` match. |

---

### Found by the 2026-08-14 CI/CD repo-wide ruff + mypy cleanup (Fly.io migration prep)

| ID | Defect | Sev | Decision | Enforcement |
|----|--------|-----|----------|-------------|
| **D110** | **CLOSED 2026-08-14 — `process_attention_signal` raised `UnboundLocalError` on every call reaching the TEACHING branch with a valid `session_start_ts`, silently killing fatigue-trigger evaluation (and, on the affected code path, the whole signal) for the entire duration a student spends being taught.** `tutor/service.py`'s "Fatigue trigger (D7, S3-45)" block (added by Story S3-45) is duplicated verbatim, back-to-back, under the identical `if state_raw == "TEACHING" and not intervention_dispatched:` guard. The *second* copy contains `import time as _time` inside the function body — Python resolves local-variable scope statically for the WHOLE function the moment ANY assignment (including an `import ... as` binding) appears anywhere in it, so `_time` was treated as function-local for the entire function, including the FIRST copy's `_time.time()` call, which executes *before* the second copy's import statement ever runs. Any call where `session:{id}:session_start_ts` is set (i.e., any real session after the first signal) hit `UnboundLocalError: local variable '_time' referenced before assignment` at that line. **Confirmed present on `main` at `6224751` independently of this branch** — not introduced by any sprint3-master merge; a genuinely pre-existing, still-live production bug. **Correction after verification — a test DID already catch this, it was simply never individually triaged.** `tests/test_s3_45_fatigue_trigger.py::test_fatigue_detected_dispatched_after_15_minute_floor_with_low_blink_and_head_pose` fails on the pre-fix code with exactly this `UnboundLocalError` (confirmed directly: stashed the fix, re-ran the single test, reproduced the identical traceback, restored the fix). It was one of the ~183 pre-existing failures the `api` CI job's advisory "Full suite" step already reports every run (`continue-on-error: true`) — real signal, sitting in a bucket nobody reads individually, exactly the failure mode BD-3/D24 already describe elsewhere in this file for a different test set. Found incidentally while fixing repo-wide `ruff` violations ahead of turning on required CI status checks for the Fly.io migration — `ruff`'s F823 (undefined-local) rule flagged the second block's `_time` reference, and reading *why* (rather than just deleting to silence the lint) surfaced that this was the real, live cause of an existing red test. | **High** (CES fatigue monitoring — and, per the scoping analysis above, potentially the whole TEACHING-state attention-signal path on any call after the first — has been non-functional for an unknown period on `main` itself, not just this branch; exact blast radius depends on whether the crash is caught and swallowed somewhere upstream of `process_attention_signal`, which was not verified here) | **FIXED 2026-08-14.** Removed the entire duplicate second block (dead/duplicated fatigue-trigger logic plus the scope-poisoning local `import time as _time`), leaving the original module-level-`_time` version (`import time as _time` at `tutor/service.py:13`) as the only implementation. **Owner: Dev 4** (tutor state machine / `process_attention_signal` is Dev 4's module). **Trigger for a follow-up:** confirm whether this same duplication pattern exists on `main` right now and needs its own independent fix/PR there, since this branch's fix only lands on `main` once PR #139 merges. | `tests/test_s3_45_fatigue_trigger.py::test_fatigue_detected_dispatched_after_15_minute_floor_with_low_blink_and_head_pose` — confirmed by direct reproduction (stash fix → same `UnboundLocalError` → restore fix → passes) to be a real, already-existing regression guard for exactly this failure mode: `state_raw == "TEACHING"`, a real `session_start_ts`, asserting the call completes and dispatches. Its only actual gap was never having gated CI (it was absorbed into the ~183-count advisory bucket instead of failing loudly on its own). Plus `tests/test_tutor_service.py` (87 tests) + `tests/test_s3_52_ces_production_hardening.py` + `tests/test_s3_53_ces_production_closure.py` (117 tests total across the four files), all verified passing post-fix. |
| **D111** | **Every recorded intervention event has `window_index=0` and `ces_at_trigger=0.0`, regardless of the real values at trigger time — silently corrupting Dev 3's CES-calibration and intervention-effectiveness analytics.** `tutor/state_machine/graph.py`'s `intervening_node` reads `state.get("window_index")` / `state.get("last_ces")` and passes them to `write_intervention_event`, but nothing ever puts either key into `dispatch_event`'s `input_state`. The two call sites that actually dispatch `distraction_detected`/`fatigue_detected` (`tutor/service.py`, `process_attention_signal`) have the real current window index (`await redis.incr(...)`) and the real current CES value (`v0`) in scope at the call site — they're just never threaded through the dispatch payload. No crash, no test failure: nothing in the test suite asserts a non-zero value for either field, so this has shipped silently since intervention-event persistence was added (S3-36/D12). Found while fixing `mypy`'s errors on `TutorMachineState.get(...)` calls for these two keys — the type-check pass required adding the keys to the `TypedDict`, which is what surfaced that no caller populates them. | **Med** (silent-wrong-result in analytics, not a crash or user-facing symptom — but it invalidates any calibration or effectiveness analysis Dev 3 runs against `session_events.ces_at_trigger`/`window_index` today, since both columns are constant `0`/`0.0` for every row ever written) | **Deferred — not fixed in this pass** (out of scope for a type-annotation cleanup; the `TypedDict` keys were added so mypy could check the existing `.get()` calls, but the actual dispatch-payload wiring was deliberately left alone). **Owner: Dev 3 (CES/analytics) with Dev 4 (dispatch plumbing) — the fix touches both `tutor/service.py`'s two dispatch call sites and `graph.py`'s `dispatch_event`/`intervening_node`. Trigger: before any CES-calibration or intervention-effectiveness analysis is run against real `session_events` data.** | DISCIPLINE — no test currently asserts non-zero `window_index`/`ces_at_trigger` on a persisted intervention event; writing one would both document the intended contract and catch a regression once fixed. |
| **D112** | **Dead code: `getattr(resp, "error", None)` checks in `assessment/service.py`'s onboarding rollback-logging paths can never be truthy with the installed `postgrest-py` (2.31.0) — `APIResponse` only exposes `.data`/`.count`; failed `.execute()` calls raise `postgrest.exceptions.APIError` instead of returning a response with a set `.error` field.** Two sites (`process_onboarding`'s rollback-of-`onboarding_responses` paths, ~line 1337 and ~line 1388) check `if getattr(del_resp, "error", None):` after a successful `.execute()` inside a `try` — on success this is always `None`/falsy (no `.error` attribute exists to `getattr`), and on failure the surrounding `except Exception` catches the raised `APIError` before this line is ever reached. Effect is minor: the specific-error log message is unreachable, so a rollback failure always falls through to the generic `except Exception` handler's "(network/client error)"-style message instead of whatever `.error` would have said — logging fidelity only, not a functional gap (the rollback failure is still logged and still non-fatal either way). **Confirmed pre-existing on `main`** (`git show main:apps/api/app/modules/assessment/service.py` has the identical `getattr(del_resp, "error", None)` / `.error` pattern) — not introduced by this branch. Found during the same `mypy` cleanup pass (`"APIResponse" has no attribute "error"` under strict checking) that surfaced D111. | **Low** (log-message quality only; the actual rollback-failure handling path is intact via the outer `except Exception`) | **Deferred.** The two sites were mechanically changed to store `getattr(resp, "error", None)` in a local variable before the `if`/log call (fixes the mypy error, zero behavior change) — the dead-code condition itself was left as-is rather than removed, since removing it changes log wording/structure and this pass was scoped to type-checking, not behavior. **Owner: Dev 3. Trigger: any `postgrest-py` upgrade, or if this rollback-logging path is ever touched for an unrelated reason** — worth removing the dead `.error` branch and just logging the caught exception directly at that point instead. | DISCIPLINE — no test exercises this exact branch's log-message content either way. |

---

### Found by the 2026-08-17 BMAD 6-agent code review of Story 2-47 (fold Library into Books)

| ID | Defect | Sev | Decision | Enforcement |
|----|--------|-----|----------|-------------|
| **D115** | **The embedded `lessons` relation in `content/router.py`'s chapter-list query is unbounded and was mis-cited as tracked under D59 — it is not, and the CI guard built to catch this class has a marker-scoping blind spot that lets it through.** `GET /books/{book_id}/chapters` (`router.py:1030-1044`) selects `chapters` with an embedded `lessons!lessons_chapter_id_fkey(lesson_id,status,tier,created_at)` relation carrying no `.limit()`/`.range()` on the embedded side — every lesson row ever created for every chapter is fetched and deserialized on every request. Story 2-47 added a client-facing `_MAX_LESSONS_EXPOSED = 20` output cap (real, correctly implemented — `lesson_count` still reports the true total past it) but capped the *response*, not the *query cost*: the full unbounded row set is still fetched, sorted, and held in memory before the cap is applied. Story 2-47's own code comments and Scale & Load section cite "D59" as the tracking ID for this gap — **verified false**: D59 (this file, above) covers only `admin/router.py` (closed) and `analytics/service.py` (open, Dev 3's); it has never named `content/router.py` or `_CHAPTER_COLUMNS`. Confirmed the CI guard does not catch it either: `test_unbounded_queries.py`'s `_unbounded_selects()` run against the real file returns `[]` — the `# BOUNDED: <= 80 rows, enforced by the chapter-detection gate` marker on `router.py:1036` is written to justify the outer `chapters` row count, but the guard exempts the whole `.select()` statement it sits above, including the embedded `lessons` relation the very next comment line admits is not bounded by that argument. Reachable trigger: Gate 5's idempotency pre-check (D45) only blocks a retry while an existing `(chapter_id, tier)` lesson is `generating`/`ready`, never `failed` — a single user repeatedly retrying a failing tier accumulates one row per retry with no ceiling anywhere in this flow, reaching >20 rows on one chapter in roughly 75 minutes at the current `3/minute;20/hour` per-user rate limit (faster across N replicas, since D49's `RATE_LIMIT_STORAGE_URL` defaults to `memory://` and multiplies every ceiling by replica count). Found by the Scale & Load Hunter layer of Story 2-47's `/bmad-code-review`, 2026-08-17. | Med (Scale Contract Q4 — unbounded read on a request path; not yet observed to cause an incident, but the CI guard meant to catch this class currently reports it as covered when it is not) | **Registered here, not fixed.** The client-side cap Story 2-47 shipped is real and correctly bounds what the browser receives; the query-level fix (adding `.order().limit()` to the embedded side, or restructuring the embed to a bounded RPC) is a bigger change with its own owner and is out of scope for a frontend-led story. Story 2-47's code comments (`router.py`'s `_MAX_LESSONS_EXPOSED` docstring, `schemas.py`'s `lessons` field comment) and its story file's Scale & Load section have been corrected to cite **D115**, not D59. **Owner: Dev 1. Trigger: the first chapter observed with >20 lesson rows in production, or when `test_unbounded_queries.py`'s marker-scoping leniency (the same shape D103 already fixed once for `dna_fusion.py`) is generalized to also check embedded/nested relations, not just top-level `.select()` chains.** | `tests/unit/test_unbounded_queries.py` — currently does **not** guard this (confirmed by direct run, that is the finding); closing this entry requires either extending `_unbounded_selects()` to inspect embedded relations independently of the outer statement's `# BOUNDED:` marker, or a dedicated test asserting the embed carries its own limit once the query-level fix lands. |

---

## Scorecard

| | Count |
|---|---|
| Defects closed (fixed **and** guarded) | **56** — plus **D114** (the postgres migration-tests CI step's own D51 guard could never pass on any run, ever, due to a false-positive `deselected` match — closed 2026-08-14) and **D110** (`process_attention_signal` UnboundLocalError on every TEACHING-state call with a real `session_start_ts`, confirmed present on `main` independently of this branch, found via ruff's F823 rule during the 2026-08-14 CI cleanup, closed same-day) — on top of the 53 now recorded on `main` (which itself grew by one since the last sync: **D105** closed 2026-08-14, confirmed a stale duplicate of D93's already-FIXED-GUARDED session_count race — see D105's row) plus **D55** (`lesson_package` read-through, closed 2026-08-12 on `sprint3-master` — found live by a real full-lesson playthrough — merged into this count here since `main` never had this fix on its own timeline). Full `main`-side list beyond D105: D104 (SessionCreate UUID validation, renumbered from D94), D103 (CI scanner gap for dna_fusion.py, renumbered from D93), D102 (unbounded session_events SELECT, renumbered from D92), D72 (stale HIE brand), D71 (onboarding LLM lock) all closed 2026-08-13 via Story 3-55. D102/D103/D104 were registered and closed same-day, then renumbered from D92/D93/D94 on merge (2026-08-14) after colliding with this register's own independently-assigned D92-D94. D91 (reaper uses a real `started_at`, D53's own deferred follow-up, triggered by a live false-positive reap) closed 2026-08-13 via Story 3-54; D90 (caption overlay) + D88 (slide overflow CSS fix) closed 2026-08-13 via `sprint3/s3-53-d88-d90-player-ui-fixes` (Dev 2's normal territory, implemented directly with root causes already identified and flagged); D89 (Sarvam narration pace) closed 2026-08-13 via `sprint3/s3-52-d89-sarvam-pace`; D59(a) (bounded the admin cost-report query, D59(b)/analytics still open, Dev 3's) closed 2026-08-13 via `sprint3/s3-51-d59a-admin-cost-bounded`; D54 (`force=true` lesson regeneration, D53's own escape hatch) closed 2026-08-13 via `sprint3/s3-50-d54-force-regenerate`; D87 (slide budget re-sized to a real duration target, closing the D85 follow-up — **note: this D87 is the Story 3-49 slide-budget meaning; `main` separately and independently allocated D87 a second time to the Demo T28 reassessment-bypass race, an unresolved internal collision on `main`'s own side, not renumbered by this merge — disambiguate by section heading, not by bare id**) closed 2026-08-13 via `sprint3/s3-49-d87-slide-budget-duration-target`, which also promotes D85 itself from partial to fully closed (both rounds now landed); D53 (stale `generating` lesson reaper — the query-level workaround already existed; built the actually-missing reaper half) closed 2026-08-13 via `sprint3/s3-48-d53-stale-lesson-reaper`, the only defect in the register flagged both High and live-in-production, picked as the explicit next priority; D86 (persist real accumulated cost to `lesson_jobs.cost_usd`) closed 2026-08-13 via `sprint3/s3-47-cost-persist-lesson-jobs`, found auditing the L1 handoff's own "record cost per lesson" requirement against real data; D78 (narration cap re-sized to a real cost safety net, not a duration target) closed 2026-08-13 via `sprint3/s3-45-narration-cap-safety-net`, found inspecting the first real successful demo lesson; D77 (lesson_planner per-batch echo retry — **this is `main`'s own D77; see the "renumbering" note below for the collision with this register's separate D109**) closed 2026-08-13 via `sprint3/s3-44-planner-batch-retry`, found running D75's own fix for real; D75/D76 (lesson_planner batch reliability, narration cap for a real 15-min demo — **`main`'s own D75/D76; these ALSO collide with this register's already-established D75/D76 further above (ES256 rate-limit key, proxy beta-gate test fix, renumbered from `sprint3-master`'s D64/D65) — a second, unrelated internal collision on `main`'s own side that predates and is independent of this merge, not renumbered here for the same reason as D87 above: too many already-shipped code comments and docs on `main` reference these specific numbers under this meaning; disambiguate by section heading**) closed 2026-08-13 via `sprint3/s3-43-demo-readiness-fixes`, merged to `main`; D74 (Sarvam chunking/decoding — **also collides with the established D74 above (PdfBookmark TOC crash); same treatment**) closed 2026-08-13 via `sprint3/s3-42-...`, merged to `main`; D63 (INTERVENING one-way trap, Dev 4) closed 2026-08-11 in two rounds on `sprint4/s4-6-intervention-recovery`, merged (PR #129, `1f0eba9`) — this row previously said "not yet merged here," confirmed stale and corrected 2026-08-13 by independent branch-triage verification; D32/D33/D63 (Story 3-36, Dev 1's own D63) and D68 (formerly D64, tutor FSM tracing) closed 2026-08-11/12 via `sprint3/s3-40-...`, merged here; D67 closed 2026-08-12 via `sprint3/s3-41-...`, merged here; D31, D48, D62 closed 2026-08-11 via `sprint3/s3-35-env-config-fixes`, merged here. |
| Fixed, awaiting merge | **0** — everything now on `sprint3-master` |
| **Open** | **34** — plus **D111** (intervention events always record `window_index=0`/`ces_at_trigger=0.0`, Dev 3+4, found via the 2026-08-14 mypy cleanup), **D112** (dead `.error`-attribute check in onboarding rollback logging, Dev 3, pre-existing on `main`, same cleanup), and **D113** (renumbered from `main`'s own newest D109 — narrow residual TOCTOU race in the S3-58 retry endpoint, Dev 1, registered 2026-08-14 from `main`'s own retroactive BMAD review; collided with this register's already-established D109 below, see D113's row for the renumbering note) — on top of the 30 now open on `main`'s own count (below, down from 31 last sync since D105 closed) plus **D109** (no durable full-session CES history beyond the last 10 Redis-capped windows, Dev 3, found while scoping Story 2-46 — renumbered from this branch's own D77 on an earlier merge to avoid colliding with `main`'s already-closed, already-widely-referenced D77 above). `main`'s own 30: **D64** (confusion-type interventions uncapped, Dev 4, deferred), D69/D70 (no span hierarchy, no `user_id` attribution), **D79–D84** (Story 4-26 review deferred findings, Dev 4, registered 2026-08-13), **D106** (quiz_attempts DNA accuracy over retake history, renumbered from D96, Dev 3, deferred Sprint 4, registered 2026-08-13 Story 3-55 review), **D107** (teachback_attempts unlimited retakes + uncapped LLM cost, renumbered from D97, Dev 3, deferred Sprint 4, registered 2026-08-13 Story 3-55 review), **D66** (Dev 2, MediaPipe model tag pin), and **D59** (part (b), `analytics/service.py`, Dev 3's — part (a) closed 2026-08-13). **D105 closed 2026-08-14** as a stale duplicate of D93 — no longer counted here. **D85 and D54 both closed 2026-08-13** — no longer counted here. D88–D91 were opened and closed the same day (2026-08-13) and were never counted in this total. D71 and D72 closed 2026-08-13 (Story 3-55). D102/D103/D104 (renumbered from D92/D93/D94) opened and closed same-day (2026-08-13, Story 3-55) — never counted in Open. |
| Of which **live in production** | **0** — **D71 closed 2026-08-13 (Story 3-55)**; code fix landed Story 3-54, register entry closed Story 3-55 after verification. **D53 closed 2026-08-13 (Story 3-48)** — was the other entry on this line; D29 was also stale here (already closed 2026-08-05, see its own row) and is corrected out of this count too. D18/D35 closed 2026-08-04 on `main`; D34 closed 2026-08-04 by book-scale Phase 6.5; D31 closed 2026-08-11 (Story 3-35); D55 closed 2026-08-12 on `sprint3-master`. |
| Of which **self-inflicted 2026-07-29** | **0** — all six resolved (5 fixed, D15 rejected as a wrong finding) |
| Of which **found by the 2026-07-29 cross-team Sprint 2 completion audit** | **2** (D29, D30) — `docs/sprint2-completion-audit-2026-07-29.md`; **D29 closed 2026-08-05, D30 closed 2026-08-04** |
| Binding decisions relying on `DISCIPLINE` alone | **6 of 9** — BD-9 (scale) joins them until `tests/unit/test_unbounded_queries.py` is written |
| Open entries with a named owner **and** a trigger | **30 of 31** (D36 still has no owner; D61's owner is whoever fixes `.env.example`; D79–D84 all have owners and triggers — Dev 4; D87 owner Dev 4; D109 owner Dev 3; D105's closure 2026-08-14 removed it from this count) |
| ⚠️ **Unreconciled internal collisions on `main`'s own history, found during this merge, not fixed by it** | **D74, D75, D76, D87 each carry two unrelated meanings on `main` alone** (PdfBookmark/ES256/proxy-beta-gate vs. Sarvam-chunking/planner-batch-size/narration-cap for D74–D76; slide-budget-duration-target vs. reassessment-bypass-race for D87) — pre-existing, not introduced by this merge, and not mechanically renumbered here because both meanings of each already have shipped code comments and story docs on `main` referencing the bare number. Disambiguate by section heading until `main`'s own owners reconcile this properly. `main`'s D77 (planner-batch-retry) additionally collides with THIS register's separate `D109` (renumbered here from this branch's own D77, CES-history-Redis-cap) — that one **was** renumbered, since D109's footprint was small and fully within this branch's control. |
| Found by the 2026-07-30 wiring audit | **7** registered (D31–D37), ~40 more in the report |
| Found by the 2026-07-30 topology review | **1** (D62 — see below; renumbered twice on merge, from D38 then D61, both already taken independently on `main` by the time this branch merged) — see `docs/decisions/ADR-001-india-region-migration-topology.md` |
| Found by the 2026-08-13 Story 4-26 six-layer review | **6** registered (D79–D84): IDOR+force+WS delivery, concurrent double-fire race, MemorySaver scope undocumented, full LessonPackage JSON deser per call (pre-existing), null response fields silent-wrong-result, intervention message always index 0 (pre-existing) |
| Found by the 2026-08-13 Demo T28 six-layer review | **1** registered (D87): reassessment bypass non-atomic 3-step Redis race (pre-existing in `router.py`) |
| Found by the 2026-08-03 Story 1-9 five-agent review | **3** registered (D38–D40); **D38 and D39 both closed same day**|
| Found by the 2026-08-04 book-scale UI re-plan and Phase 3 gate | **2** registered (D41, D42) |
| Found by the 2026-08-04 Story 1-14 review (Phase 6 endpoints) | **11** registered (D44–D54), plus **D34 amended and re-severitied Med → High**. D52/D53/D54 were added by the five-agent review AFTER the story was written — **D52 was cited by ID in three files, including a load-bearing code comment, before its register row existed**, which is precisely the collision the banner at the top of this file exists to prevent |

That last row is the honest health metric. Six of nine rules currently depend on someone
remembering. **BD-3 is the one that converts the most of them into machine checks, which is
why it is first.**

