# Story 2-1b — Phase 1 Economy Node Checkpoint/Idempotency

**Status:** done — reviewed (3-layer adversarial review 2026-07-13), all 11 patch findings applied including a critical IDOR fix; 2 pre-existing/out-of-scope items deferred (see `docs/stories/deferred-work.md`)
**Sprint:** 2
**Owner:** Dev 1
**Branch:** `sprint2/phase-b-generation-nodes` (shared Sprint 2 branch)
**Source:** Deferred from Story 2-1's code review (2026-07-13) — see `docs/stories/2-1-phase1-economy-nodes.md` Review Findings and `docs/stories/deferred-work.md`

## Context

Story 2-1 made `summarise_segment_node` and `segment_complexity_node` real (billed OpenAI calls), fanned out once per section via `Send()`. Unlike the four Phase A nodes (`extract`, `structure`, `chunk`, `embed`), which each check `lesson_jobs.node_outputs[node_name]` before doing work and skip on a cache hit, none of the six Phase 1 economy nodes have any idempotency guard. Before Story 2-1 this was free (they were stubs); now a worker crash mid-fan-out means an ARQ retry silently re-runs and re-bills every already-completed section's LLM call.

This also blocks S2-3 through S2-6 (the remaining four economy nodes) from inheriting the same gap — better to design the checkpoint pattern once, here, before they're built.

## Acceptance Criteria

1. Each of the 6 economy nodes checks for a per-section completion record before calling its provider — e.g. `node_outputs[f"{node_name}:{section_id}"]` in the existing `lesson_jobs.node_outputs` JSONB column — and returns the cached result on a hit instead of re-calling the LLM.
2. Each node writes its per-section result to that same key on success, following the existing Phase A checkpoint-write pattern (see `embed_node`'s checkpoint write for the established convention).
3. A simulated ARQ retry (job cancelled after 2 of 3 sections complete for a node, then re-invoked) results in exactly 0 additional LLM calls for the 2 already-completed sections and exactly 1 call for the remaining section.
4. Progress visibility during Phase 1 fan-out: `lesson_jobs` (or a Redis-backed alternative, given `progress_pct` can't be a shared graph-state channel under concurrent `Send()` writes — see Story 2-1's Review Findings) reflects how many of the `6 × N` dispatched calls have completed, not just the pre/post Phase-1 progress percentages already in place.
5. Existing Story 2-1 tests (`test_phase1_economy_nodes.py`) continue to pass with node **behavior** unchanged when no cache hit exists — this story only adds idempotency, it does not change what the nodes produce. **Correction during implementation:** "unmodified" (as originally written) turned out to be unachievable — those tests never mocked Supabase/Redis (no dependency on either existed yet), so adding the checkpoint read/write and progress counter required adding an `autouse` fixture that mocks both as a permanent cache-miss/no-op. The tests' *assertions* and *intent* are unchanged; their *mocking setup* needed updating for the new dependency.

### Review Findings (2026-07-13 — 3-layer adversarial review: Blind Hunter, Edge Case Hunter, Acceptance Auditor)

- [x] [Review][Patch] **CRITICAL: `merge_lesson_job_node_output` has no access control — cross-tenant IDOR / RLS bypass** — Postgres grants `EXECUTE` on new functions to `PUBLIC` by default, and Supabase/PostgREST auto-exposes every `public`-schema function as a `/rest/v1/rpc/` endpoint. Any caller who can reach that endpoint can pass an arbitrary `p_lesson_id` and overwrite another user's `lesson_jobs.node_outputs` — e.g. inject a fake "completed" checkpoint into someone else's lesson so Phase 1 silently skips real LLM calls, or corrupt `package_builder`'s later JSONB assembly. `CLAUDE.md` §18 requires RLS on all Supabase tables; this function routes around that entirely. [`supabase/migrations/20260713020000_lesson_job_node_output_merge_fn.sql`] (Blind Hunter)
- [x] [Review][Patch] **Mutable `search_path` — function hijack risk** — the function references bare `lesson_jobs` with no `SET search_path` / schema qualification. A same-named shadow table/view earlier in the caller's `search_path` would silently redirect the write. [`supabase/migrations/20260713020000_lesson_job_node_output_merge_fn.sql`] (Blind Hunter)
- [x] [Review][Patch] **Silent no-op write when the `lesson_jobs` row doesn't exist — checkpoint loss with no error** — `UPDATE ... WHERE lesson_id = p_lesson_id` with 0 matching rows returns success with 0 rows affected, no exception. If Phase 1 fan-out ever races ahead of the row insert, the checkpoint write silently does nothing — the LLM call is billed but the checkpoint is lost, so a retry re-bills it, defeating the story's entire purpose. [`supabase/migrations/20260713020000_lesson_job_node_output_merge_fn.sql`] (Blind Hunter + Edge Case Hunter, independently)
- [x] [Review][Patch] **`.single()` on checkpoint read raises hard on a missing row — crashes the node instead of degrading** — contradicts `_read_phase1_checkpoint`'s own docstring claim that a missing value "just means an occasional redundant LLM call, never lost data." A missing `lesson_jobs` row produces an unhandled exception, not a graceful cache-miss. [`graph.py::_read_phase1_checkpoint`] (Blind Hunter + Edge Case Hunter, independently)
- [x] [Review][Patch] **Cache-hit path trusts unvalidated stored JSON** — only guard is `if cached is not None`; nothing checks the cached dict actually has the expected keys (`summary`, or the 7 `SegmentComplexity` fields) before forwarding it to `lesson_planner`. A malformed or schema-drifted checkpoint propagates downstream with no validation, unlike the fresh-computation path (which validates via Pydantic). [`graph.py::summarise_segment_node`, `segment_complexity_node`] (Blind Hunter + Edge Case Hunter, independently)
- [x] [Review][Patch] **Progress counter double-counts on ARQ retry, contradicting AC-4's own intent** — `_increment_phase1_progress` runs on both the cache-hit AND real-completion paths, and the Redis key is never reset on retry. On the exact AC-3 retry scenario (2 of 3 sections already checkpointed), re-invoking all 3 re-increments the counter for the 2 cached sections *in addition to* their original increment before the crash — the counter overcounts past the true total. The diff's own test (`test_cache_hit_also_increments_progress_counter`) locks this in as "correct" rather than catching it. [`graph.py::_increment_phase1_progress`] (Blind Hunter + Edge Case Hunter + Acceptance Auditor — three-way convergence)
- [x] [Review][Patch] **No error handling around the Redis progress call — can crash an already-billed, already-checkpointed dispatch** — unlike the established `_update_job_progress` convention (which wraps its write in try/except so a non-critical progress write can never abort the pipeline), `_increment_phase1_progress` has no guard. A transient Redis failure raises AFTER the LLM call succeeded and the Postgres checkpoint was written — crashing that dispatch for a purely cosmetic metric. [`graph.py::_increment_phase1_progress`] (Blind Hunter + Acceptance Auditor, independently)
- [x] [Review][Patch] **Progress denominator (`6×N`) doesn't match the 2-of-6 nodes actually instrumented** — `_total_sections = len(sections) * len(_ECONOMY_NODES)` uses the full 6-entry list, but only 2 nodes call the counter in this diff. The logged "X/6N" figure can never exceed ~33% until S2-3..S2-6 land. [`graph.py::_fan_out_phase1_economy_nodes`] (Blind Hunter + Acceptance Auditor, independently)
- [x] [Review][Patch] **No test exercises actual concurrency, despite that being the stated justification for the atomic RPC** — `TestSimulatedRetry` calls the node three times sequentially via plain `await`, never via `asyncio.gather`. The lost-update race the RPC exists to prevent is asserted in a comment, not demonstrated by a test. [`test_phase1_checkpoint_idempotency.py::TestSimulatedRetry`] (Blind Hunter)
- [x] [Review][Patch] **Dead mock scaffolding in `_make_jobs_table`** — sets up `t.update...` return values that production code never calls (checkpoint writes go through `.rpc()` exclusively) — harmless now, but will bit-rot into a false-negative trap if a client-side `.update()` write path is ever reintroduced. [`test_phase1_checkpoint_idempotency.py::_make_jobs_table`] (Blind Hunter)
- [x] [Review][Patch] **Test comment mis-cites AC-5** — `mock_jobs_table.update.assert_not_called()` is annotated "AC-5" but the atomic-merge-only requirement is actually the Dev Notes' rationale under AC-2. Misleads a future AC-to-test traceability audit. [`test_phase1_checkpoint_idempotency.py::TestCheckpointWriteOnSuccess`] (Acceptance Auditor)
- [x] [Review][Defer] **Synchronous Supabase client called inside async nodes — blocks the event loop under concurrency** — every Phase A node already does this (no `asyncio.to_thread` wrapping anywhere in `graph.py`); this diff doesn't introduce the pattern, but is the first place `6×N` real concurrent dispatches make it actually matter (serializing what should be parallel I/O). Pre-existing project-wide pattern, not fixable in one story — flag for a dedicated async-client migration story. [`graph.py::_read_phase1_checkpoint`, `_write_phase1_checkpoint`] (Blind Hunter)
- [x] [Review][Defer] **Same-key duplicate-dispatch race — the read isn't atomic with the write** — two concurrent dispatches for the SAME checkpoint key could both see a cache-miss before either writes, both calling the LLM (duplicate cost, not data loss). Shouldn't occur under the normal crash-then-sequential-retry model this story targets; would need a distributed lock to close fully, which is real added scope. Deferred — the docstring's "occasional redundant call, never lost data" claim is being corrected (see the missing-row finding above, which showed there IS a lost-data path) rather than promising full protection against this race. [`graph.py::_read_phase1_checkpoint`, `_write_phase1_checkpoint`] (Edge Case Hunter)

## Dev Notes

- `MemorySaver` (the project's mandated checkpointer, `PostgresSaver` banned per `CLAUDE.md`) is in-memory only and lost on a worker crash — the per-section idempotency guard must live in `lesson_jobs.node_outputs` (durable, Supabase-backed), the same store the four Phase A nodes already use, not in LangGraph's own checkpoint.
- `progress_pct` cannot be written by the economy nodes directly (see Story 2-1's fan-out-safe stub pattern — concurrent writes to a non-reducer `PipelineState` key raise `InvalidUpdateError`). Any progress-visibility mechanism here needs its own channel (e.g. a Redis hash keyed by lesson_id, incremented per completed dispatch) rather than the `PipelineState` graph channel.
- Scope boundary: this story does not implement S2-3 through S2-6's actual generation logic — only retrofits the checkpoint pattern onto whichever economy nodes exist by the time this lands (currently 2 of 6; more may exist by the time this is picked up).
