# Story BR-5 — Deploy-time required-secrets verification (closes D146's residual gap)

Status: ready for review (implementation complete; 6-agent `/bmad-code-review` gate not yet run — see CLAUDE.md's BMAD Code Review Gate before merge)

## Story

As Dev 4 (picking this up opportunistically — it is not squarely WS/JWT/tutor-FSM domain, but
surfaced directly out of BR-2's review chain and D146's investigation),
I want the deploy pipeline to verify, before `flyctl deploy` runs, that every secret the app's own
startup guards require is actually present on the target Fly app,
so that a correctly-implemented fail-loud guard (like D49's) never again produces a silent,
multi-hour production crash-loop (D146) that nobody notices until Fly's retry budget is already
exhausted.

## Background

- **D49** (closed, PR #159/Story 5-4): added `assert_rate_limit_storage_configured()` —
  correctly raises `RuntimeError` at boot outside debug mode if `RATE_LIMIT_STORAGE_URL` resolves
  to in-process `memory://` storage.
- **D146** (open, logged 2026-09-03): the guard fired exactly as designed on every boot attempt
  after PR #159 deployed, because `RATE_LIMIT_STORAGE_URL` was never set as a live Fly secret.
  Both `hie-api` machines (`api` and `worker`) crash-looped for ~3 hours (02:51–05:58 UTC) before
  anyone noticed — Fly's `on-failure` policy exhausted `max_retries: 10` and gave up silently (no
  alert exists; W10-2 monitoring is not built). D146's own disposition names the gap this story
  closes verbatim: *"nothing currently diffs 'secrets a story's code depends on' against 'secrets
  actually deployed.'"*
- **D145** (open, separate but same class): `hie-api`'s live Fly region list silently drifted from
  `fly.toml`'s committed `primary_region = "bom"` — a different instance of the same root problem
  (live Fly configuration state diverging from what the repo assumes, with nothing to catch it).
  This story does not fix D145 (region drift isn't a "secret," `flyctl secrets list` doesn't see
  it) but is worth naming as the sibling gap in the same family.

The guard itself is not the defect. Fail-loud-at-boot is the correct pattern (CLAUDE.md's own
"silent truncation/degradation is never acceptable" rule) and this story does not touch it. The
defect is that **nothing verifies, before a deploy is attempted, that the environment a guard
depends on is actually configured** — so "the code is correct" and "the deploy will succeed" are
two different, unverified claims currently conflated.

## Acceptance Criteria

1. A single, anti-drift source of truth for "secrets required for a non-debug `hie-api` boot"
   exists in `scripts/check_fly_secrets_configured.py`, built from two sources:
   - Every `pydantic.Field` in `app.config.Settings` with no default, introspected live via
     `Settings.model_fields` — never hand-copied, so a future story adding a new required
     `Settings` field is automatically covered with no separate list to remember to update.
   - Every env var gated by a custom fail-loud startup assertion whose value has a *non-raising*
     pydantic default (so pydantic alone would never catch it) — currently exactly one:
     `RATE_LIMIT_STORAGE_URL` (`assert_rate_limit_storage_configured`, D49). Hand-listed, backed
     by a source-scan test (AC5) proving the name still appears in that guard's own module, so
     the two can't silently drift apart the way the underlying app code already did once.
2. `scripts/check_fly_secrets_configured.py --app <name>` calls `flyctl secrets list --app <name>
   --json` (names only — `flyctl` never exposes secret values, and this check only ever needs
   presence, so no secret material touches CI logs), diffs required vs. present, and:
   - exits `0` with a clear confirmation line if every required secret's name is present;
   - exits `1` and prints every missing secret's name plus which guard/field requires it if
     anything is absent;
   - exits `1` with a distinguishable error message (never a silent pass) if `flyctl` itself
     fails, times out, or returns unparseable output — "couldn't verify" must never be reported
     as "verified OK."
3. `.github/workflows/deploy.yml` runs this check as its own step, using the already-configured
   `FLY_API_TOKEN` secret, **before** the `flyctl deploy --remote-only` step — so a deploy with a
   missing required secret fails the GitHub Actions job loudly, before Fly ever attempts to boot
   the app and burns its retry budget the way it did for D146.
4. The pure diff logic (`list_missing_secrets`) is unit-tested directly (empty / partial / full
   coverage) without shelling out to a real `flyctl` process.
5. A guard test proves the two anti-drift claims in AC1 actually hold:
   - the pydantic-derived portion of the manifest is generated by calling
     `Settings.model_fields` at test time and comparing to what the script reports (not a
     hand-copied list that could go stale the next time a required field is added or removed);
   - `RATE_LIMIT_STORAGE_URL` is confirmed present, by source scan, in
     `app.core.rate_limit`'s guard — so if a future refactor renames that env var in one place
     and not the other, this test reddens instead of the drift going unnoticed a second time.
6. `docs/DEFECT-REGISTER.md` gets a new entry — **D150** — documenting this fix, explicitly citing
   D146 (the incident that surfaced the gap) and D145 (the sibling live-config-drift class), per
   binding rule 5 (a documented limitation needs its own register ID, not just prose inside
   another entry's disposition). Per binding rule 7, the CI step (AC3) is what makes this
   `FIXED`, not `FIXED-UNGUARDED` — the register entry must cite it, not just the script's
   existence.

## Scale & Load

1. **What is ONE unit of work, and what is its range?**
   One unit is one CI-triggered deploy attempt against one Fly app (`hie-api`, `fly.toml`'s only
   declared app — ADR-001 §3 explicitly rejects splitting into multiple Fly apps). Range: exactly
   one app, one secrets list, one check, once per deploy — not a per-user or per-request
   operation. `flyctl secrets list` for this app currently returns a small, bounded set (the 6
   pydantic-required fields + 1 guard-enforced var = 7 names today; realistically low tens even
   after several more stories add required config). This is not a scale-sensitive path.

2. **Which budgets are FIXED while the input VARIES — and what happens past them?**
   N/A — no fixed-size budget applies here. The required-secrets manifest grows exactly as fast
   as `Settings.model_fields` and the hand-listed guard set grow; nothing is truncated or capped.
   The one thing that must never silently degrade is the *check itself* failing open: if `flyctl`
   errors, times out, or returns something unparseable, AC2 requires an explicit `exit 1`, never a
   quiet pass — this is the one place this story's own logic could reproduce D146's shape
   (a check that looks like it ran but didn't actually verify anything), so it's covered directly
   by AC2/AC4, not left as an assumption.

3. **What is the SCOPE of every limit — per user, per instance, or per deployment?**
   Per deployment. One Fly app, one secrets namespace (`fly secrets` are app-scoped, not
   per-machine/per-process — `api` and `worker` share the same secret set per `fly.toml`'s single
   `[build]`/two-`[processes]` layout), one CI check per deploy attempt. No per-user or
   per-instance dimension exists in this problem.

4. **Which reads and writes are UNBOUNDED?**
   None. `flyctl secrets list --app hie-api --json` returns one bounded JSON array (secret names
   only, no values, no pagination — Fly's own secrets API has no documented case of an
   unbounded/paginated response for a single app). No Supabase read or write is introduced;
   `tests/unit/test_unbounded_queries.py`'s source scan is unaffected by this story.

5. **Which caps were INHERITED from an earlier design, and have they been re-derived?**
   N/A — this is a new mechanism with no prior cap to inherit or re-derive.

6. **Is every check-then-act sequence safe under CONCURRENT requests?**
   Yes, by inheritance from an existing guarantee, not a new one: `deploy.yml` already declares
   `concurrency: { group: deploy-hie-api, cancel-in-progress: false }`, which serializes every
   deploy attempt against this app (documented in the existing workflow file, unrelated to this
   story). This story's new step runs inside that same serialized job, so there is no new
   check-then-act race between "verify secrets" and "deploy" — they are two sequential steps in
   one already-serialized workflow run, not two independent processes that could interleave.

## Tasks / Subtasks

- [x] **Task 1 — `scripts/check_fly_secrets_configured.py` (AC: #1, #2)**
  - [x] `required_settings_fields()` — introspects `app.config.Settings.model_fields`, returns the
    env var names (uppercased) of every field with no default.
  - [x] `_GUARD_ENFORCED_SECRETS` — hand-listed dict for `RATE_LIMIT_STORAGE_URL` only, citing
    D49/D146 in its value string.
  - [x] `required_secrets()` — merges both sources into one `{name: reason}` manifest.
  - [x] `list_missing_secrets(required, configured_names)` — pure diff, no I/O, directly testable.
  - [x] `_fetch_configured_secret_names(app)` — shells out to `flyctl secrets list --app <app>
    --json`, returns the set of names; raises a distinguishable `RuntimeError` (not a silent
    empty set) on any non-zero exit or unparseable output.
  - [x] `main()` — wires the above, prints an actionable per-secret message on failure (cites
    D150, tells the operator the exact `flyctl secrets set` command), exit code `0`/`1` per AC2.

- [x] **Task 2 — Wire into the deploy workflow (AC: #3)**
  - [x] New step in `.github/workflows/deploy.yml`, before `flyctl deploy --remote-only`, running
    `python scripts/check_fly_secrets_configured.py --app hie-api` with `FLY_API_TOKEN` in env
    (same secret the existing deploy step already uses).
  - [x] Confirm the step actually blocks the job on failure (`flyctl deploy` must not run if this
    step exits non-zero) — GitHub Actions' default sequential-step failure behavior, verify no
    `continue-on-error` is accidentally inherited from anywhere in the file.

- [x] **Task 3 — Tests (AC: #4, #5)**
  - [x] `apps/api/tests/test_check_fly_secrets_configured.py` (script lives under `scripts/`, not
    a package — load by `importlib` from file path, mirroring `test_ws_load_test.py`'s existing
    pattern for `scripts/ws_load_test.py`).
  - [x] `list_missing_secrets` — empty configured set (all required missing), full set (none
    missing), partial overlap.
  - [x] Anti-drift test: call `required_settings_fields()` for real against the real
    `app.config.Settings` and assert it's a non-empty set containing at minimum
    `SUPABASE_URL`/`OPENAI_API_KEY` (spot-checks the introspection actually walks real fields,
    not an empty/broken import) — not a fixed hand-typed list that could itself drift from
    `config.py`.
  - [x] Source-scan test: `RATE_LIMIT_STORAGE_URL` (the literal string) is present in
    `apps/api/app/core/rate_limit.py`'s source — reddens if a future rename of that env var in
    the guard isn't mirrored in this script's `_GUARD_ENFORCED_SECRETS`.
  - [x] `_fetch_configured_secret_names` / `main()` — mock `subprocess.run` (never invoke a real
    `flyctl` in the unit suite); cover the non-zero-exit and unparseable-JSON paths explicitly,
    asserting each raises/exits loud rather than returning an empty "looks fine" set.

- [x] **Task 4 — Register + tracker close-out (AC: #6)**
  - [x] `docs/DEFECT-REGISTER.md` — new **D150** entry, citing D146/D145, this story's branch, and
    the new CI step + test file per binding rule 7.
  - [x] `docs/dev4-tracker.md` — add this task under "Bug Resolution — Feature Sprint 2" as BR-5
    (next available local number; distinct from the differently-prefixed `bug-resolution/br-5-*`
    branch namespace another dev is using independently — no collision, confirmed via
    `git ls-remote` before choosing this number), flip to `[Completed]` on close, update the
    Quick Status Dashboard and header date.

## Dev Notes

- **Why this isn't scoped to "add monitoring/alerting."** D146's own disposition separately names
  "no alert exists (W10-2 monitoring isn't built yet)" as a distinct, larger, already-tracked gap
  (CLAUDE.md's Build Roadmap, Week 10). This story is deliberately the narrower, immediately
  actionable piece: catch the missing secret in CI, before Fly ever boots, rather than requiring a
  monitoring stack that doesn't exist yet to catch it after boot.
- **Why the manifest is introspected, not hand-maintained.** The exact failure class this story
  guards against — "the code's real requirement and someone's separately-maintained understanding
  of it silently diverge" — is what happened once already (D49's fix existed in code; the
  deployed environment's secret set was a human's separate, unverified assumption). A hand-typed
  Python list of required env var names would reproduce the identical risk one layer up. Deriving
  the pydantic-required portion directly from `Settings.model_fields` removes that whole class for
  every future required `Settings` field, at zero ongoing maintenance cost.
- **Why `RATE_LIMIT_STORAGE_URL` still needs a hand-listed entry.** It is not (and should not
  become) a required `Settings` field — `memory://` is a legitimate default for local
  single-process development (`assert_rate_limit_storage_configured`'s own `debug=True` exemption
  depends on this). The guard's requirement is conditional ("required outside debug"), which
  pydantic's plain required/optional model can't express — hence the second, explicitly
  anti-drift-tested source.
- **`flyctl` availability in CI.** `deploy.yml` already installs a pinned `flyctl` version
  (`0.4.77`, pinned 2026-08-17 per that file's own comment — "latest" broke auth from this
  runner) before its deploy step; Task 2's new step reuses that same installed binary, added
  nothing new to install.
- **Testing standard**: per CLAUDE.md's Defect Register binding rule 2, `_fetch_configured_secret_names`'s
  failure-path tests must assert the real raised exception / real exit code — not merely that
  `subprocess.run` was called with certain arguments.

### Project Structure Notes

- New file: `scripts/check_fly_secrets_configured.py` (repo-root `scripts/`, alongside
  `ws_load_test.py`, `check_dev4_progress.py` — matches existing convention for
  operational/CI scripts that aren't part of the `apps/api` package).
- New test: `apps/api/tests/test_check_fly_secrets_configured.py` — imported by file path via
  `importlib`, exactly mirroring `apps/api/tests/test_ws_load_test.py`'s existing pattern for a
  `scripts/`-resident script; this keeps the script runnable standalone (`python
  scripts/check_fly_secrets_configured.py --app hie-api`) without needing `apps/api` on
  `PYTHONPATH` for normal (non-test) use, while still letting pytest exercise its internals.
- `.github/workflows/deploy.yml` — one new step only; no change to the existing
  `flyctl deploy --remote-only` step or its `FLY_API_TOKEN` env wiring.
- No Supabase migration, no `packages/shared` contract, no frontend change — backend/CI-only.

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (claude-sonnet-5), 2026-09-04.

### Debug Log References

- RED confirmed before GREEN: `test_check_fly_secrets_configured.py` collection failed with
  `FileNotFoundError` (script didn't exist yet) before `scripts/check_fly_secrets_configured.py`
  was written.
- **Real bug found live, not in review:** dev-verifying the script by running it directly
  (`python scripts/check_fly_secrets_configured.py --app hie-api`) — `flyctl` isn't installed on
  this dev machine, and `subprocess.run` raises `FileNotFoundError` directly rather than returning
  a non-zero exit code. The original `_fetch_configured_secret_names` only caught
  `returncode != 0` and bad JSON, so this propagated as a raw, uncaught traceback past `main()`'s
  `except RuntimeError` — the same "silent-unclean-failure" class AC2 exists to rule out, just via
  a crash instead of a false OK. Fixed (catch `FileNotFoundError` + `subprocess.TimeoutExpired`,
  re-raise as the same clean `RuntimeError`) and regression-locked with 2 new tests before moving
  on — confirmed the fix live: re-running the script now prints a clean `ERROR: flyctl executable
  not found on PATH...` and exits 1, not a traceback.
- Mutation check on `list_missing_secrets`: replaced with `return []`, confirmed exactly 3 of 14
  tests reddened (the ones depending on it), restored, confirmed 14/14 green again.
- `ruff check`/`ruff format --check` clean on both touched files (one `PLW1510` finding — missing
  explicit `check=` on `subprocess.run` — fixed by adding `check=False`, since the non-zero-exit
  case is deliberately handled explicitly just below, not left to `CalledProcessError`).
- Full `apps/api/tests` unit suite: this dev sandbox was missing several packages entirely
  (`python-multipart`, `tinytag`, `fpdf`, `fakeredis`) — confirmed via `git stash` that this
  predates this branch and is unrelated to any file this story touches. After installing them:
  **35 failed / 2243 passed / 32 skipped.** All 35 pre-existing and outside this story's changed
  files — 3 are the already-registered **D143** (`test_tutor_graph.py` fatigue tests, import-order
  binding bug), the rest are stale/absent eval-PDF fixture files and other onboarding/session/
  posthog endpoint failures untouched by this branch. Confirmed directly (not assumed): neither
  `test_check_fly_secrets_configured.py` nor D49's own `test_rate_limit_storage_guard.py` appear
  anywhere in the failure list.
- `dev4/master-bug-resolution` (this story's base integration branch) was cut from `main` on
  2026-08-27, before PR #159 (D49's guard) and D146 existed — merged current `main` into this
  branch first (`f26a3b7`) so the code this story extends was actually present to build against.

### Completion Notes List

- AC1–AC6 all met. `scripts/check_fly_secrets_configured.py` + 14 tests in
  `apps/api/tests/test_check_fly_secrets_configured.py`, mutation-checked.
- `.github/workflows/deploy.yml`: added `actions/setup-python@v5` (this job never ran Python
  before — pinned "3.12" to match `ci.yml`), a minimal `pip install "pydantic>=2.7.0"
  "pydantic-settings>=2.2.0"` step (deliberately not the full `uv sync --locked` `apps/api` tree,
  which pulls in `docling`'s heavy ML/torch install path — disproportionate for a script that only
  needs `Settings.model_fields`'s shape, no instance, no real secret values), and the verification
  step itself, all before the existing `flyctl deploy --remote-only` step. No `continue-on-error`
  anywhere — a non-zero exit from the new step blocks the job by GitHub Actions' default behavior.
- `docs/DEFECT-REGISTER.md`: new **D150** entry, re-verified against a freshly-fetched `origin/main`
  immediately before writing it (per the register's own binding collision-avoidance rule) —
  highest allocated id was D148 at write time.
- `docs/dev4-tracker.md`: added BR-5 under "Bug Resolution — Feature Sprint 2"; dashboard row
  (4→5 tasks, 0→1 Completed) and header (36/48 Completed, last-updated date) reconciled.
- **Not done in this branch:** the 6-agent `/bmad-code-review` gate CLAUDE.md requires before
  merge. Implementation, tests, and register/tracker close-out are complete; the adversarial
  review is the explicit next step, not silently skipped.

### File List

- `scripts/check_fly_secrets_configured.py` — new
- `apps/api/tests/test_check_fly_secrets_configured.py` — new, 14 tests
- `.github/workflows/deploy.yml` — 3 new steps (setup-python, minimal pip install, the check)
- `docs/DEFECT-REGISTER.md` — new D150 entry
- `docs/dev4-tracker.md` — new BR-5 task entry
- `docs/stories/br-5-deploy-secrets-verification.md` — this file

### References

- [Source: docs/DEFECT-REGISTER.md#D146] — the incident this story's fix is a direct response to.
- [Source: docs/DEFECT-REGISTER.md#D145] — the sibling live-Fly-config-drift class (region list),
  named for context, not fixed by this story.
- [Source: docs/DEFECT-REGISTER.md#D49] — the guard whose deploy-time verification was missing.
- [Source: apps/api/app/core/rate_limit.py:101-144] — `assert_rate_limit_storage_configured`,
  the guard this story's manifest cross-references.
- [Source: apps/api/app/config.py:33-108] — `Settings`, the pydantic-required fields
  (`supabase_url`, `supabase_anon_key`, `supabase_service_role_key`, `supabase_jwt_secret`,
  `openai_api_key`, `sarvam_api_key`, `langfuse_public_key`, `langfuse_secret_key`) this story's
  manifest introspects live rather than hand-copies.
- [Source: apps/api/app/main.py:59-70] — `lifespan()`'s existing startup-guard call ordering,
  unchanged by this story (this story is deploy-time/CI, not app-startup code).
- [Source: apps/api/tests/unit/test_rate_limit_storage_guard.py] — the D49 guard's own test file;
  this story's Task 3 tests are a sibling, not a replacement.
- [Source: apps/api/tests/test_ws_load_test.py:1-28] — the existing `importlib`-by-file-path
  pattern this story's new test file mirrors for loading a `scripts/`-resident module.
- [Source: .github/workflows/deploy.yml] — existing deploy job: `concurrency` group (Scale & Load
  Q6), pinned `flyctl` 0.4.77 install, `FLY_API_TOKEN` wiring, the step this story's new step
  precedes.
- [Source: fly.toml] — one app (`hie-api`), two process groups (`api`, `worker`) sharing one
  secret namespace (Scale & Load Q3).
- [Source: CLAUDE.md#Defect-Register — binding rules 5, 7] — "a documented limitation needs a
  D-nn register ID," "a fix without a guard is FIXED-UNGUARDED."
