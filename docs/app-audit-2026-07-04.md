# App-Wide Frontend Audit — 2026-07-04

**Scope:** `apps/web` (Dev 2's domain) — the entire frontend, not a diff. Backend (`apps/api`) and other devs' domains are out of scope.
**Method:** 5 parallel review agents, each covering a subsystem (player/lesson, auth/middleware/shell, dashboard/upload/library/settings, landing/marketing, test coverage), run after S2-03 (Onboarding Assessment Flow) shipped. 37 distinct findings, then cross-referenced against `docs/dev2-sprint-tracker.md` to separate real bugs from already-planned future-sprint work.

## Legend

| Tag | Meaning |
|---|---|
| 🐛 **Bug** | A defect in code the tracker marks `✅ DONE` — a real regression, not excused by scope |
| 📅 **Known gap** | The tracker already lists this as `🔲 NOT STARTED` for a specific future sprint — expected, not urgent |
| 🆕 **New gap** | Not mentioned anywhere in the tracker before this audit — needs to be triaged into a future sprint |
| ⚠️ **Compliance** | Violates an explicit hard rule in `CLAUDE.md`, regardless of sprint — always urgent |

**Status** column: `Open` / `✅ Patched 2026-07-04` / `Documented only (not patched this pass)`.

---

## 🔴 Critical

| # | Finding | File | Tag | Status |
|---|---|---|---|---|
| 1 | `/auth/callback` was missing from `middleware.ts`'s `PUBLIC_PATHS`. Every request to it — including the OAuth/email-confirmation code-exchange redirect — ran through the auth gate *before* `exchangeCodeForSession()` could run, and got bounced to `/signin` with no session yet. **This broke 100% of Google sign-in and email-confirmation links.** Root cause: a regression from S1-13's allow-list → deny-list rewrite (2026-07-02), which fixed 4 real unauth'd routes but never added this one. No test covered it. | `apps/web/src/middleware.ts` | 🐛 Bug (S1-13 regression) | ✅ Patched 2026-07-04 |
| 2 | Public `Footer.tsx` copy reads *"not raw IQ, EQ, or SQ scores"* — CLAUDE.md bans this terminology anywhere in the product, even as a negation. | `apps/web/src/components/layout/Footer.tsx:46` | ⚠️ Compliance | ✅ Patched 2026-07-04 |
| 3 | Open redirect: the auth callback's `next` query param is used in raw string concatenation (`${origin}${next}`) instead of being validated as a same-origin relative path. Low exploitability today (both callers hardcode `/dashboard`), but a real, unguarded parameter. | `apps/web/src/app/auth/callback/route.ts` | 🐛 Bug / security | ✅ Patched 2026-07-04 |

---

## 🟡 High / Medium

| # | Finding | File | Tag | Status |
|---|---|---|---|---|
| 4 | Settings tabs (`ProfileTab`, `LearningTab`, `PrivacyTab`) don't call `settingsService` or its mocks at all — `LearningTab` literally comments "Dummy state for settings." Even if wired up, the UI's enum values (`pace: relaxed/balanced/intensive`) don't match the real `LearningPreferences` type (`pace: relaxed/moderate/accelerated`), and `NotificationsTab` has a "Product Updates" toggle with no corresponding field in `NotificationSettings`. The tracker's Primary Pages section assumed these tabs "exist" (implying at least mock-wired) — they're not wired to anything. | `apps/web/src/components/settings/tabs/*.tsx` | 🆕 New gap | ✅ Patched 2026-07-04 — all 4 tabs fetch/persist through `settingsService`; `LearningTab`'s enums corrected to match the real `LearningPreferences` type; new `PrivacySettings` type+mock+service added (previously didn't exist); "Product Updates" toggle removed (no backing field); `ProfileTab` now shows the real mock identity |
| 5 | Real `/lesson/[id]` route always returns the same hardcoded mock lesson regardless of `lessonId` (S1-08/S1-09/S1-10 real-API work is tracked as blocked on Dev 1's backend — expected). The newly-surfaced risk: quiz/teach-back submissions from that same route go through the **real, authenticated** `lib/assessment.ts` → real backend, carrying a bogus `lesson_id`/`segment_id` and a client-generated `session_id` never registered server-side. Will reject or write garbage once pointed at a real backend. | `apps/web/src/services/lesson.service.ts`, `apps/web/src/lib/assessment.ts` | 📅 Known gap (S1-08/09/10) + 🆕 new nuance | Documented only — needs backend session creation, not a frontend patch |
| 6 | The tutor WebSocket (`useLessonSocket`) is built, tested, and **correctly not called** by `Player`/`PlayerLoader` — its only consumers (`AttentionMonitor` S3-02, `TutorInterventionCard` S3-03, `CESIndicator` S3-04) are all `🔲 NOT STARTED` in Sprint 3. `tutorState` being stuck at `IDLE` and attention signals being unreachable is **expected, not a bug**, per S1-07's own tracker note ("the send side has no caller yet... unscoped work"). | `apps/web/src/hooks/useLessonSocket.ts` | 📅 Known gap (Sprint 3) | Not a bug — no action needed |
| 7 | Segment replay bug: seeking backward to replay a segment whose quiz already fired, then letting that segment's audio reach its natural end, leaves the player silently frozen (`status` stays `PLAYING`, UI shows a pause icon, but the new `<audio>` element never gets `.play()` called since no `status` transition occurs). Recoverable only by manually toggling pause/play. This is a real bug in S1-03 (`AudioTimeline`), which is marked `✅ DONE`. | `apps/web/src/components/player/AudioTimeline.tsx` | 🐛 Bug | ✅ Patched 2026-07-04 — added `currentSegmentIndex` to the play/pause effect's dependency array; first component-level test coverage added for `AudioTimeline.tsx` (previously only its exported pure functions were tested) |
| 8 | Mid-lesson progress silently resets to segment 0 on browser tab-focus regain. `useLesson`'s `useSWR` call has no config, so SWR's default `revalidateOnFocus: true` refetches on focus; the mock fetcher returns a fresh object reference every call, and `Player`'s `useEffect([lesson, loadLesson])` treats that as a new lesson and resets the store. Thematically related to S2-05 (Player State Persistence, `🔲 NOT STARTED`) but a distinct root cause S2-05 wouldn't fix by itself. | `apps/web/src/hooks/useLesson.ts` | 🐛 Bug | ✅ Patched 2026-07-04 — `revalidateOnFocus: false` |
| 9 | `AuthContext` never subscribes to `supabase.auth.onAuthStateChange`. The tracker's **own** Section 15 cross-cutting risk table already prescribes this exact mitigation for "Auth token expiry mid-lesson" — it was never implemented. `user` can also stay stale/null right after a successful sign-in since nothing re-fetches session state post-login. `logout()` also doesn't reset local state if `signOut()` throws. | `apps/web/src/contexts/AuthContext.tsx` | 🐛 Bug (tracker's own prescribed mitigation, unimplemented) | ✅ Patched 2026-07-04 — live subscription added, `logout()` now fails closed, `getUser()` failures set a real `error` message |
| 10 | Dashboard Hero's two primary CTAs ("Resume Journey", "Upload PDF") have no `onClick`/`href` — they do nothing when clicked. Highest-visibility broken control in the app. | `apps/web/src/components/dashboard/sections/HeroSection.tsx:40-47` | 🆕 New gap | ✅ Patched 2026-07-04 — wired to `/lesson/{id}` (or `/upload` if none in progress) and `/upload` respectively |
| 11 | Privacy tab's camera-consent toggle defaults to **on**, is local `useState` only, and doesn't persist. Notable given CLAUDE.md's own DPDP consent-audit priority (the real `user_consents` table already exists per Story 3-17) — this UI doesn't hook into it at all yet. | `apps/web/src/components/settings/tabs/PrivacyTab.tsx:8` | 🆕 New gap, compliance-adjacent | ⚠️ Partially patched 2026-07-04 (via #4) — now persists through a mock `PrivacySettings` service, but is explicitly commented as NOT the real DPDP `user_consents` consent record; that backend integration (S3-01) is still open |
| 12 | `AudioTimeline` will throw an uncaught `TypeError` if a segment's `narration.timestamps` is ever empty (`timestamps[idx].slide_id`, `timestamps.at(-1)!.end_ms` with no length guard) — no error boundary anywhere in the tree to catch it. Requires malformed pipeline output to trigger; latent, not currently reproducible with the well-formed mock data. | `apps/web/src/components/player/AudioTimeline.tsx` | 🐛 Bug (defensive gap) | ✅ Patched 2026-07-04 — early return when `timestamps.length === 0` |
| 13 | Hardcoded stock Unsplash thumbnail arrays baked directly into `RecentLessons.tsx` and `LibraryView.tsx` (picked via `index % 3` / digits parsed from `lesson.id`) instead of living in the service/mock layer — will keep showing random stock photos once real thumbnail URLs exist, masking a missing-integration bug. | `apps/web/src/components/dashboard/sections/RecentLessons.tsx`, `apps/web/src/components/library/LibraryView.tsx` | 🆕 New gap | Documented only |
| 14 | Broad accessibility gaps: interactive `<div>`/`<span>` cards with `onClick` but no keyboard/ARIA support across dashboard/library; `QuizOverlay`/`TeachBackModal` have no dialog semantics or focus trap; `PlayerControls`' seek slider has no keyboard support; FAQ accordion missing `aria-expanded`; ~~`Toggle` missing `role="switch"`~~; ~2.7:1 contrast text on the dark `FinalCTA` section (WCAG AA needs 4.5:1). **All of this is explicitly covered by S4-04 "Accessibility Audit (WCAG AA)", `🔲 NOT STARTED`.** Correctly deferred, not urgent — but worth having this concrete pre-built findings list ready for whoever picks up S4-04. | Multiple — see S4-04 file list | 📅 Known gap (S4-04) | Not urgent, mostly — `Toggle`'s `role="switch"`/`aria-checked` ✅ patched 2026-07-04 as a side effect of #4 (touched every usage anyway); the rest feeds into S4-04 when scheduled |
| 15 | Server components (`dashboard/page.tsx`, `library/page.tsx`) have zero error handling around service calls; `library/page.tsx` uses a non-null assertion (`response.data!`) that would throw a runtime error on a real null API response. Today's mock layer never actually rejects, so this path is never exercised — will surface the moment S1-09/S1-10 land. | `apps/web/src/app/(dashboard)/{dashboard,library}/page.tsx` | 🆕 New gap (latent until S1-09/10) | Documented only |
| 16 | Hand-rolled buttons instead of the shared `Button` component, broadly, across dashboard/upload/settings (`ContinueLearningCard`, `UploadFlow`, `AccountTab`, etc.) — same class of issue just fixed in the onboarding flow this session. | Multiple — see full agent report | 🆕 New gap (design consistency) | Documented only |

---

## 🟢 Low priority / cosmetic / dead code

| # | Finding | File | Tag |
|---|---|---|---|
| 17 | Landing page: ~half the sections (`TheCrisis`, `TransformationPromise`, `JourneyToSelfReliance`, `Features`, `HowItWorks`, `CognitiveVisualization`, `ProductPreview`) still use raw `slate-*` classes / bare hex literals instead of brand tokens, violating the tracker's own Design System rule ("no hardcoded hex values"). **Explicitly covered by S4-01 "Landing Page + Pricing Polish", `🔲 NOT STARTED`.** | `apps/web/src/components/sections/*.tsx` | 📅 Known gap (S4-01) |
| 18 | Pricing "Teams" plan CTA is a dead `href="#"` link on a monetization path — worse than "needs polish," it's simply broken. Sits inside the same S4-01 umbrella but worth flagging as higher priority than the color-token cleanup once S4-01 starts. | `apps/web/src/components/sections/Pricing.tsx:60-61` | 📅 Known gap (S4-01), prioritize within it |
| 19 | `WhyTransformED.tsx` (exported as `WhyHIE`) is a fully-built section never imported/rendered anywhere on the public page. Filename is also a rebrand leftover. | `apps/web/src/components/sections/WhyTransformED.tsx` | 🆕 Dead code |
| 20 | Footer Privacy/Terms links are `href="#"` placeholders, while the FAQ section makes explicit privacy claims ("your PDFs are encrypted... no webcam data leaves your browser") with no policy page to back them. Likely an accepted pre-launch stub, but a public trust/compliance surface worth a real page before launch. | `apps/web/src/components/layout/Footer.tsx:18-28` | 🆕 New gap |
| 21 | `uploadService` (REST-style, `getUploadHistory`/`uploadFile`/`getUploadStatus`) is orphaned — `UploadFlow` only uses the WebSocket-based `uploadGenerationService`. Two incompatible "upload" mental models coexist; unclear which one real API integration replaces. | `apps/web/src/services/upload.service.ts` | 🆕 Dead code / maintenance trap |
| 22 | Contradictory hardcoded "current user" identities: `HeroSection` says "Robert", `ProfileTab` shows "Alex Student", mock data (`mocks/data/users.ts`) has "J. Robert Oppenheimer" — confirms the profile data path is never actually exercised anywhere. | `HeroSection.tsx`, `ProfileTab.tsx`, `mocks/data/users.ts` | 🆕 New gap — ⚠️ partially patched 2026-07-04 (via #4): `ProfileTab` now fetches the real mock profile; `HeroSection`'s "Good evening, Robert" greeting is still static JSX text, not fetched — left as-is since wiring the greeting wasn't part of #4's scope |
| 23 | `usePlayerMachine.ts` is entirely unused — every real component reads `usePlayerStore` directly instead. Its own doc comment claims selectors return "stable references — safe to destructure," which is false for a plain Zustand object-literal selector. Also dead inside `player.machine.ts`: `enterTeachBack` (duplicates `exitQuiz`'s transition, never called) and `setSessionId` (never invoked). | `apps/web/src/hooks/usePlayerMachine.ts`, `apps/web/src/stores/player.machine.ts` | 🆕 Dead code |
| 24 | `AuroraBackground.tsx` is defined but never imported anywhere (its sibling `MouseAmbientGlow` *is* wired up in `layout.tsx`) — looks like an abandoned/replaced background effect. | `apps/web/src/components/ui/AuroraBackground.tsx` | 🆕 Dead code |
| 25 | ~~Dead "View Path" / "View All" `<span>` links styled as clickable with no `onClick`/`href`.~~ | `ContinueLearningCard.tsx:27-29`, `RecentLessons.tsx:19-21` | 🆕 New gap — ✅ Patched 2026-07-04 (now real buttons, navigate to `/library`) |
| 26 | ~~`ContinueLearningCard` hardcodes "Last opened 2 hours ago" instead of using the real `lastAccessed` timestamp already present in mock data; also imports `Button` but never uses it, rendering its CTA as a hand-styled `<div>` instead.~~ | `apps/web/src/components/dashboard/sections/ContinueLearningCard.tsx` | 🆕 New gap — ✅ Patched 2026-07-04 (new `lib/utils.ts#formatTimeAgo`; Resume now uses the shared `Button`) |
| 27 | Misc unused imports: `HowItWorks.tsx` (`MessageSquare`, `BarChart3`, `CheckCircle2`), `TransformationPromise.tsx` (`Focus`, `BrainCircuit`), `LearningPulse.tsx` (`motion`, `Compass`), `Features.tsx`'s `PhaseCard` has an unused `index` prop. | Multiple | 🆕 Lint-level |
| 28 | Duplicate/conflicting Tailwind class: `lg:grid-cols-[auto_auto_auto] lg:grid-cols-[1fr_auto_1fr]` declared twice on the same element — one is dead. | `TransformationPromise.tsx:41` | 🆕 Lint-level |
| 29 | `QuickActions` hardcodes "Access your 12 processed materials" — doesn't match the 5-lesson mock library and will drift further as mock data changes. | `apps/web/src/components/dashboard/sections/QuickActions.tsx:20` | 🆕 New gap |
| 30 | Confusing parallel module naming: `lib/ws/lessonSocket.ts` (real tutor client) vs `lib/websocket/mockSocket.ts` (upload-progress mock) — no live leakage today, but an easy future wrong-import mistake. | `apps/web/src/lib/{ws,websocket}/*` | 🆕 Naming risk |
| 31 | 5 unused icon imports (`BookOpen`, `Globe`, `Briefcase`, `Code2`, `Mail`) found while patching #2 above. | `apps/web/src/components/layout/Footer.tsx:2` | 🆕 Lint-level |

---

## 📋 Test coverage gaps (shipped code with zero tests)

Untested, but the underlying feature is already `✅ DONE` in the tracker (not "not built yet") — these are real coverage gaps, not expected mock-stage gaps:

- ~~`apps/web/src/app/auth/callback/route.ts` — no test on the code/error/no-code branches or the `next` redirect~~ ✅ 6 tests added 2026-07-04 alongside the Critical #1/#3 patches
- ~~`apps/web/src/contexts/AuthContext.tsx` — no test on `fetchSession`/`logout`/error-state paths~~ ✅ 8 tests added 2026-07-04 alongside the #9 patch
- `apps/web/src/lib/supabase/middleware.ts` — fully mocked out in `middleware.test.ts`, so its real cookie-propagation logic has zero real coverage
- `apps/web/src/lib/assessment.ts` — `submitQuiz`/`submitTeachBack` payload shapes untested; both callers (`QuizOverlay`, `TeachBackModal`) wrap calls in empty `catch` blocks, so a broken field name fails with zero signal anywhere
- `apps/web/src/services/uploadGeneration.service.ts` — singleton WS lifecycle (connect/disconnect/double-subscribe) untested; `UploadFlow` depends on `disconnect()` to stop a background loop on unmount
- `apps/web/src/components/dashboard/upload/UploadFlow.tsx` — no test on any socket-event branch or unmount-mid-generation cleanup
- `apps/web/src/components/player/{AudioTimeline,Player,PlayerControls,QuizOverlay,TeachBackModal}.tsx` — zero tests; exactly where the segment-freeze bug (#7) and empty-timestamps crash (#12) live
- `apps/web/src/components/auth/{SignInForm,SignUpForm}.tsx` — no test on failed-credentials, OAuth-error query param, or redirect-on-success
- ~~`apps/web/src/hooks/useLesson.ts` — no test on loading/error/null-lessonId branches~~ ✅ 3 tests added 2026-07-04 alongside the #8 patch
- Thin service wrappers (`dashboard`, `library`, `reports`, `settings`, `upload`, `lesson`, `uploadGeneration` `.service.ts`) — no test locks down expected method signatures before they're swapped from mocks to real calls

---

## Summary

| Category | Count |
|---|---|
| 🔴 Critical (3/3 patched) | 3 |
| 🟡 High/Medium (6 fully patched: #4, #7, #8, #9, #10, #12; 2 partially: #11, #14; 5 still open: #5, #6*, #13, #15, #16) | 13 |
| 🟢 Low/cosmetic/dead code (2 fully patched: #25, #26; 1 partial: #22; 11 still open) | 14 |
| 📋 Test coverage gaps | ~10 files (5 closed today: `/auth/callback`, `AuthContext`, `useLesson`, `AudioTimeline` component, all 4 settings tabs) |
| **Confirmed NOT bugs** (correctly-scoped future-sprint work) | 3 (tutor WS wiring* = #6, accessibility pass = #14's remainder, landing brand polish = #17/#18) |

**What got fixed today**, on branch `sprint2/codebase-audit-fixes` (based on `main`, independent of the still-unmerged `sprint2/s2-3-onboarding-flow`):

*Round 1 — Critical (#1, #2, #3):*
- `apps/web/src/middleware.ts` — added `/auth/callback` to `PUBLIC_PATHS`
- `apps/web/src/app/auth/callback/route.ts` — added `safeNextPath()` validation (same-origin relative paths only) for the `next` redirect param
- `apps/web/src/components/layout/Footer.tsx` — removed "IQ, EQ, or SQ" from the public tagline

*Round 2 — High/Medium + cheap wins (#8, #9, #10, #25, #26):*
- `apps/web/src/contexts/AuthContext.tsx` — live `onAuthStateChange` subscription, fail-closed `logout()`, real `error` messages
- `apps/web/src/hooks/useLesson.ts` — `revalidateOnFocus: false`
- `apps/web/src/components/dashboard/sections/{HeroSection,ContinueLearningCard,RecentLessons}.tsx` — wired the dead CTAs, replaced the hardcoded "2 hours ago" with a real `formatTimeAgo()` (new in `lib/utils.ts`), replaced a hand-rolled Resume `<div>` with the shared `Button`

*Round 3 — Player sync bugs (#7, #12):*
- `apps/web/src/components/player/AudioTimeline.tsx` — fixed the segment-replay freeze (added `currentSegmentIndex` to the play/pause effect's deps) and the empty-timestamps crash guard; added the component's first-ever test coverage (previously only its exported pure functions were tested)

*Round 4 — Settings tabs (#4, partially #11/#14/#22):*
- All 4 tabs (`ProfileTab`, `LearningTab`, `NotificationsTab`, `PrivacyTab`) now fetch/persist through `settingsService` instead of pure local dummy state
- `LearningTab`'s enum values corrected to match the real `LearningPreferences` type
- New `PrivacySettings` type + mock + service functions (didn't exist before); Camera-Based Focus Detection toggle explicitly commented as UI-only, not the real DPDP consent record
- `NotificationsTab`'s unbacked "Product Updates" toggle removed; new `updateNotificationSettings` mock+service added
- `ProfileTab` now shows the real mock identity instead of hardcoded "Alex Student"
- `Toggle.tsx` — added `role="switch"`/`aria-checked` (touched every usage anyway)

**Verification:** every fix across all 4 rounds followed red→green — a failing regression test was written first to prove the bug, then the fix. 59 new tests added total. Full suite: 178/178 passing, `tsc --noEmit` clean, lint clean (0 errors; a few pre-existing unrelated warnings noted inline as low-priority findings #27/#31, plus one new `<img>`-optimization warning in `ProfileTab.tsx` from an already-existing `<img>` tag whose `src` value I changed).

Commits: `d199c05` (docs), `4423ed6` (round 1 — critical), `3ab232e` (round 2 — #8/#9/#10/#25/#26), `586d26e` (docs update), `fae84c2` (round 3 — #7/#12), `346a31a` (round 4 — #4/#11/#14/#22).

**Remaining open items, ranked**: #13/#5 (mock-data integration risks, need backend coordination) → #15/#16 (design-system consistency: hand-rolled buttons, server-component error handling) → #19/#20/#21/#23/#24 (dead code cleanup) → feed #14's remainder/#17/#18 into S4-04/S4-01 when those sprints start → #6 stays correctly un-actioned until Sprint 3.
